// AUTO GENERATED FILE, DO NOT EDIT.
//
// Generated by `package:ffigen`.
// ignore_for_file: type=lint, unused_import, unused_element, deprecated_member_use_from_same_package
import 'dart:ffi' as ffi;

/// Auto-generated FFI bindings for zenoh_dart C shim.
class ZenohDartBindings {
  /// Holds the symbol lookup function.
  final ffi.Pointer<T> Function<T extends ffi.NativeType>(String symbolName)
  _lookup;

  /// The symbols are looked up in [dynamicLibrary].
  ZenohDartBindings(ffi.DynamicLibrary dynamicLibrary)
    : _lookup = dynamicLibrary.lookup;

  /// The symbols are looked up with [lookup].
  ZenohDartBindings.fromLookup(
    ffi.Pointer<T> Function<T extends ffi.NativeType>(String symbolName) lookup,
  ) : _lookup = lookup;

  /// Declares a background matching listener on the advanced publisher.
  ///
  /// Posts an int64 1/0 to @p dart_port on every matching-status transition.
  /// Canon binds the listener's lifetime to the PUBLISHER: it runs until the
  /// corresponding advanced publisher is dropped.
  ///
  /// @return 0 on success; canon's own negative code on a canon failure;
  /// ZD_DECLARE_EALLOC (11) if the context allocation fails, in which
  /// case nothing was declared. Positive because canon owns 0-and-
  /// negative on this channel.
  int zd_advanced_publisher_declare_background_matching_listener(
    ffi.Pointer<ffi.Opaque> publisher,
    int dart_port,
  ) {
    return _zd_advanced_publisher_declare_background_matching_listener(
      publisher,
      dart_port,
    );
  }

  late final _zd_advanced_publisher_declare_background_matching_listenerPtr =
      _lookup<
        ffi.NativeFunction<ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Int64)>
      >('zd_advanced_publisher_declare_background_matching_listener');
  late final _zd_advanced_publisher_declare_background_matching_listener =
      _zd_advanced_publisher_declare_background_matching_listenerPtr
          .asFunction<int Function(ffi.Pointer<ffi.Opaque>, int)>();

  /// Sends a DELETE through the advanced publisher.
  int zd_advanced_publisher_delete(
    ffi.Pointer<ffi.Opaque> publisher,
  ) {
    return _zd_advanced_publisher_delete(
      publisher,
    );
  }

  late final _zd_advanced_publisher_deletePtr =
      _lookup<ffi.NativeFunction<ffi.Int Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_advanced_publisher_delete',
      );
  late final _zd_advanced_publisher_delete = _zd_advanced_publisher_deletePtr
      .asFunction<int Function(ffi.Pointer<ffi.Opaque>)>();

  /// Drops (undeclares and frees) an advanced publisher.
  void zd_advanced_publisher_drop(
    ffi.Pointer<ffi.Opaque> publisher,
  ) {
    return _zd_advanced_publisher_drop(
      publisher,
    );
  }

  late final _zd_advanced_publisher_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_advanced_publisher_drop',
      );
  late final _zd_advanced_publisher_drop = _zd_advanced_publisher_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Reads whether any subscriber currently matches the advanced publisher's
  /// key expression.
  ///
  /// @param matching  Out-param, written 1/0 ONLY on rc 0. On any error it is
  /// left untouched, mirroring canon's own contract
  /// ("in this case matching_status is not updated").
  /// @return canon's rc verbatim: 0 on success, negative on a canon failure.
  /// This entry adds no failure code of its own.
  int zd_advanced_publisher_get_matching_status(
    ffi.Pointer<ffi.Opaque> publisher,
    ffi.Pointer<ffi.Int> matching,
  ) {
    return _zd_advanced_publisher_get_matching_status(
      publisher,
      matching,
    );
  }

  late final _zd_advanced_publisher_get_matching_statusPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Int>)
        >
      >('zd_advanced_publisher_get_matching_status');
  late final _zd_advanced_publisher_get_matching_status =
      _zd_advanced_publisher_get_matching_statusPtr
          .asFunction<
            int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Int>)
          >();

  /// Obtains a const loaned reference to the advanced publisher.
  ffi.Pointer<ffi.Opaque> zd_advanced_publisher_loan(
    ffi.Pointer<ffi.Opaque> publisher,
  ) {
    return _zd_advanced_publisher_loan(
      publisher,
    );
  }

  late final _zd_advanced_publisher_loanPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_advanced_publisher_loan');
  late final _zd_advanced_publisher_loan = _zd_advanced_publisher_loanPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Publishes data through the advanced publisher.
  int zd_advanced_publisher_put(
    ffi.Pointer<ffi.Opaque> publisher,
    ffi.Pointer<ffi.Opaque> payload,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
    ffi.Pointer<ffi.Opaque> attachment,
  ) {
    return _zd_advanced_publisher_put(
      publisher,
      payload,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
      attachment,
    );
  }

  late final _zd_advanced_publisher_putPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_advanced_publisher_put');
  late final _zd_advanced_publisher_put = _zd_advanced_publisher_putPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Opaque>,
        )
      >();

  /// Returns the size of ze_owned_advanced_publisher_t in bytes.
  int zd_advanced_publisher_sizeof() {
    return _zd_advanced_publisher_sizeof();
  }

  late final _zd_advanced_publisher_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>(
        'zd_advanced_publisher_sizeof',
      );
  late final _zd_advanced_publisher_sizeof = _zd_advanced_publisher_sizeofPtr
      .asFunction<int Function()>();

  /// Declares a background sample miss listener on the advanced subscriber.
  int zd_advanced_subscriber_declare_background_sample_miss_listener(
    ffi.Pointer<ffi.Opaque> subscriber,
    int dart_port,
  ) {
    return _zd_advanced_subscriber_declare_background_sample_miss_listener(
      subscriber,
      dart_port,
    );
  }

  late final _zd_advanced_subscriber_declare_background_sample_miss_listenerPtr =
      _lookup<
        ffi.NativeFunction<ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Int64)>
      >('zd_advanced_subscriber_declare_background_sample_miss_listener');
  late final _zd_advanced_subscriber_declare_background_sample_miss_listener =
      _zd_advanced_subscriber_declare_background_sample_miss_listenerPtr
          .asFunction<int Function(ffi.Pointer<ffi.Opaque>, int)>();

  /// Declares a background subscriber on the liveliness tokens of matching
  /// advanced publishers that enable publisher detection.
  ///
  /// Posts ordinary sample arrays to @p dart_port — PUT-kind on a publisher's
  /// appearance, DELETE-kind on its disappearance — and posts a NULL sentinel
  /// from its drop callback so the Dart side can complete the stream.
  ///
  /// Canon binds this listener's lifetime to the SESSION: it runs until the
  /// corresponding session is closed or dropped, outliving the advanced
  /// subscriber it was declared on.
  ///
  /// @param history  **-1 means UNSPECIFIED**: NULL options are passed and
  /// canon's own default applies (history = false). 0 or 1
  /// fills `z_liveliness_subscriber_options_t.history`.
  /// @return 0 on success; canon's own negative code on a canon failure;
  /// ZD_DECLARE_EALLOC (11) if the context allocation fails, in which
  /// case nothing was declared and no sentinel will arrive.
  int zd_advanced_subscriber_detect_publishers_background(
    ffi.Pointer<ffi.Opaque> subscriber,
    int dart_port,
    int history,
    int retain_payload,
  ) {
    return _zd_advanced_subscriber_detect_publishers_background(
      subscriber,
      dart_port,
      history,
      retain_payload,
    );
  }

  late final _zd_advanced_subscriber_detect_publishers_backgroundPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Int64, ffi.Int, ffi.Int)
        >
      >('zd_advanced_subscriber_detect_publishers_background');
  late final _zd_advanced_subscriber_detect_publishers_background =
      _zd_advanced_subscriber_detect_publishers_backgroundPtr
          .asFunction<int Function(ffi.Pointer<ffi.Opaque>, int, int, int)>();

  /// Drops (undeclares and frees) an advanced subscriber.
  void zd_advanced_subscriber_drop(
    ffi.Pointer<ffi.Opaque> subscriber,
  ) {
    return _zd_advanced_subscriber_drop(
      subscriber,
    );
  }

  late final _zd_advanced_subscriber_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_advanced_subscriber_drop',
      );
  late final _zd_advanced_subscriber_drop = _zd_advanced_subscriber_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Obtains a const loaned reference to the advanced subscriber.
  ffi.Pointer<ffi.Opaque> zd_advanced_subscriber_loan(
    ffi.Pointer<ffi.Opaque> subscriber,
  ) {
    return _zd_advanced_subscriber_loan(
      subscriber,
    );
  }

  late final _zd_advanced_subscriber_loanPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_advanced_subscriber_loan');
  late final _zd_advanced_subscriber_loan = _zd_advanced_subscriber_loanPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the size of ze_owned_advanced_subscriber_t in bytes.
  int zd_advanced_subscriber_sizeof() {
    return _zd_advanced_subscriber_sizeof();
  }

  late final _zd_advanced_subscriber_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>(
        'zd_advanced_subscriber_sizeof',
      );
  late final _zd_advanced_subscriber_sizeof = _zd_advanced_subscriber_sizeofPtr
      .asFunction<int Function()>();

  /// Clones owned bytes into a pre-allocated destination.
  ///
  /// Loans the source, then calls z_bytes_clone() to produce an independent
  /// copy that shares the underlying reference-counted data.
  ///
  /// @param dst  Pointer to an uninitialized z_owned_bytes_t (cast to uint8_t*).
  /// @param src  Pointer to a valid z_owned_bytes_t (cast to uint8_t*).
  /// @return 0 on success.
  int zd_bytes_clone(
    ffi.Pointer<ffi.Uint8> dst,
    ffi.Pointer<ffi.Uint8> src,
  ) {
    return _zd_bytes_clone(
      dst,
      src,
    );
  }

  late final _zd_bytes_clonePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Uint8>)
        >
      >('zd_bytes_clone');
  late final _zd_bytes_clone = _zd_bytes_clonePtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Uint8>)
      >();

  /// Copies a buffer into owned bytes.
  ///
  /// @param bytes  Pointer to an uninitialized z_owned_bytes_t.
  /// @param data   Pointer to the buffer data.
  /// @param len    Length of the buffer in bytes.
  /// @return 0 on success, negative on failure.
  int zd_bytes_copy_from_buf(
    ffi.Pointer<ffi.Opaque> bytes,
    ffi.Pointer<ffi.Uint8> data,
    int len,
  ) {
    return _zd_bytes_copy_from_buf(
      bytes,
      data,
      len,
    );
  }

  late final _zd_bytes_copy_from_bufPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Size,
          )
        >
      >('zd_bytes_copy_from_buf');
  late final _zd_bytes_copy_from_buf = _zd_bytes_copy_from_bufPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Uint8>, int)
      >();

  /// Drops (frees) owned bytes.
  ///
  /// After this call the owned bytes are in gravestone state.
  /// A second drop is a safe no-op.
  ///
  /// @param bytes  Pointer to a z_owned_bytes_t to drop.
  void zd_bytes_drop(
    ffi.Pointer<ffi.Opaque> bytes,
  ) {
    return _zd_bytes_drop(
      bytes,
    );
  }

  late final _zd_bytes_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_bytes_drop',
      );
  late final _zd_bytes_drop = _zd_bytes_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Converts a mutable SHM buffer into owned bytes (consuming the buffer).
  ///
  /// @param bytes  Pointer to an uninitialized z_owned_bytes_t.
  /// @param buf    Pointer to a z_owned_shm_mut_t (consumed).
  /// @return 0 on success, negative on failure.
  int zd_bytes_from_shm_mut(
    ffi.Pointer<ffi.Opaque> bytes,
    ffi.Pointer<ffi.Opaque> buf,
  ) {
    return _zd_bytes_from_shm_mut(
      bytes,
      buf,
    );
  }

  late final _zd_bytes_from_shm_mutPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_bytes_from_shm_mut');
  late final _zd_bytes_from_shm_mut = _zd_bytes_from_shm_mutPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
      >();

  /// Creates a slice iterator from loaned bytes and copies it to *iter.
  ///
  /// @param bytes  Const pointer to loaned bytes.
  /// @param iter   Pointer to caller-allocated z_bytes_slice_iterator_t.
  void zd_bytes_get_slice_iterator(
    ffi.Pointer<ffi.Opaque> bytes,
    ffi.Pointer<z_bytes_slice_iterator_t> iter,
  ) {
    return _zd_bytes_get_slice_iterator(
      bytes,
      iter,
    );
  }

  late final _zd_bytes_get_slice_iteratorPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<z_bytes_slice_iterator_t>,
          )
        >
      >('zd_bytes_get_slice_iterator');
  late final _zd_bytes_get_slice_iterator = _zd_bytes_get_slice_iteratorPtr
      .asFunction<
        void Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<z_bytes_slice_iterator_t>,
        )
      >();

  /// Checks whether owned bytes are backed by shared memory.
  ///
  /// Uses z_bytes_as_loaned_shm() to probe the bytes. If the call succeeds
  /// (returns 0), the bytes are SHM-backed.
  ///
  /// @param bytes  Pointer to a z_owned_bytes_t (cast to uint8_t*).
  /// @return 1 if SHM-backed, 0 otherwise.
  int zd_bytes_is_shm(
    ffi.Pointer<ffi.Uint8> bytes,
  ) {
    return _zd_bytes_is_shm(
      bytes,
    );
  }

  late final _zd_bytes_is_shmPtr =
      _lookup<ffi.NativeFunction<ffi.Int8 Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_bytes_is_shm',
      );
  late final _zd_bytes_is_shm = _zd_bytes_is_shmPtr
      .asFunction<int Function(ffi.Pointer<ffi.Uint8>)>();

  /// Returns the total number of bytes in the payload.
  ///
  /// FULL-WIDTH: `size_t`, matching canon's `z_bytes_len`. It was `int32_t`,
  /// which is a lossy narrowing of the contract's domain -- a payload in
  /// [2^31, 2^32) came back negative and one at or above 2^32 came back
  /// silently short. The callback receive legs were full-width all along; this
  /// is the sync extractor catching up.
  ///
  /// @param bytes  Pointer to a z_owned_bytes_t (cast to uint8_t*).
  /// @return Total number of bytes.
  int zd_bytes_len(
    ffi.Pointer<ffi.Uint8> bytes,
  ) {
    return _zd_bytes_len(
      bytes,
    );
  }

  late final _zd_bytes_lenPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_bytes_len',
      );
  late final _zd_bytes_len = _zd_bytes_lenPtr
      .asFunction<int Function(ffi.Pointer<ffi.Uint8>)>();

  /// Obtains a const loaned reference to the bytes.
  ///
  /// @param bytes  Pointer to a valid z_owned_bytes_t.
  /// @return Const pointer to the loaned bytes.
  ffi.Pointer<ffi.Opaque> zd_bytes_loan(
    ffi.Pointer<ffi.Opaque> bytes,
  ) {
    return _zd_bytes_loan(
      bytes,
    );
  }

  late final _zd_bytes_loanPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_bytes_loan');
  late final _zd_bytes_loan = _zd_bytes_loanPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the size of z_owned_bytes_t in bytes.
  ///
  /// Used by Dart to allocate the correct amount of native memory
  /// for opaque zenoh types.
  int zd_bytes_sizeof() {
    return _zd_bytes_sizeof();
  }

  late final _zd_bytes_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_bytes_sizeof');
  late final _zd_bytes_sizeof = _zd_bytes_sizeofPtr
      .asFunction<int Function()>();

  /// Advances the slice iterator.
  ///
  /// @param iter  Pointer to a z_bytes_slice_iterator_t.
  /// @param out   Pointer to a z_view_slice_t to receive the next slice.
  /// @return true if a slice was written to out, false if iteration is done.
  bool zd_bytes_slice_iterator_next(
    ffi.Pointer<z_bytes_slice_iterator_t> iter,
    ffi.Pointer<z_view_slice_t> out,
  ) {
    return _zd_bytes_slice_iterator_next(
      iter,
      out,
    );
  }

  late final _zd_bytes_slice_iterator_nextPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Bool Function(
            ffi.Pointer<z_bytes_slice_iterator_t>,
            ffi.Pointer<z_view_slice_t>,
          )
        >
      >('zd_bytes_slice_iterator_next');
  late final _zd_bytes_slice_iterator_next = _zd_bytes_slice_iterator_nextPtr
      .asFunction<
        bool Function(
          ffi.Pointer<z_bytes_slice_iterator_t>,
          ffi.Pointer<z_view_slice_t>,
        )
      >();

  /// Returns the size of z_bytes_slice_iterator_t in bytes.
  int zd_bytes_slice_iterator_sizeof() {
    return _zd_bytes_slice_iterator_sizeof();
  }

  late final _zd_bytes_slice_iterator_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>(
        'zd_bytes_slice_iterator_sizeof',
      );
  late final _zd_bytes_slice_iterator_sizeof =
      _zd_bytes_slice_iterator_sizeofPtr.asFunction<int Function()>();

  /// Reads the content of owned bytes into a caller-provided buffer.
  ///
  /// Uses z_bytes_reader to copy up to `capacity` bytes into `out`.
  ///
  /// @param bytes     Pointer to a z_owned_bytes_t (cast to uint8_t*).
  /// @param out       Pointer to a buffer to receive the data.
  /// @param capacity  Maximum number of bytes to read (full-width `size_t`,
  /// matching zd_bytes_len's return).
  /// @return 0 on success.
  int zd_bytes_to_buf(
    ffi.Pointer<ffi.Uint8> bytes,
    ffi.Pointer<ffi.Uint8> out,
    int capacity,
  ) {
    return _zd_bytes_to_buf(
      bytes,
      out,
      capacity,
    );
  }

  late final _zd_bytes_to_bufPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Size,
          )
        >
      >('zd_bytes_to_buf');
  late final _zd_bytes_to_buf = _zd_bytes_to_bufPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Uint8>, int)
      >();

  /// Appends owned bytes into the writer. Consumes the bytes.
  int zd_bytes_writer_append(
    ffi.Pointer<z_loaned_bytes_writer_t> writer,
    ffi.Pointer<ffi.Opaque> bytes,
  ) {
    return _zd_bytes_writer_append(
      writer,
      bytes,
    );
  }

  late final _zd_bytes_writer_appendPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<z_loaned_bytes_writer_t>,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_bytes_writer_append');
  late final _zd_bytes_writer_append = _zd_bytes_writer_appendPtr
      .asFunction<
        int Function(
          ffi.Pointer<z_loaned_bytes_writer_t>,
          ffi.Pointer<ffi.Opaque>,
        )
      >();

  /// Drops the writer without finishing.
  void zd_bytes_writer_drop(
    ffi.Pointer<z_owned_bytes_writer_t> writer,
  ) {
    return _zd_bytes_writer_drop(
      writer,
    );
  }

  late final _zd_bytes_writer_dropPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(ffi.Pointer<z_owned_bytes_writer_t>)
        >
      >('zd_bytes_writer_drop');
  late final _zd_bytes_writer_drop = _zd_bytes_writer_dropPtr
      .asFunction<void Function(ffi.Pointer<z_owned_bytes_writer_t>)>();

  /// Creates an empty bytes writer.
  int zd_bytes_writer_empty(
    ffi.Pointer<z_owned_bytes_writer_t> writer,
  ) {
    return _zd_bytes_writer_empty(
      writer,
    );
  }

  late final _zd_bytes_writer_emptyPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<z_owned_bytes_writer_t>)
        >
      >('zd_bytes_writer_empty');
  late final _zd_bytes_writer_empty = _zd_bytes_writer_emptyPtr
      .asFunction<int Function(ffi.Pointer<z_owned_bytes_writer_t>)>();

  /// Finishes the writer and produces owned bytes.
  void zd_bytes_writer_finish(
    ffi.Pointer<z_owned_bytes_writer_t> writer,
    ffi.Pointer<ffi.Opaque> out,
  ) {
    return _zd_bytes_writer_finish(
      writer,
      out,
    );
  }

  late final _zd_bytes_writer_finishPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(
            ffi.Pointer<z_owned_bytes_writer_t>,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_bytes_writer_finish');
  late final _zd_bytes_writer_finish = _zd_bytes_writer_finishPtr
      .asFunction<
        void Function(
          ffi.Pointer<z_owned_bytes_writer_t>,
          ffi.Pointer<ffi.Opaque>,
        )
      >();

  /// Obtains a mutable loan of the writer.
  void zd_bytes_writer_loan_mut(
    ffi.Pointer<z_owned_bytes_writer_t> writer,
    ffi.Pointer<ffi.Pointer<z_loaned_bytes_writer_t>> out,
  ) {
    return _zd_bytes_writer_loan_mut(
      writer,
      out,
    );
  }

  late final _zd_bytes_writer_loan_mutPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(
            ffi.Pointer<z_owned_bytes_writer_t>,
            ffi.Pointer<ffi.Pointer<z_loaned_bytes_writer_t>>,
          )
        >
      >('zd_bytes_writer_loan_mut');
  late final _zd_bytes_writer_loan_mut = _zd_bytes_writer_loan_mutPtr
      .asFunction<
        void Function(
          ffi.Pointer<z_owned_bytes_writer_t>,
          ffi.Pointer<ffi.Pointer<z_loaned_bytes_writer_t>>,
        )
      >();

  /// Returns the size of z_owned_bytes_writer_t in bytes.
  int zd_bytes_writer_sizeof() {
    return _zd_bytes_writer_sizeof();
  }

  late final _zd_bytes_writer_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>(
        'zd_bytes_writer_sizeof',
      );
  late final _zd_bytes_writer_sizeof = _zd_bytes_writer_sizeofPtr
      .asFunction<int Function()>();

  /// Writes all bytes from src into the writer.
  int zd_bytes_writer_write_all(
    ffi.Pointer<z_loaned_bytes_writer_t> writer,
    ffi.Pointer<ffi.Uint8> data,
    int len,
  ) {
    return _zd_bytes_writer_write_all(
      writer,
      data,
      len,
    );
  }

  late final _zd_bytes_writer_write_allPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<z_loaned_bytes_writer_t>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Size,
          )
        >
      >('zd_bytes_writer_write_all');
  late final _zd_bytes_writer_write_all = _zd_bytes_writer_write_allPtr
      .asFunction<
        int Function(
          ffi.Pointer<z_loaned_bytes_writer_t>,
          ffi.Pointer<ffi.Uint8>,
          int,
        )
      >();

  /// Gracefully closes and drops the session.
  ///
  /// Calls z_close for graceful shutdown, then z_session_drop to release
  /// resources. After this call the owned session is in gravestone state.
  ///
  /// RETAINED alongside zd_open_session_async / zd_session_close_drop, which the
  /// Dart API now uses. This pair operates on a CALLER-allocated slot and frees
  /// nothing; the async pair owns its block end to end. Both remain exported: the
  /// synchronous pair is the direct, allocation-free entry a non-Dart embedder
  /// or a direct-bindings test needs, and removing it would delete the only
  /// non-offloaded path to z_open.
  ///
  /// @param session  Pointer to a z_owned_session_t to close and drop.
  void zd_close_session(
    ffi.Pointer<ffi.Opaque> session,
  ) {
    return _zd_close_session(
      session,
    );
  }

  late final _zd_close_sessionPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_close_session',
      );
  late final _zd_close_session = _zd_close_sessionPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Creates a default configuration.
  ///
  /// @param config  Pointer to an uninitialized z_owned_config_t.
  /// @return 0 on success, negative on failure.
  int zd_config_default(
    ffi.Pointer<ffi.Opaque> config,
  ) {
    return _zd_config_default(
      config,
    );
  }

  late final _zd_config_defaultPtr =
      _lookup<ffi.NativeFunction<ffi.Int Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_config_default',
      );
  late final _zd_config_default = _zd_config_defaultPtr
      .asFunction<int Function(ffi.Pointer<ffi.Opaque>)>();

  /// Drops (frees) the configuration.
  ///
  /// After this call the owned config is in gravestone state.
  /// A second drop is a safe no-op.
  ///
  /// @param config  Pointer to a z_owned_config_t to drop.
  void zd_config_drop(
    ffi.Pointer<ffi.Opaque> config,
  ) {
    return _zd_config_drop(
      config,
    );
  }

  late final _zd_config_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_config_drop',
      );
  late final _zd_config_drop = _zd_config_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Creates a configuration from the ZENOH_CONFIG environment variable.
  ///
  /// Wraps zc_config_from_env(). Detail-carrying: on failure canon's own
  /// explanation of THIS call is written into the caller's err_buf, so an
  /// enriched ZenohException can surface it. See the err_buf/err_cap/err_len
  /// contract above.
  ///
  /// @param config   Pointer to an uninitialized z_owned_config_t.
  /// @param err_buf  Caller storage for this call's detail; NULL to decline.
  /// @param err_cap  Capacity of err_buf in bytes.
  /// @param err_len  Out: bytes written to err_buf; always set, 0 when none.
  /// @return 0 on success, negative on failure.
  int zd_config_from_env(
    ffi.Pointer<ffi.Opaque> config,
    ffi.Pointer<ffi.Uint8> err_buf,
    int err_cap,
    ffi.Pointer<ffi.Int> err_len,
  ) {
    return _zd_config_from_env(
      config,
      err_buf,
      err_cap,
      err_len,
    );
  }

  late final _zd_config_from_envPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Pointer<ffi.Int>,
          )
        >
      >('zd_config_from_env');
  late final _zd_config_from_env = _zd_config_from_envPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Uint8>,
          int,
          ffi.Pointer<ffi.Int>,
        )
      >();

  /// Creates a configuration from a JSON5 file at the given path.
  ///
  /// Wraps zc_config_from_file(). Detail-carrying: on failure canon's own
  /// explanation of THIS call is written into the caller's err_buf, so an
  /// enriched ZenohException can surface it. See the err_buf/err_cap/err_len
  /// contract above.
  ///
  /// @param config   Pointer to an uninitialized z_owned_config_t.
  /// @param path     Path to a JSON5 configuration file.
  /// @param err_buf  Caller storage for this call's detail; NULL to decline.
  /// @param err_cap  Capacity of err_buf in bytes.
  /// @param err_len  Out: bytes written to err_buf; always set, 0 when none.
  /// @return 0 on success, negative on failure.
  int zd_config_from_file(
    ffi.Pointer<ffi.Opaque> config,
    ffi.Pointer<ffi.Char> path,
    ffi.Pointer<ffi.Uint8> err_buf,
    int err_cap,
    ffi.Pointer<ffi.Int> err_len,
  ) {
    return _zd_config_from_file(
      config,
      path,
      err_buf,
      err_cap,
      err_len,
    );
  }

  late final _zd_config_from_filePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Pointer<ffi.Int>,
          )
        >
      >('zd_config_from_file');
  late final _zd_config_from_file = _zd_config_from_filePtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Char>,
          ffi.Pointer<ffi.Uint8>,
          int,
          ffi.Pointer<ffi.Int>,
        )
      >();

  /// Creates a configuration from a JSON5 string.
  ///
  /// Wraps zc_config_from_str(). Detail-carrying: on failure canon's own
  /// explanation of THIS call is written into the caller's err_buf, so an
  /// enriched ZenohException can surface it. See the err_buf/err_cap/err_len
  /// contract above.
  ///
  /// @param config   Pointer to an uninitialized z_owned_config_t.
  /// @param s        JSON5 configuration string.
  /// @param err_buf  Caller storage for this call's detail; NULL to decline.
  /// @param err_cap  Capacity of err_buf in bytes.
  /// @param err_len  Out: bytes written to err_buf; always set, 0 when none.
  /// @return 0 on success, negative on failure.
  int zd_config_from_str(
    ffi.Pointer<ffi.Opaque> config,
    ffi.Pointer<ffi.Char> s,
    ffi.Pointer<ffi.Uint8> err_buf,
    int err_cap,
    ffi.Pointer<ffi.Int> err_len,
  ) {
    return _zd_config_from_str(
      config,
      s,
      err_buf,
      err_cap,
      err_len,
    );
  }

  late final _zd_config_from_strPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Pointer<ffi.Int>,
          )
        >
      >('zd_config_from_str');
  late final _zd_config_from_str = _zd_config_from_strPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Char>,
          ffi.Pointer<ffi.Uint8>,
          int,
          ffi.Pointer<ffi.Int>,
        )
      >();

  /// Reads the JSON value at the given configuration key.
  ///
  /// Loans the config internally and wraps zc_config_get_from_str(), filling the
  /// caller-allocated z_owned_string_t. Detail-carrying: on failure canon's own
  /// explanation of THIS call is written into the caller's err_buf, so an
  /// enriched ZenohException can surface it. See the err_buf/err_cap/err_len
  /// contract above.
  ///
  /// @param config   Pointer to a valid z_owned_config_t.
  /// @param key      Configuration key path.
  /// @param out      Pointer to an uninitialized z_owned_string_t to fill.
  /// @param err_buf  Caller storage for this call's detail; NULL to decline.
  /// @param err_cap  Capacity of err_buf in bytes.
  /// @param err_len  Out: bytes written to err_buf; always set, 0 when none.
  /// @return 0 on success, negative on failure.
  int zd_config_get(
    ffi.Pointer<ffi.Opaque> config,
    ffi.Pointer<ffi.Char> key,
    ffi.Pointer<ffi.Opaque> out,
    ffi.Pointer<ffi.Uint8> err_buf,
    int err_cap,
    ffi.Pointer<ffi.Int> err_len,
  ) {
    return _zd_config_get(
      config,
      key,
      out,
      err_buf,
      err_cap,
      err_len,
    );
  }

  late final _zd_config_getPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Pointer<ffi.Int>,
          )
        >
      >('zd_config_get');
  late final _zd_config_get = _zd_config_getPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Char>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Uint8>,
          int,
          ffi.Pointer<ffi.Int>,
        )
      >();

  /// Inserts a JSON5 value into the configuration at the given key path.
  ///
  /// Takes a mutable owned config pointer. Internally obtains a mutable loan
  /// via z_config_loan_mut() before calling zc_config_insert_json5().
  ///
  /// @param config   Pointer to a valid z_owned_config_t.
  /// @param key      Configuration key path (e.g., "mode").
  /// @param value    JSON5 value string (e.g., "\"peer\"").
  /// @param err_buf  Caller storage for this call's detail; NULL to decline.
  /// @param err_cap  Capacity of err_buf in bytes.
  /// @param err_len  Out: bytes written to err_buf; always set, 0 when none.
  /// @return 0 on success, negative on failure.
  int zd_config_insert_json5(
    ffi.Pointer<ffi.Opaque> config,
    ffi.Pointer<ffi.Char> key,
    ffi.Pointer<ffi.Char> value,
    ffi.Pointer<ffi.Uint8> err_buf,
    int err_cap,
    ffi.Pointer<ffi.Int> err_len,
  ) {
    return _zd_config_insert_json5(
      config,
      key,
      value,
      err_buf,
      err_cap,
      err_len,
    );
  }

  late final _zd_config_insert_json5Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Pointer<ffi.Char>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Pointer<ffi.Int>,
          )
        >
      >('zd_config_insert_json5');
  late final _zd_config_insert_json5 = _zd_config_insert_json5Ptr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Char>,
          ffi.Pointer<ffi.Char>,
          ffi.Pointer<ffi.Uint8>,
          int,
          ffi.Pointer<ffi.Int>,
        )
      >();

  /// Returns the size of z_owned_config_t in bytes.
  ///
  /// Used by Dart to allocate the correct amount of native memory
  /// for opaque zenoh types.
  int zd_config_sizeof() {
    return _zd_config_sizeof();
  }

  late final _zd_config_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_config_sizeof');
  late final _zd_config_sizeof = _zd_config_sizeofPtr
      .asFunction<int Function()>();

  /// Serializes the whole configuration to a JSON string.
  ///
  /// Loans the config internally and wraps zc_config_to_string(), filling the
  /// caller-allocated z_owned_string_t. The rc is checked (canon/cpp leaves it
  /// unchecked).
  ///
  /// ⛔ NOT detail-carrying, deliberately. It was wired to the deleted capture
  /// and nothing ever read it. Promoting it to a detail-carrying entry would
  /// widen the enrichment surface to a call whose input is the WHOLE RENDERED
  /// CONFIG -- the value-echo hazard the enrichment fence exists to bound. Its
  /// Dart site is Object.toString(), which by convention must not throw, so it
  /// has no throw to enrich in the first place.
  ///
  /// @param config  Pointer to a valid z_owned_config_t.
  /// @param out     Pointer to an uninitialized z_owned_string_t to fill.
  /// @return 0 on success, negative on failure.
  int zd_config_to_string(
    ffi.Pointer<ffi.Opaque> config,
    ffi.Pointer<ffi.Opaque> out,
  ) {
    return _zd_config_to_string(
      config,
      out,
    );
  }

  late final _zd_config_to_stringPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_config_to_string');
  late final _zd_config_to_string = _zd_config_to_stringPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
      >();

  /// Declares an advanced publisher on the given key expression.
  ///
  /// @param enable_cache        Whether to enable the publisher-side cache.
  /// @param cache_max_samples   Cache bound when the cache is enabled.
  /// **-1 means UNSPECIFIED**: canon's own default
  /// (`ze_advanced_publisher_cache_options_default`,
  /// currently 1) is left untouched. Any value >= 0 is
  /// assigned verbatim, zero included -- canon's zero
  /// carries NO unlimited semantics (measured), so
  /// there is no in-band value to misread. Ignored
  /// entirely when `enable_cache` is false.
  /// @return 0 on success; canon's own negative code on a canon failure;
  /// ZD_DECLARE_ECAPACITY (10) when `cache_max_samples` is outside the
  /// representable domain (< -1, or > SIZE_MAX on ILP32) -- rejected
  /// before anything is declared, so nothing ran.
  int zd_declare_advanced_publisher(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Opaque> publisher,
    ffi.Pointer<ffi.Opaque> keyexpr,
    bool enable_cache,
    int cache_max_samples,
    bool publisher_detection,
    bool sample_miss_detection,
    int heartbeat_mode,
    int heartbeat_period_ms,
  ) {
    return _zd_declare_advanced_publisher(
      session,
      publisher,
      keyexpr,
      enable_cache,
      cache_max_samples,
      publisher_detection,
      sample_miss_detection,
      heartbeat_mode,
      heartbeat_period_ms,
    );
  }

  late final _zd_declare_advanced_publisherPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Bool,
            ffi.Int64,
            ffi.Bool,
            ffi.Bool,
            ffi.Int,
            ffi.Uint64,
          )
        >
      >('zd_declare_advanced_publisher');
  late final _zd_declare_advanced_publisher = _zd_declare_advanced_publisherPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          bool,
          int,
          bool,
          bool,
          int,
          int,
        )
      >();

  /// Declares an advanced subscriber on the given key expression.
  int zd_declare_advanced_subscriber(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Opaque> subscriber,
    ffi.Pointer<ffi.Opaque> keyexpr,
    int dart_port,
    bool history,
    bool history_detect_late_publishers,
    bool recovery,
    bool recovery_last_sample_miss_detection,
    int recovery_periodic_queries_period_ms,
    bool subscriber_detection,
    int retain_payload,
  ) {
    return _zd_declare_advanced_subscriber(
      session,
      subscriber,
      keyexpr,
      dart_port,
      history,
      history_detect_late_publishers,
      recovery,
      recovery_last_sample_miss_detection,
      recovery_periodic_queries_period_ms,
      subscriber_detection,
      retain_payload,
    );
  }

  late final _zd_declare_advanced_subscriberPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Bool,
            ffi.Bool,
            ffi.Bool,
            ffi.Bool,
            ffi.Uint64,
            ffi.Bool,
            ffi.Int,
          )
        >
      >('zd_declare_advanced_subscriber');
  late final _zd_declare_advanced_subscriber =
      _zd_declare_advanced_subscriberPtr
          .asFunction<
            int Function(
              ffi.Pointer<ffi.Opaque>,
              ffi.Pointer<ffi.Opaque>,
              ffi.Pointer<ffi.Opaque>,
              int,
              bool,
              bool,
              bool,
              bool,
              int,
              bool,
              int,
            )
          >();

  /// Declares a fire-and-forget background queryable.
  ///
  /// No handle is returned; the queryable lives until the session is closed,
  /// at which point its drop posts a null sentinel to the Dart port to signal
  /// stream completion. Queries are posted to the Dart isolate via the port.
  ///
  /// @param session   Const pointer to a loaned session (as uint8_t*).
  /// @param key_expr  Const pointer to the loaned key expression, already
  /// validated Dart-side.
  /// @param port      The Dart native port to post queries to.
  /// @param complete  Whether the queryable is a complete data source (0/1).
  /// @return 0 on success, negative on failure.
  /// @param allowed_origin  z_locality_t 0..2 restricting which peers' traffic
  /// this declaration accepts, or -1 to leave canon's
  /// default (ANY).
  int zd_declare_background_queryable(
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int port,
    int complete,
    int allowed_origin,
  ) {
    return _zd_declare_background_queryable(
      session,
      key_expr,
      port,
      complete,
      allowed_origin,
    );
  }

  late final _zd_declare_background_queryablePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Int8,
            ffi.Int,
          )
        >
      >('zd_declare_background_queryable');
  late final _zd_declare_background_queryable =
      _zd_declare_background_queryablePtr
          .asFunction<
            int Function(
              ffi.Pointer<ffi.Uint8>,
              ffi.Pointer<ffi.Opaque>,
              int,
              int,
              int,
            )
          >();

  /// Declares a background subscriber on the given key expression.
  ///
  /// Unlike a regular subscriber, a background subscriber has no handle --
  /// it lives until the session is closed. Samples are posted to the Dart
  /// native port. When the session closes and the background subscriber is
  /// dropped internally by zenoh-c, a null sentinel is posted to signal
  /// stream completion.
  ///
  /// @param session   Const pointer to a loaned session.
  /// @param key_expr  Const pointer to the loaned key expression. Already
  /// validated -- the Dart side constructs and validates it,
  /// so a declared key expression drives the declaration
  /// without a string round-trip.
  /// @param dart_port The Dart native port to post samples to.
  /// @return 0 on success, negative on failure.
  /// @param allowed_origin  z_locality_t 0..2 restricting which peers' traffic
  /// this declaration accepts, or -1 to leave canon's
  /// default (ANY).
  int zd_declare_background_subscriber(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int dart_port,
    int allowed_origin,
    int retain_payload,
  ) {
    return _zd_declare_background_subscriber(
      session,
      key_expr,
      dart_port,
      allowed_origin,
      retain_payload,
    );
  }

  late final _zd_declare_background_subscriberPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Int,
            ffi.Int,
          )
        >
      >('zd_declare_background_subscriber');
  late final _zd_declare_background_subscriber =
      _zd_declare_background_subscriberPtr
          .asFunction<
            int Function(
              ffi.Pointer<ffi.Opaque>,
              ffi.Pointer<ffi.Opaque>,
              int,
              int,
              int,
            )
          >();

  /// Declares a key expression on the session, returning an owned handle.
  ///
  /// Canon reduces the key expression to a numerical id in the session's
  /// routing tables, which saves bandwidth when the expression is passed
  /// between Zenoh entities. The input is LOANED, not consumed. The out-param
  /// is written on EVERY path -- a gravestone on failure -- so a caller must
  /// check the return code before wrapping it.
  ///
  /// @param session       Const pointer to a loaned (live) session.
  /// @param declared_out  Pointer to an uninitialized z_owned_keyexpr_t.
  /// @param key_expr      Const pointer to the loaned key expression to declare.
  /// @return 0 on success, Z_EGENERIC (-128) on failure.
  int zd_declare_keyexpr(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<z_owned_keyexpr_t> declared_out,
    ffi.Pointer<ffi.Opaque> key_expr,
  ) {
    return _zd_declare_keyexpr(
      session,
      declared_out,
      key_expr,
    );
  }

  late final _zd_declare_keyexprPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<z_owned_keyexpr_t>,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_declare_keyexpr');
  late final _zd_declare_keyexpr = _zd_declare_keyexprPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<z_owned_keyexpr_t>,
          ffi.Pointer<ffi.Opaque>,
        )
      >();

  /// Declares a publisher on the given key expression.
  ///
  /// @param session             Const pointer to a loaned session.
  /// @param publisher           Pointer to an uninitialized z_owned_publisher_t.
  /// @param keyexpr             Const pointer to a loaned key expression.
  /// @param encoding            MIME type string for default encoding (NULL = default).
  /// @param congestion_control  Congestion control strategy (-1 = default/DROP).
  /// Publisher is a PUSH path, so canon's default is
  /// CongestionControl::DEFAULT_PUSH = DROP -- not
  /// BLOCK, which is the request-path default.
  /// @param priority            Message priority (-1 = default/data=5).
  /// @param is_express          Express mode (-1 = default, 0 = false, 1 = true).
  /// @param allowed_destination z_locality_t 0..2, or -1 to leave canon's
  /// default (ANY).
  /// @return 0 on success, negative on failure.
  int zd_declare_publisher(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Opaque> publisher,
    ffi.Pointer<ffi.Opaque> keyexpr,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
    int congestion_control,
    int priority,
    int is_express,
    int allowed_destination,
  ) {
    return _zd_declare_publisher(
      session,
      publisher,
      keyexpr,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
      congestion_control,
      priority,
      is_express,
      allowed_destination,
    );
  }

  late final _zd_declare_publisherPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Int,
            ffi.Int,
            ffi.Int8,
            ffi.Int,
          )
        >
      >('zd_declare_publisher');
  late final _zd_declare_publisher = _zd_declare_publisherPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          int,
          int,
          int,
          int,
        )
      >();

  /// Tries to receive a sample from the handler, without blocking.
  ///
  /// RETURN CODE CONTRACT -- three of these four are CANON'S OWN VALUES, passed
  /// through unaltered:
  ///
  /// 0   a sample is available; every out_ parameter is populated
  /// (malloc'd -- the caller frees).
  /// 1   `Z_CHANNEL_DISCONNECTED`: the channel was dropped. Terminal and
  /// sticky; every later call reports it again.
  /// 2   `Z_CHANNEL_NODATA`: the channel is alive, its buffer is empty.
  /// -1   SHIM-ORIGINATED: one of the four remote-length-driven allocations
  /// below failed. Everything already allocated has been released and
  /// every out_ parameter it had set is NULL again.
  ///
  /// ⚠️ The -1 here is sound even though a shim-originated negative is BARRED on
  /// the declare channel above, and the asymmetry is not an accident: canon's
  /// recv family provably emits no negatives (zero `Z_E*` references anywhere in
  /// zenoh-c's `src/closures/`, and every implementation is a total 2- or 3-arm
  /// match), so the negative space is genuinely free HERE and genuinely occupied
  /// THERE. The two entries differ because their canon contracts differ.
  ///
  /// 1 and 2 are STATES, not failures -- the only positive result codes in the
  /// whole zenoh-c API, deliberately outside its negative error space. The Dart
  /// surface renders them as sealed variants and reserves throwing for the -1.
  ///
  /// ONE SHARED EXTRACTION BODY serves both kinds: only the loan and the
  /// try_recv call are kind-dispatched. Anything that alters how a value is
  /// rendered therefore lands once and covers both kinds by construction.
  ///
  /// The key expression AND the encoding are both LENGTH-CARRIED, not
  /// NUL-terminated: read exactly `*out_keyexpr_len` / `*out_encoding_len` bytes.
  /// Both domains admit an interior NUL and canon carries one byte-exact, so
  /// measuring either buffer with strlen truncates a real value. A trailing NUL
  /// is written as hygiene only.
  ///
  /// ⚠️ The encoding used to stay a bare C string here, on the reason that a probe
  /// had refuted the remote non-UTF-8 trigger for that field at this pin. That
  /// reason is TRUE and it is about the wrong property: F-R1 refuted a
  /// strict-decode CRASH trigger, and says nothing about LENGTH CARRIAGE. Measured
  /// at seed #10: a canon publisher using `z_encoding_from_substr` puts an interior
  /// NUL on the wire, and this surface truncated there.
  ///
  /// @param handler           Const pointer to an owned handler of `kind` (as uint8_t*).
  /// @param kind              0 = ring, 1 = fifo. MUST match the kind the
  /// handler was declared with.
  /// @param out_keyexpr       Out: malloc'd key expression bytes (length-carried).
  /// @param out_keyexpr_len   Out: key expression length in bytes (`size_t`).
  /// @param out_payload       Out: malloc'd payload bytes.
  /// @param out_payload_len   Out: payload length (full-width `size_t`).
  /// @param out_kind          Out: sample kind (0=put, 1=delete).
  /// @param out_encoding      Out: malloc'd encoding bytes (length-carried).
  /// ALWAYS non-NULL when the return is 0 -- a
  /// zero-length rendered encoding yields a 1-byte
  /// allocation at length 0 rather than NULL,
  /// identically to the subscriber callback path.
  /// Absent-vs-empty is not a distinction canon draws
  /// for this field.
  /// @param out_encoding_len  Out: encoding length in bytes (`size_t`).
  /// @param out_attachment     Out: malloc'd attachment bytes (or NULL if absent).
  /// @param out_attachment_len Out: attachment length (full-width `size_t`).
  /// @param out_timestamp   Out: malloc'd 24-byte z_timestamp_t image (or NULL if
  /// the sample carries no timestamp; caller frees).
  /// @param out_priority    Out: sample priority (1..7).
  /// @param out_congestion  Out: congestion control (0=block, 1=drop, 2=blockFirst).
  /// @param out_express     Out: express flag (0/1).
  /// @return 0=sample, 1=disconnected, 2=empty, -1=allocation failure.
  /// Declares a LIVELINESS subscriber backed by a bounded channel of `kind`.
  ///
  /// The fifth channel carrier, and the thinnest: canon's liveliness declare
  /// consumes the SAMPLE closure, so this reuses the sample column's shared
  /// channel build, handlers, extraction body and `PullSubscriber` handle
  /// wholesale. Only the canon entry and its options struct differ, and that
  /// struct carries exactly one field.
  ///
  /// Delivers the alive/gone TRANSITIONS as samples — `SampleKind::Put` when a
  /// token appears, `SampleKind::Delete` when it goes — which is a different
  /// capability from `zd_liveliness_get`'s snapshot of who is alive now.
  ///
  /// @param history  Non-zero to replay tokens that were already alive when this
  /// subscriber was declared. canon's only option here.
  /// @return 0 / 10 (capacity) / 11 (tee allocation) / negative from canon.
  int zd_declare_pull_liveliness_subscriber(
    ffi.Pointer<ffi.Uint8> subscriber_out,
    ffi.Pointer<ffi.Uint8> handler_out,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> tee_out,
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int kind,
    int capacity,
    int history,
    int dart_port,
  ) {
    return _zd_declare_pull_liveliness_subscriber(
      subscriber_out,
      handler_out,
      tee_out,
      session,
      key_expr,
      kind,
      capacity,
      history,
      dart_port,
    );
  }

  late final _zd_declare_pull_liveliness_subscriberPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int32,
            ffi.Int64,
            ffi.Int8,
            ffi.Int64,
          )
        >
      >('zd_declare_pull_liveliness_subscriber');
  late final _zd_declare_pull_liveliness_subscriber =
      _zd_declare_pull_liveliness_subscriberPtr
          .asFunction<
            int Function(
              ffi.Pointer<ffi.Uint8>,
              ffi.Pointer<ffi.Uint8>,
              ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
              ffi.Pointer<ffi.Uint8>,
              ffi.Pointer<ffi.Opaque>,
              int,
              int,
              int,
              int,
            )
          >();

  /// Declares a pull subscriber backed by a bounded channel of `kind`.
  ///
  /// RETURN CODE CONTRACT -- the channel is SPLIT, and the split is deliberate:
  ///
  /// 0            success.
  /// POSITIVE     SHIM-ORIGINATED. Canon never returns a positive here.
  /// 10 = capacity out of range (negative, or larger than this
  /// target's `size_t`). Nothing was allocated.
  /// 11 = an allocation the shim itself needed failed. LIVE:
  /// this entry claims the readiness-tee context, and this
  /// is the code its malloc failure returns. (The comment
  /// that used to sit here still called it RESERVED and
  /// "reachable when the readiness-tee context lands" --
  /// the tee landed in the same change that wrote the code,
  /// so the line described a state that never shipped.)
  /// NEGATIVE     CANON'S OWN, passed through unchanged (e.g. `Z_EINVAL -1`,
  /// `Z_EPARSE -2` from `z_declare_subscriber`).
  ///
  /// Why the shim's codes are positive rather than the usual -1: canon OWNS the
  /// negative space on this channel and actively uses it, so a shim-owned -1
  /// would squat on a live canon code and let a canon EINVAL masquerade as our
  /// allocation failure. That is an rc-conflation -- the same defect class as
  /// collapsing distinct channel states onto one value -- and it is barred.
  /// Low positives 1 and 2 stay reserved repo-wide for canon's channel-state
  /// meanings, which is why shim-originated call failures start at 10.
  ///
  /// CAPACITY is validated before anything is allocated and is never silently
  /// transformed. Canon documents nothing about capacity and its constructor
  /// returns void -- it cannot fail and cannot reject a value -- so every domain
  /// check that exists at all exists here. Capacity 0 is IN canon's domain and
  /// is passed through (measured working on both kinds at 1.8.0, not promised by
  /// canon).
  ///
  /// @param subscriber_out  Pointer to an uninitialized z_owned_subscriber_t (as uint8_t*).
  /// @param handler_out     Pointer to an uninitialized owned handler slot of
  /// `kind`, sized by `zd_pull_handler_sizeof(kind)`.
  /// @param tee_out         Out: the readiness-tee context this call allocates,
  /// or NULL on any failure. SHIM-OWNED: release it with
  /// `zd_pull_tee_drop`, and only AFTER dropping the
  /// subscriber.
  /// @param session         Const pointer to a loaned session (as uint8_t*).
  /// @param key_expr        Const pointer to the loaned key expression, already
  /// validated Dart-side.
  /// @param kind            0 = ring, 1 = fifo.
  /// @param capacity        Channel capacity. Carried full-width so no value a
  /// caller can express is truncated on the way in.
  /// @param allowed_origin  z_locality_t 0..2 restricting which peers' traffic
  /// this declaration accepts, or -1 to leave canon's
  /// default (ANY).
  /// @param dart_port       NativePort the readiness tee posts to: an int64 `1`
  /// when an armed waiter should look again, and a null
  /// sentinel from the closure's drop when the producer
  /// is gone.
  /// @return 0, a shim-originated positive, or canon's own negative (above).
  int zd_declare_pull_subscriber(
    ffi.Pointer<ffi.Uint8> subscriber_out,
    ffi.Pointer<ffi.Uint8> handler_out,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> tee_out,
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int kind,
    int capacity,
    int allowed_origin,
    int dart_port,
  ) {
    return _zd_declare_pull_subscriber(
      subscriber_out,
      handler_out,
      tee_out,
      session,
      key_expr,
      kind,
      capacity,
      allowed_origin,
      dart_port,
    );
  }

  late final _zd_declare_pull_subscriberPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int32,
            ffi.Int64,
            ffi.Int,
            ffi.Int64,
          )
        >
      >('zd_declare_pull_subscriber');
  late final _zd_declare_pull_subscriber = _zd_declare_pull_subscriberPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
          int,
        )
      >();

  /// Declares a querier on the given key expression.
  ///
  /// @param querier_out    Pointer to uninitialized z_owned_querier_t (as uint8_t*).
  /// @param session        Pointer to a loaned session (as uint8_t*).
  /// @param key_expr       Null-terminated key expression string.
  /// @param target         Query target (z_query_target_t value).
  /// @param consolidation  Consolidation mode (-1=auto, 0=none, 1=monotonic, 2=latest).
  /// @param timeout_ms     Timeout in milliseconds (0 = default).
  /// @return 0 on success, negative on failure.
  /// @param congestion_control  Congestion control strategy, or -1 to leave
  /// canon's own default. NOTE: querier is a REQUEST path,
  /// so canon's default is DEFAULT_REQUEST = BLOCK.
  /// @param priority       Priority 1..7, or -1 to leave canon's default (data=5).
  /// @param is_express     1/0 to set express mode, or -1 to leave canon's
  /// default (false).
  /// @param allowed_destination  z_locality_t 0..2, or -1 to leave canon's
  /// default (ANY).
  /// @param accept_replies z_reply_keyexpr_t 0..1, or -1 to leave canon's
  /// default (MATCHING_QUERY = 1).
  ///
  /// All five are DECLARATION-time: canon's z_querier_get_options_t carries no
  /// QoS, locality or accept-replies field, so there is no per-call form.
  int zd_declare_querier(
    ffi.Pointer<ffi.Uint8> querier_out,
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int target,
    int consolidation,
    int timeout_ms,
    int congestion_control,
    int priority,
    int is_express,
    int allowed_destination,
    int accept_replies,
  ) {
    return _zd_declare_querier(
      querier_out,
      session,
      key_expr,
      target,
      consolidation,
      timeout_ms,
      congestion_control,
      priority,
      is_express,
      allowed_destination,
      accept_replies,
    );
  }

  late final _zd_declare_querierPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int8,
            ffi.Int8,
            ffi.Uint64,
            ffi.Int,
            ffi.Int,
            ffi.Int8,
            ffi.Int,
            ffi.Int,
          )
        >
      >('zd_declare_querier');
  late final _zd_declare_querier = _zd_declare_querierPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
          int,
          int,
          int,
          int,
          int,
        )
      >();

  /// Declares a queryable on the given key expression.
  ///
  /// Incoming queries are posted to the Dart isolate via the given native port.
  /// The query's key expression rides the message as a length-carried
  /// `Uint8List`, so an interior NUL survives; the selector `parameters` beside
  /// it are still a C string.
  ///
  /// @param queryable_out  Pointer to an uninitialized z_owned_queryable_t.
  /// @param session        Const pointer to a loaned session (as uint8_t*).
  /// @param key_expr       Const pointer to the loaned key expression, already
  /// validated Dart-side.
  /// @param port           The Dart native port to post queries to.
  /// @param complete       Whether this queryable is complete (1) or not (0).
  /// @return 0 on success, negative on failure.
  /// @param allowed_origin  z_locality_t 0..2 restricting which peers' traffic
  /// this declaration accepts, or -1 to leave canon's
  /// default (ANY).
  int zd_declare_queryable(
    ffi.Pointer<ffi.Uint8> queryable_out,
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int port,
    int complete,
    int allowed_origin,
  ) {
    return _zd_declare_queryable(
      queryable_out,
      session,
      key_expr,
      port,
      complete,
      allowed_origin,
    );
  }

  late final _zd_declare_queryablePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Int8,
            ffi.Int,
          )
        >
      >('zd_declare_queryable');
  late final _zd_declare_queryable = _zd_declare_queryablePtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
        )
      >();

  /// Declares a queryable whose queries land in a BOUNDED CHANNEL.
  ///
  /// The channel-mode sibling of zd_declare_queryable. Both fill canon's
  /// `z_queryable_options_t` through one shared body, so their option surfaces
  /// cannot drift; they differ only in which closure canon receives.
  ///
  /// Unlike a reply channel, this one lives until the queryable is undeclared or
  /// its session closes — canon's own words for the recv contract — so releasing
  /// the handle is REMOTE-VISIBLE.
  ///
  /// @param queryable_out  Caller-allocated `zd_queryable_sizeof()` buffer.
  /// @param handler_out    Caller-allocated `zd_query_handler_sizeof(kind)`
  /// buffer, written with the owned handler on success.
  /// @param tee_out        Out-param for the SHIM-owned readiness-tee context,
  /// released through the shared `zd_pull_tee_drop`. NULL
  /// on every failure path.
  /// @param dart_port      Dart NativePort the tee posts readiness pings to.
  /// @param kind           0 = ring (lossy, drop-oldest), 1 = fifo (lossless,
  /// producer-backpressured).
  /// @param capacity       Channel capacity, carried full width.
  /// @return 0 on success.
  /// **10** (`ZD_DECLARE_ECAPACITY`) — capacity outside canon's domain;
  /// nothing was allocated.
  /// **11** (`ZD_DECLARE_EALLOC`) — the readiness-tee context could not be
  /// claimed.
  /// negative — canon's own code, passed through unchanged.
  int zd_declare_queryable_channel(
    ffi.Pointer<ffi.Uint8> queryable_out,
    ffi.Pointer<ffi.Uint8> handler_out,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> tee_out,
    int dart_port,
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int kind,
    int capacity,
    int complete,
    int allowed_origin,
  ) {
    return _zd_declare_queryable_channel(
      queryable_out,
      handler_out,
      tee_out,
      dart_port,
      session,
      key_expr,
      kind,
      capacity,
      complete,
      allowed_origin,
    );
  }

  late final _zd_declare_queryable_channelPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Int64,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int32,
            ffi.Int64,
            ffi.Int8,
            ffi.Int,
          )
        >
      >('zd_declare_queryable_channel');
  late final _zd_declare_queryable_channel = _zd_declare_queryable_channelPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          int,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
          int,
        )
      >();

  /// Declares a subscriber on the given key expression.
  ///
  /// Samples are posted to the Dart isolate via `Dart_PostCObject_DL` on
  /// the given native port. Each sample is sent as a `Dart_CObject` array
  /// of 9 elements: [keyexpr(Uint8List), payload(Uint8List), kind(int64),
  /// attachment(null or Uint8List), encoding(string), timestamp(null or
  /// Uint8List), priority(int64), congestion(int64), express(int64)].
  ///
  /// The key expression is a `Uint8List`, not a string: it is length-carried
  /// so an interior NUL survives the posting (the grammar permits one and canon
  /// carries it byte-exact). Dart decodes it leniently for display.
  ///
  /// @param session     Const pointer to a loaned session.
  /// @param subscriber  Pointer to an uninitialized z_owned_subscriber_t.
  /// @param keyexpr     Const pointer to a loaned key expression.
  /// @param dart_port   The Dart native port to post samples to.
  /// @return 0 on success, negative on failure.
  /// @param allowed_origin  z_locality_t 0..2 restricting which peers' traffic
  /// this declaration accepts, or -1 to leave canon's
  /// default (ANY).
  /// @param retain_payload  Non-zero makes every delivered sample carry an
  /// OWNED clone of its payload as message element 9,
  /// transferring that clone to Dart. Zero posts kNull
  /// there and allocates nothing.
  /// ⛔ OWNERSHIP: on the delivered path the clone belongs
  /// to Dart, which must release it; on a FAILED post the
  /// callback reclaims it, because the discarded message
  /// held the only reference.
  int zd_declare_subscriber(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Opaque> subscriber,
    ffi.Pointer<ffi.Opaque> keyexpr,
    int dart_port,
    int allowed_origin,
    int retain_payload,
  ) {
    return _zd_declare_subscriber(
      session,
      subscriber,
      keyexpr,
      dart_port,
      allowed_origin,
      retain_payload,
    );
  }

  late final _zd_declare_subscriberPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Int,
            ffi.Int,
          )
        >
      >('zd_declare_subscriber');
  late final _zd_declare_subscriber = _zd_declare_subscriberPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
        )
      >();

  /// Deletes a resource on the given key expression.
  ///
  /// @param session    Const pointer to a loaned session.
  /// @param keyexpr    Const pointer to a loaned key expression.
  /// @param timestamp  Optional pointer to 24 raw z_timestamp_t bytes, or NULL
  /// for none. Borrowed (copied into aligned storage), not
  /// consumed.
  /// @param congestion_control  Congestion control strategy, or -1 to leave
  /// canon's own default (DROP -- this is a push path).
  /// @param priority   Priority 1..7, or -1 to leave canon's default (data=5).
  /// @param is_express 1/0 to set express mode, or -1 to leave canon's
  /// default (false).
  /// @param allowed_destination  z_locality_t 0..2, or -1 to leave canon's
  /// default (ANY).
  /// @return 0 on success, negative on failure.
  int zd_delete(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Opaque> keyexpr,
    ffi.Pointer<ffi.Uint8> timestamp,
    int congestion_control,
    int priority,
    int is_express,
    int allowed_destination,
  ) {
    return _zd_delete(
      session,
      keyexpr,
      timestamp,
      congestion_control,
      priority,
      is_express,
      allowed_destination,
    );
  }

  late final _zd_deletePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Int,
            ffi.Int8,
            ffi.Int,
          )
        >
      >('zd_delete');
  late final _zd_delete = _zd_deletePtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Uint8>,
          int,
          int,
          int,
          int,
        )
      >();

  int zd_deserializer_deserialize_bool(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Bool> out,
  ) {
    return _zd_deserializer_deserialize_bool(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_boolPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Bool>,
          )
        >
      >('zd_deserializer_deserialize_bool');
  late final _zd_deserializer_deserialize_bool =
      _zd_deserializer_deserialize_boolPtr
          .asFunction<
            int Function(ffi.Pointer<ze_deserializer_t>, ffi.Pointer<ffi.Bool>)
          >();

  /// Deserializes a byte buffer (slice). Outputs owned bytes.
  int zd_deserializer_deserialize_buf(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Opaque> out,
  ) {
    return _zd_deserializer_deserialize_buf(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_bufPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_deserializer_deserialize_buf');
  late final _zd_deserializer_deserialize_buf =
      _zd_deserializer_deserialize_bufPtr
          .asFunction<
            int Function(
              ffi.Pointer<ze_deserializer_t>,
              ffi.Pointer<ffi.Opaque>,
            )
          >();

  int zd_deserializer_deserialize_double(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Double> out,
  ) {
    return _zd_deserializer_deserialize_double(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_doublePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Double>,
          )
        >
      >('zd_deserializer_deserialize_double');
  late final _zd_deserializer_deserialize_double =
      _zd_deserializer_deserialize_doublePtr
          .asFunction<
            int Function(
              ffi.Pointer<ze_deserializer_t>,
              ffi.Pointer<ffi.Double>,
            )
          >();

  int zd_deserializer_deserialize_float(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Float> out,
  ) {
    return _zd_deserializer_deserialize_float(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_floatPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Float>,
          )
        >
      >('zd_deserializer_deserialize_float');
  late final _zd_deserializer_deserialize_float =
      _zd_deserializer_deserialize_floatPtr
          .asFunction<
            int Function(ffi.Pointer<ze_deserializer_t>, ffi.Pointer<ffi.Float>)
          >();

  int zd_deserializer_deserialize_int16(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Int16> out,
  ) {
    return _zd_deserializer_deserialize_int16(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_int16Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Int16>,
          )
        >
      >('zd_deserializer_deserialize_int16');
  late final _zd_deserializer_deserialize_int16 =
      _zd_deserializer_deserialize_int16Ptr
          .asFunction<
            int Function(ffi.Pointer<ze_deserializer_t>, ffi.Pointer<ffi.Int16>)
          >();

  int zd_deserializer_deserialize_int32(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Int32> out,
  ) {
    return _zd_deserializer_deserialize_int32(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_int32Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Int32>,
          )
        >
      >('zd_deserializer_deserialize_int32');
  late final _zd_deserializer_deserialize_int32 =
      _zd_deserializer_deserialize_int32Ptr
          .asFunction<
            int Function(ffi.Pointer<ze_deserializer_t>, ffi.Pointer<ffi.Int32>)
          >();

  int zd_deserializer_deserialize_int64(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Int64> out,
  ) {
    return _zd_deserializer_deserialize_int64(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_int64Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Int64>,
          )
        >
      >('zd_deserializer_deserialize_int64');
  late final _zd_deserializer_deserialize_int64 =
      _zd_deserializer_deserialize_int64Ptr
          .asFunction<
            int Function(ffi.Pointer<ze_deserializer_t>, ffi.Pointer<ffi.Int64>)
          >();

  int zd_deserializer_deserialize_int8(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Int8> out,
  ) {
    return _zd_deserializer_deserialize_int8(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_int8Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Int8>,
          )
        >
      >('zd_deserializer_deserialize_int8');
  late final _zd_deserializer_deserialize_int8 =
      _zd_deserializer_deserialize_int8Ptr
          .asFunction<
            int Function(ffi.Pointer<ze_deserializer_t>, ffi.Pointer<ffi.Int8>)
          >();

  /// Deserializes a sequence length header.
  int zd_deserializer_deserialize_sequence_length(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Size> out,
  ) {
    return _zd_deserializer_deserialize_sequence_length(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_sequence_lengthPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Size>,
          )
        >
      >('zd_deserializer_deserialize_sequence_length');
  late final _zd_deserializer_deserialize_sequence_length =
      _zd_deserializer_deserialize_sequence_lengthPtr
          .asFunction<
            int Function(ffi.Pointer<ze_deserializer_t>, ffi.Pointer<ffi.Size>)
          >();

  /// Deserializes a string. Caller must drop the owned string.
  int zd_deserializer_deserialize_string(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Opaque> out,
  ) {
    return _zd_deserializer_deserialize_string(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_stringPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_deserializer_deserialize_string');
  late final _zd_deserializer_deserialize_string =
      _zd_deserializer_deserialize_stringPtr
          .asFunction<
            int Function(
              ffi.Pointer<ze_deserializer_t>,
              ffi.Pointer<ffi.Opaque>,
            )
          >();

  int zd_deserializer_deserialize_uint16(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Uint16> out,
  ) {
    return _zd_deserializer_deserialize_uint16(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_uint16Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Uint16>,
          )
        >
      >('zd_deserializer_deserialize_uint16');
  late final _zd_deserializer_deserialize_uint16 =
      _zd_deserializer_deserialize_uint16Ptr
          .asFunction<
            int Function(
              ffi.Pointer<ze_deserializer_t>,
              ffi.Pointer<ffi.Uint16>,
            )
          >();

  int zd_deserializer_deserialize_uint32(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Uint32> out,
  ) {
    return _zd_deserializer_deserialize_uint32(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_uint32Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Uint32>,
          )
        >
      >('zd_deserializer_deserialize_uint32');
  late final _zd_deserializer_deserialize_uint32 =
      _zd_deserializer_deserialize_uint32Ptr
          .asFunction<
            int Function(
              ffi.Pointer<ze_deserializer_t>,
              ffi.Pointer<ffi.Uint32>,
            )
          >();

  int zd_deserializer_deserialize_uint64(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Uint64> out,
  ) {
    return _zd_deserializer_deserialize_uint64(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_uint64Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Uint64>,
          )
        >
      >('zd_deserializer_deserialize_uint64');
  late final _zd_deserializer_deserialize_uint64 =
      _zd_deserializer_deserialize_uint64Ptr
          .asFunction<
            int Function(
              ffi.Pointer<ze_deserializer_t>,
              ffi.Pointer<ffi.Uint64>,
            )
          >();

  int zd_deserializer_deserialize_uint8(
    ffi.Pointer<ze_deserializer_t> deser,
    ffi.Pointer<ffi.Uint8> out,
  ) {
    return _zd_deserializer_deserialize_uint8(
      deser,
      out,
    );
  }

  late final _zd_deserializer_deserialize_uint8Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_deserializer_t>,
            ffi.Pointer<ffi.Uint8>,
          )
        >
      >('zd_deserializer_deserialize_uint8');
  late final _zd_deserializer_deserialize_uint8 =
      _zd_deserializer_deserialize_uint8Ptr
          .asFunction<
            int Function(ffi.Pointer<ze_deserializer_t>, ffi.Pointer<ffi.Uint8>)
          >();

  /// Creates a deserializer from loaned bytes.
  ///
  /// @param bytes  Loaned bytes to deserialize from.
  /// @param out    Pointer to an uninitialized ze_deserializer_t.
  void zd_deserializer_from_bytes(
    ffi.Pointer<ffi.Opaque> bytes,
    ffi.Pointer<ze_deserializer_t> out,
  ) {
    return _zd_deserializer_from_bytes(
      bytes,
      out,
    );
  }

  late final _zd_deserializer_from_bytesPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ze_deserializer_t>,
          )
        >
      >('zd_deserializer_from_bytes');
  late final _zd_deserializer_from_bytes = _zd_deserializer_from_bytesPtr
      .asFunction<
        void Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ze_deserializer_t>)
      >();

  /// Checks if the deserializer has consumed all data.
  ///
  /// @return true if no more data to parse, false otherwise.
  bool zd_deserializer_is_done(
    ffi.Pointer<ze_deserializer_t> deser,
  ) {
    return _zd_deserializer_is_done(
      deser,
    );
  }

  late final _zd_deserializer_is_donePtr =
      _lookup<
        ffi.NativeFunction<ffi.Bool Function(ffi.Pointer<ze_deserializer_t>)>
      >('zd_deserializer_is_done');
  late final _zd_deserializer_is_done = _zd_deserializer_is_donePtr
      .asFunction<bool Function(ffi.Pointer<ze_deserializer_t>)>();

  /// Returns the size of ze_deserializer_t in bytes.
  int zd_deserializer_sizeof() {
    return _zd_deserializer_sizeof();
  }

  late final _zd_deserializer_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>(
        'zd_deserializer_sizeof',
      );
  late final _zd_deserializer_sizeof = _zd_deserializer_sizeofPtr
      .asFunction<int Function()>();

  /// Returns a bitmask of the optional zenoh features this native was built with.
  ///
  /// ZD_FEATURE_UNSTABLE_API is set when Z_FEATURE_UNSTABLE_API was defined at
  /// compile time; ZD_FEATURE_SHARED_MEMORY when Z_FEATURE_SHARED_MEMORY was.
  /// Which bits are set depends on the build variant the native was compiled
  /// as, so two natives for the same platform can answer differently. One fact
  /// holds on every variant: an Android native never carries shared memory. The
  /// Dart layer uses this to fail loudly when an unstable entrypoint is called
  /// against a native that lacks the feature (a build-variant mismatch), instead
  /// of crashing in zenoh-c. Cheap enough to call once and cache.
  int zd_features() {
    return _zd_features();
  }

  late final _zd_featuresPtr =
      _lookup<ffi.NativeFunction<ffi.Uint32 Function()>>('zd_features');
  late final _zd_features = _zd_featuresPtr.asFunction<int Function()>();

  /// `AdvancedPublisher`'s net: `zd_advanced_publisher_drop` then `free(token)`.
  ///
  /// ⚠️ Matching-listener-OFF only, exactly as `zd_fin_publisher` above.
  /// ⛔ **Guarded by `Z_FEATURE_UNSTABLE_API`, so it is ABSENT from the `stable`
  /// native this package also ships.** The Dart side resolves each `zd_fin_*`
  /// entry lazily, at first attach, and this one is only ever reached from a
  /// path already behind the same feature predicate -- eager resolution of the
  /// family would throw at initialization on a variant that legitimately lacks
  /// it.
  void zd_fin_advanced_publisher(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_advanced_publisher(
      token,
    );
  }

  late final _zd_fin_advanced_publisherPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_advanced_publisher',
      );
  late final _zd_fin_advanced_publisher = _zd_fin_advanced_publisherPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// `ZBytes`'s net: `zd_bytes_drop` then `free(token)`.
  ///
  /// ⚠️ The per-message class. `ZBytes` is constructed once per message on the
  /// clone-in-loop throughput and ping paths, so this is the entry whose cost is
  /// priced rather than assumed (seed criterion A9b). Its `markConsumed()` frees
  /// the Dart block on the send path exactly as `Config`'s does, with the same
  /// double-free consequence for a missed detach. See the family contract above.
  void zd_fin_bytes(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_bytes(
      token,
    );
  }

  late final _zd_fin_bytesPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_bytes',
      );
  late final _zd_fin_bytes = _zd_fin_bytesPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// `ZBytesWriter`'s net: `zd_bytes_writer_drop` then `free(token)`.
  ///
  /// ⚠️ `finish()` is a RELEASE path, not just a state change: it moves the
  /// handle into canon and frees the slot. It detaches for the same reason
  /// `markConsumed()` does. See the family contract above.
  void zd_fin_bytes_writer(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_bytes_writer(
      token,
    );
  }

  late final _zd_fin_bytes_writerPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_bytes_writer',
      );
  late final _zd_fin_bytes_writer = _zd_fin_bytes_writerPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// `Config`'s net: `zd_config_drop` then `free(token)`.
  ///
  /// ⚠️ The hot detach of this family. `Config.markConsumed()` FREES the Dart
  /// block rather than moving ownership, so a missed detach on that path is a
  /// double free of a block on the session-open path — and its ordering is a
  /// pinned regression guard (PR #47, "a use-after-free on a 2008-byte block").
  /// See the family contract above.
  void zd_fin_config(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_config(
      token,
    );
  }

  late final _zd_fin_configPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_config',
      );
  late final _zd_fin_config = _zd_fin_configPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// Frees a block with no canon handle in it. The slot-only shape.
  ///
  /// Used where the Dart wrapper's whole release is a `calloc.free` — today
  /// `ZDeserializer` — and as the RE-ATTACHED shape for a wrapper whose canon
  /// handle has moved out from under it (a consumed `ShmMutBuffer`, a
  /// `KeyExpr` view's string block).
  ///
  /// See the family contract above: callback-only, not idempotent, frees a
  /// Dart-allocated block.
  void zd_fin_free_block(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_free_block(
      token,
    );
  }

  late final _zd_fin_free_blockPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_free_block',
      );
  late final _zd_fin_free_block = _zd_fin_free_blockPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// Returns how many times the entry for @p kind has fired in this process.
  ///
  /// ⚠️ **A TEST INSTRUMENT, and the only member of this family Dart calls
  /// directly.** It exists because a leak is invisible to a behavioural
  /// assertion: dispose-after-consume, double-dispose and accessor-guard cells
  /// all pass identically on leaking and on fixed code. This counter plus
  /// allocation pressure is what makes a release-path cell a RESOURCE
  /// measurement, with a both-ways injected calibration
  /// (attach-without-detach → 2 · detach-before-drop → 1 · no-attach → 0).
  ///
  /// ⚠️ **PER ENTRY, NOT GLOBAL, AND THAT IS LOAD-BEARING.** A single counter
  /// cannot say WHICH entry fired, so a wrong implementation — an owned
  /// `KeyExpr` left on the free-only shape (canon's keyexpr leaked), an
  /// escaped-pointer `ShmMutBuffer` left on the drop+free shape (a
  /// use-after-free) — would read 1 on the same counter and the cell would go
  /// green on the exact hazard it exists for.
  ///
  /// @param kind  One of the ZD_FIN_KIND_* codes above. Out-of-range returns 0:
  /// the domain is a fixed header-declared enumeration and the Dart
  /// mirror validates before calling, so this arm is unreachable by
  /// construction — the same "the Dart guard is the guard" shape
  /// the family contract describes. No failure code is introduced.
  /// @return The count, monotonically non-decreasing, never reset.
  int zd_fin_invocations(
    int kind,
  ) {
    return _zd_fin_invocations(
      kind,
    );
  }

  late final _zd_fin_invocationsPtr =
      _lookup<ffi.NativeFunction<ffi.Int Function(ffi.Int)>>(
        'zd_fin_invocations',
      );
  late final _zd_fin_invocations = _zd_fin_invocationsPtr
      .asFunction<int Function(int)>();

  /// `KeyExpr`'s net, OWNED backing only: `zd_keyexpr_drop` then `free(token)`.
  ///
  /// ⚠️ A VIEW-backed key expression does NOT use this entry. A view owns
  /// nothing — it borrows the caller's bytes — so there is no canon handle to
  /// drop, and it carries TWO `zd_fin_free_block` attachments instead (its
  /// `calloc`'d view slot and its `malloc`'d string), under one detach key.
  /// Routing an owned backing onto the free-only shape would leak canon's
  /// keyexpr silently; routing a view onto this one would drop a handle that was
  /// never owned. That is why the two counters are read separately in the cells.
  void zd_fin_keyexpr(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_keyexpr(
      token,
    );
  }

  late final _zd_fin_keyexprPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_keyexpr',
      );
  late final _zd_fin_keyexpr = _zd_fin_keyexprPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// Reports whether the LAST firing of @p kind ran on the thread that called
  /// zd_init_dart_api_dl -- i.e. the Dart mutator.
  ///
  /// ⚠️ **A TEST INSTRUMENT, and it exists because a freeze and a mutator stall
  /// read identically on a stopwatch.** The SDK documents a `NativeFinalizer`
  /// callback as running "on an arbitrary thread, with no current isolate", and
  /// a hypothesis phrased "on the finalizer thread ... freezes the isolate
  /// group" is asserting something a timing measurement alone cannot establish:
  /// a blocking release reached from the MUTATOR blocks that mutator inside GC
  /// instead, which is a different failure with the same observable. So every
  /// finalizer-timing and criterion-(ii) result in this seed records the thread
  /// beside it.
  ///
  /// The reference thread is the one that initialised the Dart API DL, captured
  /// there rather than assumed: this shim is loaded eagerly on the main thread by
  /// `native_lib.dart`, so that call site IS the mutator.
  ///
  /// @param kind  One of the ZD_FIN_KIND_* codes. Out of range returns
  /// ZD_FIN_ON_MAIN_UNOBSERVED, for the same
  /// unreachable-by-construction reason as zd_fin_invocations.
  /// @return One of the ZD_FIN_ON_MAIN_* values above.
  int zd_fin_last_on_main(
    int kind,
  ) {
    return _zd_fin_last_on_main(
      kind,
    );
  }

  late final _zd_fin_last_on_mainPtr =
      _lookup<ffi.NativeFunction<ffi.Int Function(ffi.Int)>>(
        'zd_fin_last_on_main',
      );
  late final _zd_fin_last_on_main = _zd_fin_last_on_mainPtr
      .asFunction<int Function(int)>();

  /// `Publisher`'s net: `zd_publisher_drop` then `free(token)`.
  ///
  /// ⚠️ **Reached only from the matching-listener-OFF configuration.** A
  /// publisher declared with `enableMatchingListener: true` holds a
  /// `ReceivePort`, its drop callback can post, and a post from a finalizer
  /// callback is documented undefined behaviour -- so that configuration is
  /// deliberately left out of the net and keeps today's leak-on-forget. The
  /// MARKER is on the class either way; only the NET is conditional.
  void zd_fin_publisher(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_publisher(
      token,
    );
  }

  late final _zd_fin_publisherPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_publisher',
      );
  late final _zd_fin_publisher = _zd_fin_publisherPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// `ZSerializer`'s net: `zd_serializer_drop` then `free(token)`.
  ///
  /// Same three-state shape as `zd_fin_bytes_writer` above.
  void zd_fin_serializer(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_serializer(
      token,
    );
  }

  late final _zd_fin_serializerPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_serializer',
      );
  late final _zd_fin_serializer = _zd_fin_serializerPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// `ShmMutBuffer`'s net, FRESH state only: `zd_shm_mut_drop` then `free`.
  ///
  /// ⛔ **A buffer whose `data` pointer has ESCAPED does NOT use this entry**, and
  /// a consumed one does not either. Both move to `zd_fin_free_block`, which
  /// reclaims the Dart slot and leaves the chunk alone.
  ///
  /// The reason is the difference between a leak and a use-after-free. Once a
  /// caller holds the raw `data` pointer, freeing the chunk under it would turn
  /// today's leak-on-forget into a UAF — strictly worse. So the escape DOWNGRADES
  /// the net rather than arming it: the chunk keeps exactly today's behaviour and
  /// the wrapper's own slot is still reclaimed. **The severity never increases.**
  ///
  /// ⚠️ SHM-guarded, absent from `stable` and from Android, resolved lazily from
  /// a path already behind `requireShm()`.
  void zd_fin_shm_mut(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_shm_mut(
      token,
    );
  }

  late final _zd_fin_shm_mutPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_shm_mut',
      );
  late final _zd_fin_shm_mut = _zd_fin_shm_mutPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// `ShmProvider`'s net: `zd_shm_provider_drop` then `free(token)`.
  ///
  /// ⛔ **SHM-guarded, so ABSENT from the `stable` native and from every Android
  /// build.** The Dart side resolves it lazily, at first attach, from a path
  /// already behind `requireShm()`.
  ///
  /// ⚠️ **Releasing a provider does NOT invalidate its children**, which is what
  /// makes this admissible at all: the segment is refcounted by the chunks, so a
  /// live `ShmMutBuffer`, a `toBytes()` payload and a `putBytes` all keep working
  /// after the provider is dropped. Measured, both at planning and as a cell.
  void zd_fin_shm_provider(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_shm_provider(
      token,
    );
  }

  late final _zd_fin_shm_providerPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_shm_provider',
      );
  late final _zd_fin_shm_provider = _zd_fin_shm_providerPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// `ShmProvider`'s net once an async request has been started: releases the
  /// box rather than dropping the provider.
  ///
  /// ⛔ The ordinary `zd_fin_shm_provider` drops directly, and that faults while
  /// canon holds a request -- measured through the finalizer itself, 3/3, on an
  /// exhausted pool. This entry defers instead; the provider is dropped by
  /// whoever releases the last reference.
  void zd_fin_shm_provider_deferred(
    ffi.Pointer<ffi.Void> token,
  ) {
    return _zd_fin_shm_provider_deferred(
      token,
    );
  }

  late final _zd_fin_shm_provider_deferredPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void>)>>(
        'zd_fin_shm_provider_deferred',
      );
  late final _zd_fin_shm_provider_deferred = _zd_fin_shm_provider_deferredPtr
      .asFunction<void Function(ffi.Pointer<ffi.Void>)>();

  /// Performs a get query on the given selector.
  ///
  /// Replies are posted to the Dart isolate via the given native port. An ok
  /// reply's key expression rides the message as a length-carried `Uint8List`,
  /// so an interior NUL survives.
  ///
  /// @param session        Const pointer to a loaned session (as uint8_t*).
  /// @param selector       Const pointer to the loaned selector key expression,
  /// already validated Dart-side. The `parameters` half of
  /// a selector is a separate argument.
  /// @param port           The Dart native port to post replies to.
  /// @param target         Query target (0=bestMatching, 1=all, 2=allComplete).
  /// @param consolidation  Consolidation mode (-1=auto, 0=none, 1=monotonic, 2=latest).
  /// @param payload        Pointer to z_owned_bytes_t (NULL = no payload).
  /// Consumed via z_bytes_move if non-NULL.
  /// @param encoding       MIME type string (NULL = default).
  /// @param timeout_ms     Timeout in milliseconds.
  /// @param parameters     Additional query parameters, LENGTH-CARRIED rather
  /// than NUL-terminated: the selector's parameters
  /// segment is UTF-8 text whose domain includes an
  /// interior NUL, and canon's own `z_get` is this entry
  /// with `strlen` applied. NULL with `parameters_len` 0
  /// is canon's "no parameters" spelling.
  /// @param parameters_len Byte length of `parameters`. Must be 0 when
  /// `parameters` is NULL (canon refuses NULL-with-length).
  /// @param attachment     Pointer to z_owned_bytes_t (NULL = no attachment).
  /// Consumed via z_bytes_move if non-NULL.
  /// @param congestion_control  Congestion control strategy, or -1 to leave
  /// canon's own default. NOTE: get is a REQUEST path, so
  /// canon's default here is CongestionControl::
  /// DEFAULT_REQUEST = BLOCK -- not DROP, which is the
  /// push-path default used by put/delete/publisher.
  /// @param priority       Priority 1..7, or -1 to leave canon's default (data=5).
  /// @param is_express     1/0 to set express mode, or -1 to leave canon's
  /// default (false).
  /// @param allowed_destination  z_locality_t 0..2, or -1 to leave canon's
  /// default (ANY).
  /// @param accept_replies z_reply_keyexpr_t 0..1, or -1 to leave canon's
  /// default (MATCHING_QUERY = 1).
  /// @return 0 on success, negative on failure.
  int zd_get(
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> selector,
    int port,
    int target,
    int consolidation,
    ffi.Pointer<ffi.Uint8> payload,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
    int timeout_ms,
    ffi.Pointer<ffi.Char> parameters,
    int parameters_len,
    ffi.Pointer<ffi.Uint8> attachment,
    int congestion_control,
    int priority,
    int is_express,
    int allowed_destination,
    int accept_replies,
    int retain_payload,
  ) {
    return _zd_get(
      session,
      selector,
      port,
      target,
      consolidation,
      payload,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
      timeout_ms,
      parameters,
      parameters_len,
      attachment,
      congestion_control,
      priority,
      is_express,
      allowed_destination,
      accept_replies,
      retain_payload,
    );
  }

  late final _zd_getPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Int8,
            ffi.Int8,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Uint64,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Int,
            ffi.Int8,
            ffi.Int,
            ffi.Int,
            ffi.Int,
          )
        >
      >('zd_get');
  late final _zd_get = _zd_getPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Uint8>,
          int,
          int,
          int,
          int,
          int,
          int,
        )
      >();

  /// Sends a query whose replies land in a BOUNDED CHANNEL instead of a callback.
  ///
  /// The channel-mode sibling of zd_get. Both fill canon's `z_get_options_t`
  /// through one shared body, so their option surfaces cannot drift; they differ
  /// only in which closure canon receives.
  ///
  /// Canon drops the reply closure once all replies are processed, so the channel
  /// SELF-TERMINATES at query completion — there is no entity to undeclare, and
  /// nothing a release on this handle could tell a peer.
  ///
  /// @param handler_out  Caller-allocated buffer of zd_reply_handler_sizeof(kind)
  /// bytes, written with the owned handler on success.
  /// @param tee_out      Out-param for the SHIM-owned readiness-tee context.
  /// Released through `zd_pull_tee_drop`, which is shared
  /// with the sample and query columns: the tee head is
  /// payload-agnostic. Set to NULL on every failure path.
  /// @param dart_port    Dart NativePort the tee posts readiness pings to (one
  /// per arming, none when nobody waits) and the completion
  /// sentinel from the closure's own drop.
  /// @param session      Const pointer to a loaned session (as uint8_t*).
  /// @param selector     Const pointer to the loaned selector key expression,
  /// already validated Dart-side.
  /// @param kind         0 = ring (lossy, drop-oldest), 1 = fifo (lossless,
  /// producer-backpressured).
  /// @param capacity     Channel capacity. Carried full width; never silently
  /// transformed.
  /// @param parameters   Length-carried query parameters (see zd_get).
  /// @param parameters_len  Byte length of `parameters`; 0 when it is NULL.
  /// @return 0 on success.
  /// **10** (`ZD_DECLARE_ECAPACITY`) — capacity outside canon's domain.
  /// Returned BEFORE the payload/attachment are moved, so a caller must
  /// not mark them consumed on this code; the Dart guard rejects a
  /// negative capacity earlier, which makes it unreachable from the
  /// public API.
  /// **11** (`ZD_DECLARE_EALLOC`) — the readiness-tee context could not be
  /// claimed. Returned BEFORE the payload/attachment are moved, like 10.
  /// negative — canon's own code, passed through unchanged, including the
  /// encoding rc from the shared options body (POST-move: the
  /// payload/attachment have been dropped).
  int zd_get_channel(
    ffi.Pointer<ffi.Uint8> handler_out,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> tee_out,
    int dart_port,
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> selector,
    int kind,
    int capacity,
    int target,
    int consolidation,
    ffi.Pointer<ffi.Uint8> payload,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
    int timeout_ms,
    ffi.Pointer<ffi.Char> parameters,
    int parameters_len,
    ffi.Pointer<ffi.Uint8> attachment,
    int congestion_control,
    int priority,
    int is_express,
    int allowed_destination,
    int accept_replies,
  ) {
    return _zd_get_channel(
      handler_out,
      tee_out,
      dart_port,
      session,
      selector,
      kind,
      capacity,
      target,
      consolidation,
      payload,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
      timeout_ms,
      parameters,
      parameters_len,
      attachment,
      congestion_control,
      priority,
      is_express,
      allowed_destination,
      accept_replies,
    );
  }

  late final _zd_get_channelPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Int64,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int32,
            ffi.Int64,
            ffi.Int8,
            ffi.Int8,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Uint64,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Int,
            ffi.Int8,
            ffi.Int,
            ffi.Int,
          )
        >
      >('zd_get_channel');
  late final _zd_get_channel = _zd_get_channelPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          int,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
          int,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Uint8>,
          int,
          int,
          int,
          int,
          int,
        )
      >();

  /// Returns the size of z_id_t in bytes (the raw ZID image, expected 16).
  ///
  /// Lets Dart size ZID buffers from the generator-emitted layout instead of a
  /// hardcoded literal, so a future zenoh-c layout change trips the Dart assert
  /// rather than silently mis-sizing the buffer.
  int zd_id_sizeof() {
    return _zd_id_sizeof();
  }

  late final _zd_id_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_id_sizeof');
  late final _zd_id_sizeof = _zd_id_sizeofPtr.asFunction<int Function()>();

  /// Converts a 16-byte ZID to its string representation.
  ///
  /// @param id   Pointer to a 16-byte ZID buffer.
  /// @param out  Pointer to an uninitialized z_owned_string_t to receive the result.
  void zd_id_to_string(
    ffi.Pointer<ffi.Uint8> id,
    ffi.Pointer<ffi.Opaque> out,
  ) {
    return _zd_id_to_string(
      id,
      out,
    );
  }

  late final _zd_id_to_stringPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_id_to_string');
  late final _zd_id_to_string = _zd_id_to_stringPtr
      .asFunction<
        void Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Opaque>)
      >();

  /// Collects the ZIDs of every connected peer into a shim-owned buffer.
  ///
  /// UNBOUNDED, exactly as zd_info_routers_zid above; the two differ only in
  /// which canon enumerator they call.
  ///
  /// @param session    Loaned session handle.
  /// @param out_ids    Receives a shim-owned buffer of `*out_count * 16` bytes --
  /// each ZID is 16 bytes, in storage order. Set to NULL when
  /// the enumeration is empty or the call fails. The caller
  /// MUST release it with zd_zid_list_drop().
  /// @param out_count  Receives the number of ZIDs -- **ids, not bytes**. 0 when
  /// the enumeration is empty or the call fails.
  /// @return 0 success; 11 = a shim-side allocation failed; negative = canon's
  /// own, passed through.
  int zd_info_peers_zid(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_ids,
    ffi.Pointer<ffi.Size> out_count,
  ) {
    return _zd_info_peers_zid(
      session,
      out_ids,
      out_count,
    );
  }

  late final _zd_info_peers_zidPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
          )
        >
      >('zd_info_peers_zid');
  late final _zd_info_peers_zid = _zd_info_peers_zidPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
        )
      >();

  /// Collects the ZIDs of every connected router into a shim-owned buffer.
  ///
  /// UNBOUNDED: canon's contract has no bound of any kind -- the callback fires
  /// once for each ID -- so neither does this. The buffer grows by realloc
  /// inside the collection closure.
  ///
  /// @param session    Loaned session handle.
  /// @param out_ids    Receives a shim-owned buffer of `*out_count * 16` bytes --
  /// each ZID is 16 bytes, in storage order. Set to NULL when
  /// the enumeration is empty or the call fails. The caller
  /// MUST release it with zd_zid_list_drop().
  /// @param out_count  Receives the number of ZIDs -- **ids, not bytes**. 0 when
  /// the enumeration is empty or the call fails.
  /// @return 0 success; 11 = a shim-side allocation failed; negative = canon's
  /// own, passed through.
  int zd_info_routers_zid(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_ids,
    ffi.Pointer<ffi.Size> out_count,
  ) {
    return _zd_info_routers_zid(
      session,
      out_ids,
      out_count,
    );
  }

  late final _zd_info_routers_zidPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
          )
        >
      >('zd_info_routers_zid');
  late final _zd_info_routers_zid = _zd_info_routers_zidPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
        )
      >();

  /// Copies the session's own ZID (16 bytes) into the provided buffer.
  ///
  /// @param session  Const pointer to a loaned session.
  /// @param out_id   Pointer to a 16-byte buffer to receive the ZID.
  void zd_info_zid(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Uint8> out_id,
  ) {
    return _zd_info_zid(
      session,
      out_id,
    );
  }

  late final _zd_info_zidPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Uint8>)
        >
      >('zd_info_zid');
  late final _zd_info_zid = _zd_info_zidPtr
      .asFunction<
        void Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Uint8>)
      >();

  /// Initializes the Dart native API for dynamic linking.
  ///
  /// Must be called before any other zenoh_dart functions that use
  /// Dart native ports. Pass `NativeApi.initializeApiDLData` from Dart.
  ///
  /// Returns 0 on success.
  int zd_init_dart_api_dl(
    ffi.Pointer<ffi.Void> data,
  ) {
    return _zd_init_dart_api_dl(
      data,
    );
  }

  late final _zd_init_dart_api_dlPtr =
      _lookup<ffi.NativeFunction<ffi.IntPtr Function(ffi.Pointer<ffi.Void>)>>(
        'zd_init_dart_api_dl',
      );
  late final _zd_init_dart_api_dl = _zd_init_dart_api_dlPtr
      .asFunction<int Function(ffi.Pointer<ffi.Void>)>();

  /// Initializes the zenoh logger from the RUST_LOG environment variable,
  /// falling back to the provided filter string if RUST_LOG is not set.
  ///
  /// ⛔ CLAIMS THE PROCESS-GLOBAL LOGGING SLOT, PERMANENTLY. Canon's inits are
  /// first-wins and silently idempotent, so calling this forecloses installing
  /// a host sink for the life of the process -- see zd_init_log_with_callback,
  /// which reports the foreclosure rather than returning a dead channel. It
  /// stays void itself: a caller who wanted the env logger got the env logger,
  /// and there is nothing to report.
  ///
  /// @param fallback_filter  Filter string (e.g., "error", "info", "debug").
  void zd_init_log(
    ffi.Pointer<ffi.Char> fallback_filter,
  ) {
    return _zd_init_log(
      fallback_filter,
    );
  }

  late final _zd_init_logPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Char>)>>(
        'zd_init_log',
      );
  late final _zd_init_log = _zd_init_logPtr
      .asFunction<void Function(ffi.Pointer<ffi.Char>)>();

  /// Installs a host log sink: routes canon's own log records to a Dart port.
  ///
  /// Wraps zc_init_log_with_callback(). Canon's records normally go to stderr
  /// under RUST_LOG; this hands them to the host application instead, which is
  /// what makes a level ceiling in code -- and host-side redaction -- possible
  /// at all.
  ///
  /// ⛔ THE RETURN IS "DID OUR BINDING CLAIM THE SLOT", NOT "DID IT SUCCEED".
  /// Canon returns void: it has no channel to report anything, because its
  /// three inits are first-wins, process-global and SILENTLY idempotent, and
  /// whichever runs first owns logging for the life of the process. So this
  /// wrapper keeps its own record of whether an init MADE THROUGH THIS BINDING
  /// has already claimed the slot, and reports that. It is the zd_scout
  /// did-it-start contract in the same words: non-zero means nothing was
  /// installed and no record will ever arrive, so the caller must fail rather
  /// than hand back a channel that can never deliver.
  ///
  /// ⚠️ The flag sees only inits made through this binding. An init made by
  /// other code in the same process is invisible to it, and canon reports
  /// nothing itself.
  ///
  /// Records are posted to dart_port as a two-element array: the severity as an
  /// int64 matching zc_log_severity_t, and the message as length-carried bytes.
  ///
  /// @param min_severity  0=trace 1=debug 2=info 3=warn 4=error; lower-severity
  /// records are dropped by canon before the callback.
  /// @param dart_port     NativePort to post records to.
  /// @return 0 when this call claimed the slot and installed the sink;
  /// non-zero when the slot was already claimed (nothing installed) or
  /// min_severity is outside canon's domain.
  int zd_init_log_with_callback(
    int min_severity,
    int dart_port,
  ) {
    return _zd_init_log_with_callback(
      min_severity,
      dart_port,
    );
  }

  late final _zd_init_log_with_callbackPtr =
      _lookup<ffi.NativeFunction<ffi.Int Function(ffi.Int, ffi.Int64)>>(
        'zd_init_log_with_callback',
      );
  late final _zd_init_log_with_callback = _zd_init_log_with_callbackPtr
      .asFunction<int Function(int, int)>();

  /// Converts a loaned key expression to a view string.
  ///
  /// The output view string borrows from the key expression and must not
  /// outlive it. Returns void -- always succeeds on a valid loaned keyexpr.
  ///
  /// @param ke   Const pointer to a loaned key expression.
  /// @param out  Pointer to an uninitialized z_view_string_t to receive the result.
  void zd_keyexpr_as_view_string(
    ffi.Pointer<ffi.Opaque> ke,
    ffi.Pointer<ffi.Opaque> out,
  ) {
    return _zd_keyexpr_as_view_string(
      ke,
      out,
    );
  }

  late final _zd_keyexpr_as_view_stringPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_keyexpr_as_view_string');
  late final _zd_keyexpr_as_view_string = _zd_keyexpr_as_view_stringPtr
      .asFunction<
        void Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
      >();

  /// Canonizes `buf[0..*len)` IN PLACE, writing the new length back to `*len`.
  ///
  /// 🔴 `buf` MUST be writable heap memory the caller owns. Canon rewrites it
  /// through a `&mut str`, so a string literal or any other read-only mapping
  /// SEGFAULTs -- this is the entry the header's own read-only warning is about.
  /// It must also be non-NULL, including at length 0.
  ///
  /// `*len` is written on SUCCESS ONLY: on failure the caller's length is left
  /// exactly as it was passed in.
  ///
  /// ⚠️ The rewrite may PRESERVE the length as well as shorten it
  /// (`demo/example/**/*` -> `demo/example/*/**`, 17 bytes either way), so
  /// `*len` is the only truth about the result's extent -- a caller must never
  /// assume the result is shorter, nor reuse the input length. Canon happens to
  /// zero-fill the tail from the new length to the old, which is what makes its
  /// own `strcmp`-based test pass even though this function writes no
  /// terminator; that behaviour is undocumented upstream, so rely on
  /// `[0..*len)` and nothing else.
  ///
  /// @param buf  Writable, caller-owned, non-NULL buffer of `*len` bytes of
  /// UTF-8 key expression text. Rewritten in place on success.
  /// @param len  In: the byte length of `buf`. Out (success only): the byte
  /// length of the canonized expression.
  /// @return 0 on success, Z_EINVAL (-1) if the expression is invalid even
  /// after canonization.
  int zd_keyexpr_canonize(
    ffi.Pointer<ffi.Char> buf,
    ffi.Pointer<ffi.Size> len,
  ) {
    return _zd_keyexpr_canonize(
      buf,
      len,
    );
  }

  late final _zd_keyexpr_canonizePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(ffi.Pointer<ffi.Char>, ffi.Pointer<ffi.Size>)
        >
      >('zd_keyexpr_canonize');
  late final _zd_keyexpr_canonize = _zd_keyexpr_canonizePtr
      .asFunction<int Function(ffi.Pointer<ffi.Char>, ffi.Pointer<ffi.Size>)>();

  /// Clones a key expression from a LOANED source into an owned handle.
  ///
  /// ⚠️ This clones what the source HOLDS, which is not always the bytes. A
  /// key expression built from an owned constructor holds its own storage and
  /// clones independently -- but a `z_view_keyexpr_t` holds a BORROW of the
  /// caller's buffer, and cloning it copies the borrow. Measured: clone a view
  /// key expression, free the buffer it borrows, and the clone reads freed
  /// memory. Use zd_keyexpr_from_substr to copy a view's bytes instead.
  ///
  /// Returns void -- canon's clone cannot fail.
  ///
  /// @param dst  Pointer to an uninitialized z_owned_keyexpr_t.
  /// @param src  Const pointer to the loaned key expression to clone.
  void zd_keyexpr_clone(
    ffi.Pointer<z_owned_keyexpr_t> dst,
    ffi.Pointer<ffi.Opaque> src,
  ) {
    return _zd_keyexpr_clone(
      dst,
      src,
    );
  }

  late final _zd_keyexpr_clonePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(
            ffi.Pointer<z_owned_keyexpr_t>,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_keyexpr_clone');
  late final _zd_keyexpr_clone = _zd_keyexpr_clonePtr
      .asFunction<
        void Function(ffi.Pointer<z_owned_keyexpr_t>, ffi.Pointer<ffi.Opaque>)
      >();

  /// Concatenates `right_start[0..right_len)` onto `left`, without a separator.
  ///
  /// The right operand crosses as a pointer and a length, NOT as a C string, so
  /// an interior NUL is carried. `right_start` must be a valid non-NULL pointer
  /// even when `right_len` is 0: canon builds a Rust slice from it, and
  /// slice::from_raw_parts(NULL, 0) is undefined behaviour.
  ///
  /// Canon does NOT validate the LEFT operand -- concatenating onto a
  /// gravestone silently yields the literal "dummy" followed by the right --
  /// and it forbids joining an expression ending in `*` to one starting with
  /// `*`. Every validation failure except invalid UTF-8 collapses to
  /// Z_EGENERIC, with the reason only in canon's log.
  ///
  /// @param out          Pointer to an uninitialized z_owned_keyexpr_t. Written
  /// on every path -- a gravestone on failure -- so the
  /// caller must check the return code before reading it.
  /// @param left         Const pointer to the loaned left operand.
  /// @param right_start  Pointer to `right_len` bytes. Never NULL.
  /// @param right_len    Length of the right operand in bytes.
  /// @return 0 on success, Z_EINVAL (-1) if the right operand is not valid
  /// UTF-8, Z_EGENERIC (-128) for every other failure.
  int zd_keyexpr_concat(
    ffi.Pointer<z_owned_keyexpr_t> out,
    ffi.Pointer<ffi.Opaque> left,
    ffi.Pointer<ffi.Char> right_start,
    int right_len,
  ) {
    return _zd_keyexpr_concat(
      out,
      left,
      right_start,
      right_len,
    );
  }

  late final _zd_keyexpr_concatPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<z_owned_keyexpr_t>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
          )
        >
      >('zd_keyexpr_concat');
  late final _zd_keyexpr_concat = _zd_keyexpr_concatPtr
      .asFunction<
        int Function(
          ffi.Pointer<z_owned_keyexpr_t>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Char>,
          int,
        )
      >();

  /// Drops an owned key expression, releasing its native allocation.
  ///
  /// This is the LOCAL release. It does not unregister a declared key
  /// expression from its session -- see zd_undeclare_keyexpr for that. Safe on
  /// a gravestone (canon's null-drop contract).
  ///
  /// @param ke  Pointer to a z_owned_keyexpr_t. Left gravestoned.
  void zd_keyexpr_drop(
    ffi.Pointer<z_owned_keyexpr_t> ke,
  ) {
    return _zd_keyexpr_drop(
      ke,
    );
  }

  late final _zd_keyexpr_dropPtr =
      _lookup<
        ffi.NativeFunction<ffi.Void Function(ffi.Pointer<z_owned_keyexpr_t>)>
      >('zd_keyexpr_drop');
  late final _zd_keyexpr_drop = _zd_keyexpr_dropPtr
      .asFunction<void Function(ffi.Pointer<z_owned_keyexpr_t>)>();

  /// Returns true if the key expressions are equal in zenoh semantics.
  ///
  /// @param a  Const pointer to a loaned key expression.
  /// @param b  Const pointer to a loaned key expression.
  /// @return true if the key expressions are equal.
  bool zd_keyexpr_equals(
    ffi.Pointer<ffi.Opaque> a,
    ffi.Pointer<ffi.Opaque> b,
  ) {
    return _zd_keyexpr_equals(
      a,
      b,
    );
  }

  late final _zd_keyexpr_equalsPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Bool Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_keyexpr_equals');
  late final _zd_keyexpr_equals = _zd_keyexpr_equalsPtr
      .asFunction<
        bool Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
      >();

  /// Constructs an OWNED key expression by COPYING `expr[0..len)`.
  ///
  /// Unlike zd_view_keyexpr_from_substr, the result borrows nothing: the buffer
  /// may be freed as soon as this returns. Length-carried, so an interior NUL
  /// is part of the copied expression rather than a terminator.
  ///
  /// @param out   Pointer to an uninitialized z_owned_keyexpr_t. Written on
  /// every path, gravestone included.
  /// @param expr  Pointer to `len` bytes of UTF-8 key expression text.
  /// @param len   Length of `expr` in bytes.
  /// @return 0 on success, negative on failure.
  int zd_keyexpr_from_substr(
    ffi.Pointer<z_owned_keyexpr_t> out,
    ffi.Pointer<ffi.Char> expr,
    int len,
  ) {
    return _zd_keyexpr_from_substr(
      out,
      expr,
      len,
    );
  }

  late final _zd_keyexpr_from_substrPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<z_owned_keyexpr_t>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
          )
        >
      >('zd_keyexpr_from_substr');
  late final _zd_keyexpr_from_substr = _zd_keyexpr_from_substrPtr
      .asFunction<
        int Function(ffi.Pointer<z_owned_keyexpr_t>, ffi.Pointer<ffi.Char>, int)
      >();

  /// Constructs an OWNED key expression from `expr[0..*len)`, canonizing first.
  ///
  /// This is the canonize-then-validate composition: a non-canon expression is
  /// rewritten and then accepted; one that is still invalid after the rewrite
  /// is rejected. There is no "was canonized" signal in the return value --
  /// canon carries none, and this shim invents none.
  ///
  /// COPIES BEFORE CANONIZING: `expr` is `const` and is never written, so a
  /// read-only buffer is safe here -- unlike zd_keyexpr_canonize, which mutates
  /// the caller's memory. `expr` must still be non-NULL, including at length 0.
  /// (Canon's view-backed sibling `z_view_keyexpr_from_substr_autocanonize`
  /// rewrites the caller's buffer in place and aliases it; it is deliberately
  /// not bound -- same service, other backing.)
  ///
  /// @param out   Pointer to an uninitialized z_owned_keyexpr_t. Written on
  /// EVERY path -- a gravestone on failure -- so the caller must
  /// check the return code before wrapping it.
  /// @param expr  Pointer to `*len` bytes of UTF-8 key expression text. Never
  /// NULL, never written.
  /// @param len   In: the byte length of `expr`. Out (success only): the byte
  /// length of the canonized expression, which may be shorter than
  /// or equal to the input length -- never assume it shrank.
  /// @return 0 on success, Z_EINVAL (-1) if the expression is invalid even
  /// after canonization.
  int zd_keyexpr_from_substr_autocanonize(
    ffi.Pointer<z_owned_keyexpr_t> out,
    ffi.Pointer<ffi.Char> expr,
    ffi.Pointer<ffi.Size> len,
  ) {
    return _zd_keyexpr_from_substr_autocanonize(
      out,
      expr,
      len,
    );
  }

  late final _zd_keyexpr_from_substr_autocanonizePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<z_owned_keyexpr_t>,
            ffi.Pointer<ffi.Char>,
            ffi.Pointer<ffi.Size>,
          )
        >
      >('zd_keyexpr_from_substr_autocanonize');
  late final _zd_keyexpr_from_substr_autocanonize =
      _zd_keyexpr_from_substr_autocanonizePtr
          .asFunction<
            int Function(
              ffi.Pointer<z_owned_keyexpr_t>,
              ffi.Pointer<ffi.Char>,
              ffi.Pointer<ffi.Size>,
            )
          >();

  /// Returns true if key expression a includes b (every key in b is in a).
  ///
  /// @param a  Const pointer to a loaned key expression.
  /// @param b  Const pointer to a loaned key expression.
  /// @return true if a includes b.
  bool zd_keyexpr_includes(
    ffi.Pointer<ffi.Opaque> a,
    ffi.Pointer<ffi.Opaque> b,
  ) {
    return _zd_keyexpr_includes(
      a,
      b,
    );
  }

  late final _zd_keyexpr_includesPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Bool Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_keyexpr_includes');
  late final _zd_keyexpr_includes = _zd_keyexpr_includesPtr
      .asFunction<
        bool Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
      >();

  /// Returns true if the key expressions intersect (share at least one key).
  ///
  /// Takes LOANED handles, so a view, an owned and a declared key expression
  /// are all admissible operands. (The view and owned C types happen to share a
  /// representation today, so a view-typed parameter would accept an owned
  /// pointer and behave correctly -- by coincidence, not by contract. These
  /// signatures do not rely on that.)
  ///
  /// @param a  Const pointer to a loaned key expression.
  /// @param b  Const pointer to a loaned key expression.
  /// @return true if the key expressions intersect.
  bool zd_keyexpr_intersects(
    ffi.Pointer<ffi.Opaque> a,
    ffi.Pointer<ffi.Opaque> b,
  ) {
    return _zd_keyexpr_intersects(
      a,
      b,
    );
  }

  late final _zd_keyexpr_intersectsPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Bool Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_keyexpr_intersects');
  late final _zd_keyexpr_intersects = _zd_keyexpr_intersectsPtr
      .asFunction<
        bool Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
      >();

  /// Reports whether `expr[0..len)` is a key expression in CANON form.
  ///
  /// Pure validation: the buffer is NEVER written, so a read-only or const
  /// buffer is safe here (unlike zd_keyexpr_canonize).
  ///
  /// 🔴 Canon has NO NULL guard on this entry -- it is the one family member
  /// canon never NULL-tests -- so `expr` MUST be non-NULL even when `len` is 0.
  /// Canon builds a Rust slice from the pointer, and
  /// slice::from_raw_parts(NULL, 0) is undefined behaviour.
  ///
  /// Length-carried, like every other key expression entry this shim binds: an
  /// interior NUL is an ordinary chunk byte of the expression, not a
  /// terminator.
  ///
  /// @param expr  Pointer to `len` bytes of UTF-8 key expression text. Never
  /// NULL, including at length 0.
  /// @param len   Length of `expr` in bytes.
  /// @return 0 if the expression is valid AND in canon form, Z_EINVAL (-1) if
  /// it is invalid OR merely non-canon -- canon does not discriminate
  /// the two, so a caller may collapse this to a bool without losing
  /// information canon was carrying.
  int zd_keyexpr_is_canon(
    ffi.Pointer<ffi.Char> expr,
    int len,
  ) {
    return _zd_keyexpr_is_canon(
      expr,
      len,
    );
  }

  late final _zd_keyexpr_is_canonPtr =
      _lookup<
        ffi.NativeFunction<ffi.Int Function(ffi.Pointer<ffi.Char>, ffi.Size)>
      >('zd_keyexpr_is_canon');
  late final _zd_keyexpr_is_canon = _zd_keyexpr_is_canonPtr
      .asFunction<int Function(ffi.Pointer<ffi.Char>, int)>();

  /// Joins two key expressions, inserting the `/` separator between them.
  ///
  /// Canon inserts the separator, which is why this takes two whole key
  /// expressions rather than a trailing byte range: the right operand must
  /// itself be a valid key expression. Failures collapse to Z_EGENERIC.
  ///
  /// @param out    Pointer to an uninitialized z_owned_keyexpr_t. Written on
  /// every path, gravestone included.
  /// @param left   Const pointer to the loaned left operand.
  /// @param right  Const pointer to the loaned right operand.
  /// @return 0 on success, Z_EGENERIC (-128) on failure.
  int zd_keyexpr_join(
    ffi.Pointer<z_owned_keyexpr_t> out,
    ffi.Pointer<ffi.Opaque> left,
    ffi.Pointer<ffi.Opaque> right,
  ) {
    return _zd_keyexpr_join(
      out,
      left,
      right,
    );
  }

  late final _zd_keyexpr_joinPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<z_owned_keyexpr_t>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_keyexpr_join');
  late final _zd_keyexpr_join = _zd_keyexpr_joinPtr
      .asFunction<
        int Function(
          ffi.Pointer<z_owned_keyexpr_t>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
        )
      >();

  /// Obtains a const loaned reference to an OWNED key expression.
  ///
  /// The owned and the view constructors produce different structs but loan to
  /// the same `z_loaned_keyexpr_t`, which is why every operation downstream can
  /// take one loaned parameter regardless of a key expression's origin.
  ///
  /// @param ke  Pointer to a valid z_owned_keyexpr_t.
  /// @return Const pointer to the loaned key expression.
  ffi.Pointer<ffi.Opaque> zd_keyexpr_loan(
    ffi.Pointer<z_owned_keyexpr_t> ke,
  ) {
    return _zd_keyexpr_loan(
      ke,
    );
  }

  late final _zd_keyexpr_loanPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<z_owned_keyexpr_t>)
        >
      >('zd_keyexpr_loan');
  late final _zd_keyexpr_loan = _zd_keyexpr_loanPtr
      .asFunction<
        ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<z_owned_keyexpr_t>)
      >();

  /// Returns the size of z_owned_keyexpr_t in bytes.
  ///
  /// Used by Dart to allocate the correct amount of native memory
  /// for opaque zenoh types.
  int zd_keyexpr_sizeof() {
    return _zd_keyexpr_sizeof();
  }

  late final _zd_keyexpr_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_keyexpr_sizeof');
  late final _zd_keyexpr_sizeof = _zd_keyexpr_sizeofPtr
      .asFunction<int Function()>();

  /// Declares a background liveliness subscriber on the given key expression.
  ///
  /// Unlike zd_liveliness_declare_subscriber, this has no handle -- it lives
  /// until the session is closed. Samples (PUT on token declare, DELETE on
  /// undeclare) are posted to the Dart NativePort. When the session closes and
  /// the background subscriber is dropped internally by zenoh-c, a null sentinel
  /// is posted to signal stream completion.
  ///
  /// @param session   Const pointer to a loaned session.
  /// @param key_expr  Const pointer to the loaned key expression, already
  /// validated Dart-side.
  /// @param dart_port The Dart native port to post samples to.
  /// @param history   Boolean (0=false, 1=true) for receiving pre-existing token state.
  /// @param retain_payload  Non-zero makes each delivered sample carry an OWNED
  /// clone of its payload as message element 9. Zero
  /// posts kNull there and allocates nothing.
  /// @return 0 on success, negative on failure.
  int zd_liveliness_declare_background_subscriber(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int dart_port,
    int history,
    int retain_payload,
  ) {
    return _zd_liveliness_declare_background_subscriber(
      session,
      key_expr,
      dart_port,
      history,
      retain_payload,
    );
  }

  late final _zd_liveliness_declare_background_subscriberPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Int8,
            ffi.Int,
          )
        >
      >('zd_liveliness_declare_background_subscriber');
  late final _zd_liveliness_declare_background_subscriber =
      _zd_liveliness_declare_background_subscriberPtr
          .asFunction<
            int Function(
              ffi.Pointer<ffi.Opaque>,
              ffi.Pointer<ffi.Opaque>,
              int,
              int,
              int,
            )
          >();

  /// Declares a liveliness subscriber on the given key expression.
  ///
  /// Reuses the same z_owned_subscriber_t type and _zd_sample_callback/drop
  /// as the regular subscriber. Samples are posted to the Dart NativePort.
  ///
  /// @param subscriber_out  Pointer to an uninitialized z_owned_subscriber_t (as uint8_t*).
  /// @param session         Const pointer to a loaned session (as uint8_t*).
  /// @param key_expr        Const pointer to the loaned key expression,
  /// already validated Dart-side.
  /// @param port            Dart NativePort for sample callbacks.
  /// @param history         Boolean (0=false, 1=true) for receiving pre-existing token state.
  /// @param retain_payload  Non-zero makes each delivered sample carry an OWNED
  /// clone of its payload as message element 9. Zero
  /// posts kNull there and allocates nothing.
  /// @return 0 on success, negative on failure.
  int zd_liveliness_declare_subscriber(
    ffi.Pointer<ffi.Uint8> subscriber_out,
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int port,
    int history,
    int retain_payload,
  ) {
    return _zd_liveliness_declare_subscriber(
      subscriber_out,
      session,
      key_expr,
      port,
      history,
      retain_payload,
    );
  }

  late final _zd_liveliness_declare_subscriberPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Int8,
            ffi.Int,
          )
        >
      >('zd_liveliness_declare_subscriber');
  late final _zd_liveliness_declare_subscriber =
      _zd_liveliness_declare_subscriberPtr
          .asFunction<
            int Function(
              ffi.Pointer<ffi.Uint8>,
              ffi.Pointer<ffi.Uint8>,
              ffi.Pointer<ffi.Opaque>,
              int,
              int,
              int,
            )
          >();

  /// Declares a liveliness token on the given key expression.
  ///
  /// @param token_out  Pointer to an uninitialized z_owned_liveliness_token_t (as uint8_t*).
  /// @param session    Const pointer to a loaned session (as uint8_t*).
  /// @param key_expr   Const pointer to the loaned key expression, already
  /// validated Dart-side.
  /// @return 0 on success, negative on failure.
  int zd_liveliness_declare_token(
    ffi.Pointer<ffi.Uint8> token_out,
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
  ) {
    return _zd_liveliness_declare_token(
      token_out,
      session,
      key_expr,
    );
  }

  late final _zd_liveliness_declare_tokenPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_liveliness_declare_token');
  late final _zd_liveliness_declare_token = _zd_liveliness_declare_tokenPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
        )
      >();

  /// Queries liveliness tokens matching the given key expression.
  ///
  /// Replies are posted to the Dart NativePort as arrays (same format as
  /// zd_get replies). A null sentinel signals completion.
  ///
  /// @param session   Loaned session pointer.
  /// @param key_expr  Const pointer to the loaned key expression to query
  /// liveliness for, already validated Dart-side.
  /// @param port      Dart NativePort for reply callbacks.
  /// @param timeout_ms  Timeout in milliseconds. ⚠️ NOT a 0-sentinel — 0 means
  /// ZERO here, and a liveliness query with a 0 ms timeout
  /// expires immediately and returns nothing. This doc line
  /// previously repeated canon's own false claim
  /// (`zenoh_commons.h`: "0 means default query timeout from
  /// zenoh configuration"); verified at source, canon applies
  /// this value UNCONDITIONALLY for liveliness
  /// (`liveliness.rs:279` — its only guard is "were options
  /// supplied", not "is the value non-zero"), unlike `z_get`
  /// and `z_querier`, which do carry a genuine `!= 0` sentinel.
  /// canon's own default is 10000; the Dart side sends it
  /// explicitly, which is why the default works today.
  /// @return 0 on success.
  int zd_liveliness_get(
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int port,
    int timeout_ms,
    int retain_payload,
  ) {
    return _zd_liveliness_get(
      session,
      key_expr,
      port,
      timeout_ms,
      retain_payload,
    );
  }

  late final _zd_liveliness_getPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Uint64,
            ffi.Int,
          )
        >
      >('zd_liveliness_get');
  late final _zd_liveliness_get = _zd_liveliness_getPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
        )
      >();

  /// Queries liveliness tokens, with replies landing in a BOUNDED CHANNEL.
  ///
  /// The channel-mode sibling of zd_liveliness_get, over the same shared
  /// reply-channel construction as the other two reply carriers.
  ///
  /// @param handler_out  Caller-allocated `zd_reply_handler_sizeof(kind)` buffer.
  /// @param tee_out      Out-param for the SHIM-owned readiness-tee context.
  /// @param dart_port    Dart NativePort the tee posts readiness pings to.
  /// @param timeout_ms   ⚠️ NOT a 0-sentinel — see zd_liveliness_get's contract.
  /// @return 0 / 10 (capacity) / 11 (tee allocation) / negative from canon.
  int zd_liveliness_get_channel(
    ffi.Pointer<ffi.Uint8> handler_out,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> tee_out,
    int dart_port,
    ffi.Pointer<ffi.Uint8> session,
    ffi.Pointer<ffi.Opaque> key_expr,
    int kind,
    int capacity,
    int timeout_ms,
  ) {
    return _zd_liveliness_get_channel(
      handler_out,
      tee_out,
      dart_port,
      session,
      key_expr,
      kind,
      capacity,
      timeout_ms,
    );
  }

  late final _zd_liveliness_get_channelPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Int64,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int32,
            ffi.Int64,
            ffi.Uint64,
          )
        >
      >('zd_liveliness_get_channel');
  late final _zd_liveliness_get_channel = _zd_liveliness_get_channelPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          int,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
        )
      >();

  /// Drops (undeclares and frees) a liveliness token.
  ///
  /// @param token  Pointer to a z_owned_liveliness_token_t (as uint8_t*).
  void zd_liveliness_token_drop(
    ffi.Pointer<ffi.Uint8> token,
  ) {
    return _zd_liveliness_token_drop(
      token,
    );
  }

  late final _zd_liveliness_token_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_liveliness_token_drop',
      );
  late final _zd_liveliness_token_drop = _zd_liveliness_token_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>)>();

  /// Returns the size of z_owned_liveliness_token_t in bytes.
  int zd_liveliness_token_sizeof() {
    return _zd_liveliness_token_sizeof();
  }

  late final _zd_liveliness_token_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>(
        'zd_liveliness_token_sizeof',
      );
  late final _zd_liveliness_token_sizeof = _zd_liveliness_token_sizeofPtr
      .asFunction<int Function()>();

  /// Opens a Zenoh session with the given configuration.
  ///
  /// @param session  Pointer to an uninitialized z_owned_session_t.
  /// @param config   Pointer to a z_owned_config_t (consumed by z_open).
  /// @return 0 on success, negative on failure.
  int zd_open_session(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Opaque> config,
  ) {
    return _zd_open_session(
      session,
      config,
    );
  }

  late final _zd_open_sessionPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_open_session');
  late final _zd_open_session = _zd_open_sessionPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Opaque>)
      >();

  /// Opens a session WITHOUT blocking the caller.
  ///
  /// z_open blocks for a caller-controlled duration -- measured 505 ms against a
  /// configured-but-unreachable endpoint and 3005 ms for a client-mode failure at
  /// the pinned 1.8.0. This entry hands that blocking call to a shim-owned
  /// detached thread and returns immediately; the outcome arrives as ONE post on
  /// @p dart_port.
  ///
  /// THE RETURN MEANS "DID IT START", NOT "DID IT SUCCEED":
  ///
  /// 0                    the worker started; EXACTLY ONE post will arrive
  /// 12  ZD_OPEN_EALLOC   a heap allocation failed; nothing started
  /// 13  ZD_OPEN_ETHREAD  pthread_create failed; nothing started
  ///
  /// Non-zero means NO post is coming, so the caller must fail rather than wait
  /// on a completion that can never arrive. Canon's own outcome -- including
  /// every canon failure -- travels in the post, never in this return.
  ///
  /// The codes sit in canon-free POSITIVE space. Canon's positive returns are 1
  /// (Z_CHANNEL_DISCONNECTED) and 2 (Z_CHANNEL_NODATA); this shim already uses
  /// 10 and 11 elsewhere. 10 is UNREACHABLE here -- it is the capacity-argument
  /// rejection and this entry takes no capacity argument. 11 is DELIBERATELY NOT
  /// REUSED: reply_channel_alloc_harness.dart:18 opens a session under the malloc
  /// injector and asserts code == 11 from zd_get_channel, so reusing it here
  /// would let a green report come from the wrong site.
  ///
  /// POST SHAPE -- one array of three:
  /// [0] kInt64  canon's z_open result: 0 on success, negative on failure
  /// [1] kInt64  the session block's address on success, 0 on failure
  /// [2] length-carried bytes, or null -- canon's failure detail, captured on
  /// the worker immediately after the failing call. Never kString: that
  /// truncates at an interior NUL.
  ///
  /// On success the address in [1] is a SHIM-OWNED block; release it with
  /// zd_session_close_drop and never with a caller-side free.
  ///
  /// @param config     Config to consume, or NULL for canon defaults. Its
  /// content is taken before this returns, on every path.
  /// @param dart_port  Native port the single completion post is sent to.
  int zd_open_session_async(
    ffi.Pointer<ffi.Opaque> config,
    int dart_port,
  ) {
    return _zd_open_session_async(
      config,
      dart_port,
    );
  }

  late final _zd_open_session_asyncPtr =
      _lookup<
        ffi.NativeFunction<ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Int64)>
      >('zd_open_session_async');
  late final _zd_open_session_async = _zd_open_session_asyncPtr
      .asFunction<int Function(ffi.Pointer<ffi.Opaque>, int)>();

  /// Declares a background matching listener on the publisher.
  ///
  /// Matching status changes are posted to the Dart isolate via the given
  /// native port as Int64 values (1 = matching, 0 = not matching).
  ///
  /// @param publisher  Const pointer to a loaned publisher.
  /// @param dart_port  The Dart native port to post matching status to.
  /// @return 0 on success, negative on failure.
  int zd_publisher_declare_background_matching_listener(
    ffi.Pointer<ffi.Opaque> publisher,
    int dart_port,
  ) {
    return _zd_publisher_declare_background_matching_listener(
      publisher,
      dart_port,
    );
  }

  late final _zd_publisher_declare_background_matching_listenerPtr =
      _lookup<
        ffi.NativeFunction<ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Int64)>
      >('zd_publisher_declare_background_matching_listener');
  late final _zd_publisher_declare_background_matching_listener =
      _zd_publisher_declare_background_matching_listenerPtr
          .asFunction<int Function(ffi.Pointer<ffi.Opaque>, int)>();

  /// Sends a DELETE through the publisher.
  ///
  /// @param publisher  Const pointer to a loaned publisher.
  /// @param timestamp  Optional pointer to 24 raw z_timestamp_t bytes, or NULL
  /// for none. Borrowed (copied into aligned storage), not
  /// consumed.
  /// @return 0 on success, negative on failure.
  int zd_publisher_delete(
    ffi.Pointer<ffi.Opaque> publisher,
    ffi.Pointer<ffi.Uint8> timestamp,
  ) {
    return _zd_publisher_delete(
      publisher,
      timestamp,
    );
  }

  late final _zd_publisher_deletePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Uint8>)
        >
      >('zd_publisher_delete');
  late final _zd_publisher_delete = _zd_publisher_deletePtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Uint8>)
      >();

  /// Drops (undeclares and frees) a publisher.
  void zd_publisher_drop(
    ffi.Pointer<ffi.Opaque> publisher,
  ) {
    return _zd_publisher_drop(
      publisher,
    );
  }

  late final _zd_publisher_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_publisher_drop',
      );
  late final _zd_publisher_drop = _zd_publisher_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Gets the current matching status of a publisher.
  ///
  /// @param publisher  Const pointer to a loaned publisher.
  /// @param matching   Out parameter: filled with 0 (no match) or 1 (match).
  /// @return 0 on success, negative on failure.
  int zd_publisher_get_matching_status(
    ffi.Pointer<ffi.Opaque> publisher,
    ffi.Pointer<ffi.Int> matching,
  ) {
    return _zd_publisher_get_matching_status(
      publisher,
      matching,
    );
  }

  late final _zd_publisher_get_matching_statusPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Int>)
        >
      >('zd_publisher_get_matching_status');
  late final _zd_publisher_get_matching_status =
      _zd_publisher_get_matching_statusPtr
          .asFunction<
            int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Int>)
          >();

  /// Returns the key expression of a publisher.
  ffi.Pointer<ffi.Opaque> zd_publisher_keyexpr(
    ffi.Pointer<ffi.Opaque> publisher,
  ) {
    return _zd_publisher_keyexpr(
      publisher,
    );
  }

  late final _zd_publisher_keyexprPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_publisher_keyexpr');
  late final _zd_publisher_keyexpr = _zd_publisher_keyexprPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Obtains a const loaned reference to the publisher.
  ffi.Pointer<ffi.Opaque> zd_publisher_loan(
    ffi.Pointer<ffi.Opaque> publisher,
  ) {
    return _zd_publisher_loan(
      publisher,
    );
  }

  late final _zd_publisher_loanPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_publisher_loan');
  late final _zd_publisher_loan = _zd_publisher_loanPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Publishes data through the publisher.
  ///
  /// @param publisher   Const pointer to a loaned publisher.
  /// @param payload     Pointer to owned bytes (consumed via z_bytes_move).
  /// @param encoding    MIME type string for per-put encoding override (NULL = publisher default).
  /// @param attachment  Pointer to owned bytes for attachment (consumed if non-NULL, NULL = no attachment).
  /// @param timestamp   Optional pointer to 24 raw z_timestamp_t bytes, or NULL
  /// for none. Borrowed (copied into aligned storage), not
  /// consumed -- the caller retains ownership.
  /// @return 0 on success, negative on failure.
  int zd_publisher_put(
    ffi.Pointer<ffi.Opaque> publisher,
    ffi.Pointer<ffi.Opaque> payload,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
    ffi.Pointer<ffi.Opaque> attachment,
    ffi.Pointer<ffi.Uint8> timestamp,
  ) {
    return _zd_publisher_put(
      publisher,
      payload,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
      attachment,
      timestamp,
    );
  }

  late final _zd_publisher_putPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Uint8>,
          )
        >
      >('zd_publisher_put');
  late final _zd_publisher_put = _zd_publisher_putPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Uint8>,
        )
      >();

  /// Returns the size of z_owned_publisher_t in bytes.
  int zd_publisher_sizeof() {
    return _zd_publisher_sizeof();
  }

  late final _zd_publisher_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_publisher_sizeof');
  late final _zd_publisher_sizeof = _zd_publisher_sizeofPtr
      .asFunction<int Function()>();

  void zd_pull_handler_drop(
    ffi.Pointer<ffi.Uint8> handler,
    int kind,
  ) {
    return _zd_pull_handler_drop(
      handler,
      kind,
    );
  }

  late final _zd_pull_handler_dropPtr =
      _lookup<
        ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>, ffi.Int32)>
      >('zd_pull_handler_drop');
  late final _zd_pull_handler_drop = _zd_pull_handler_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>, int)>();

  /// Returns the size in bytes of the owned handler slot for `kind`.
  ///
  /// The two handler types are distinct, so the caller must allocate the slot
  /// for the kind it is about to declare and release it through
  /// `zd_pull_handler_drop` with the SAME kind.
  ///
  /// @param kind  0 = ring, 1 = fifo (see above).
  /// @return The slot size in bytes. Cannot fail.
  int zd_pull_handler_sizeof(
    int kind,
  ) {
    return _zd_pull_handler_sizeof(
      kind,
    );
  }

  late final _zd_pull_handler_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Int32 Function(ffi.Int32)>>(
        'zd_pull_handler_sizeof',
      );
  late final _zd_pull_handler_sizeof = _zd_pull_handler_sizeofPtr
      .asFunction<int Function(int)>();

  int zd_pull_subscriber_try_recv(
    ffi.Pointer<ffi.Uint8> handler,
    int kind,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_keyexpr,
    ffi.Pointer<ffi.Size> out_keyexpr_len,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_payload,
    ffi.Pointer<ffi.Size> out_payload_len,
    ffi.Pointer<ffi.Int8> out_kind,
    ffi.Pointer<ffi.Pointer<ffi.Char>> out_encoding,
    ffi.Pointer<ffi.Size> out_encoding_len,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_attachment,
    ffi.Pointer<ffi.Size> out_attachment_len,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_timestamp,
    ffi.Pointer<ffi.Int8> out_priority,
    ffi.Pointer<ffi.Int8> out_congestion,
    ffi.Pointer<ffi.Int8> out_express,
    ffi.Pointer<ffi.Uint8> retain_slot,
    ffi.Pointer<ffi.Int32> out_has_retained,
  ) {
    return _zd_pull_subscriber_try_recv(
      handler,
      kind,
      out_keyexpr,
      out_keyexpr_len,
      out_payload,
      out_payload_len,
      out_kind,
      out_encoding,
      out_encoding_len,
      out_attachment,
      out_attachment_len,
      out_timestamp,
      out_priority,
      out_congestion,
      out_express,
      retain_slot,
      out_has_retained,
    );
  }

  late final _zd_pull_subscriber_try_recvPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Int32,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Int8>,
            ffi.Pointer<ffi.Pointer<ffi.Char>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Int8>,
            ffi.Pointer<ffi.Int8>,
            ffi.Pointer<ffi.Int8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Int32>,
          )
        >
      >('zd_pull_subscriber_try_recv');
  late final _zd_pull_subscriber_try_recv = _zd_pull_subscriber_try_recvPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          int,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Int8>,
          ffi.Pointer<ffi.Pointer<ffi.Char>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Int8>,
          ffi.Pointer<ffi.Int8>,
          ffi.Pointer<ffi.Int8>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Int32>,
        )
      >();

  /// Arms the readiness tee: the NEXT delivery posts exactly one ping.
  ///
  /// Idempotent and cheap -- it stores 1 into an atomic flag. The delivery that
  /// posts clears it, so one arming yields at most one ping, and a channel with
  /// nobody waiting posts nothing at all. That gate is what keeps the mechanism
  /// bounded by the channel's own capacity instead of queueing one message per
  /// delivered sample.
  ///
  /// Safe to call after the producing session has died: the context outlives the
  /// closure's drop by construction (see `zd_pull_tee_drop`). NOT safe after
  /// `zd_pull_tee_drop`.
  ///
  /// @param tee  The context from `zd_declare_pull_subscriber`.
  void zd_pull_tee_arm(
    ffi.Pointer<ffi.Uint8> tee,
  ) {
    return _zd_pull_tee_arm(
      tee,
    );
  }

  late final _zd_pull_tee_armPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_pull_tee_arm',
      );
  late final _zd_pull_tee_arm = _zd_pull_tee_armPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>)>();

  /// Frees the readiness-tee context. The DESIGNATED release point.
  ///
  /// ⚠️ Call it only AFTER `zd_subscriber_drop`, which zenoh-c 1.8.0 (#1221)
  /// guarantees blocks until executing callbacks are destroyed. The closure's
  /// own drop callback deliberately does NOT free this block: it fires whenever
  /// the SESSION dies, which can happen while a live Dart handle may still call
  /// `zd_pull_tee_arm`. Separating the free from the drop is what makes that
  /// ordering safe rather than a use-after-free.
  ///
  /// @param tee  The context from `zd_declare_pull_subscriber`.
  void zd_pull_tee_drop(
    ffi.Pointer<ffi.Uint8> tee,
  ) {
    return _zd_pull_tee_drop(
      tee,
    );
  }

  late final _zd_pull_tee_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_pull_tee_drop',
      );
  late final _zd_pull_tee_drop = _zd_pull_tee_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>)>();

  /// Publishes data on the given key expression.
  ///
  /// The payload and attachment are consumed (moved) by this call -- the
  /// caller must not use the owned bytes after calling zd_put, regardless
  /// of the return code (z_bytes_move gravestones them either way).
  ///
  /// @param session     Const pointer to a loaned session.
  /// @param keyexpr     Const pointer to a loaned key expression.
  /// @param payload     Pointer to an owned bytes (consumed via z_bytes_move).
  /// @param encoding    Optional MIME id bytes, LENGTH-CARRIED, or NULL for no
  /// encoding at all (canon's option field is left untouched).
  /// Never measured with strlen: the domain admits an interior
  /// NUL and canon carries one across the wire byte-exact.
  /// @param encoding_len       Length of `encoding` in bytes.
  /// @param encoding_schema    Optional schema bytes, LENGTH-CARRIED, on a channel
  /// INDEPENDENT of the MIME id. NULL means no schema (canon's
  /// setter is never called); non-NULL at length 0 means the
  /// PRESENT-BUT-EMPTY schema, a distinct third state canon
  /// renders as a bare trailing separator on a well-known id.
  /// @param encoding_schema_len  Length of `encoding_schema` in bytes.
  /// @param attachment  Optional owned bytes (consumed via z_bytes_move),
  /// or NULL for no attachment.
  /// @param timestamp   Optional pointer to 24 raw z_timestamp_t bytes, or NULL
  /// for none. Borrowed (copied into aligned storage), not
  /// consumed -- the caller retains ownership.
  /// @param congestion_control  Congestion control strategy, or -1 to leave
  /// canon's own default (DROP -- this is a push path).
  /// @param priority    Priority 1..7, or -1 to leave canon's default (data=5).
  /// @param is_express  1/0 to set express mode, or -1 to leave canon's
  /// default (false).
  /// @param allowed_destination  z_locality_t 0..2, or -1 to leave canon's
  /// default (ANY).
  /// @return 0 on success, negative on failure.
  int zd_put(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Opaque> keyexpr,
    ffi.Pointer<ffi.Opaque> payload,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
    ffi.Pointer<ffi.Opaque> attachment,
    ffi.Pointer<ffi.Uint8> timestamp,
    int congestion_control,
    int priority,
    int is_express,
    int allowed_destination,
  ) {
    return _zd_put(
      session,
      keyexpr,
      payload,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
      attachment,
      timestamp,
      congestion_control,
      priority,
      is_express,
      allowed_destination,
    );
  }

  late final _zd_putPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Int,
            ffi.Int8,
            ffi.Int,
          )
        >
      >('zd_put');
  late final _zd_put = _zd_putPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Uint8>,
          int,
          int,
          int,
          int,
        )
      >();

  /// Declares a background matching listener for a querier.
  ///
  /// Reuses the same matching status callback and drop function as publisher.
  ///
  /// @param querier    Pointer to a z_owned_querier_t (as uint8_t*).
  /// @param port       Dart NativePort for matching status callbacks.
  /// @return 0 on success, negative on failure.
  int zd_querier_declare_background_matching_listener(
    ffi.Pointer<ffi.Uint8> querier,
    int port,
  ) {
    return _zd_querier_declare_background_matching_listener(
      querier,
      port,
    );
  }

  late final _zd_querier_declare_background_matching_listenerPtr =
      _lookup<
        ffi.NativeFunction<ffi.Int8 Function(ffi.Pointer<ffi.Uint8>, ffi.Int64)>
      >('zd_querier_declare_background_matching_listener');
  late final _zd_querier_declare_background_matching_listener =
      _zd_querier_declare_background_matching_listenerPtr
          .asFunction<int Function(ffi.Pointer<ffi.Uint8>, int)>();

  /// Drops (frees) the querier.
  ///
  /// @param querier  Pointer to a z_owned_querier_t (as uint8_t*).
  void zd_querier_drop(
    ffi.Pointer<ffi.Uint8> querier,
  ) {
    return _zd_querier_drop(
      querier,
    );
  }

  late final _zd_querier_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_querier_drop',
      );
  late final _zd_querier_drop = _zd_querier_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>)>();

  /// Sends a query via a declared querier.
  ///
  /// Replies are delivered asynchronously to the Dart NativePort.
  /// Reuses the same reply callback as zd_get.
  ///
  /// @param querier     Pointer to a z_owned_querier_t (as uint8_t*).
  /// @param parameters  Optional query parameters, LENGTH-CARRIED (see zd_get).
  /// NULL with `parameters_len` 0 means none.
  /// @param parameters_len  Byte length of `parameters`; 0 when it is NULL.
  /// @param port        Dart NativePort for reply callbacks.
  /// @param payload     Optional z_owned_bytes_t* (consumed if non-NULL).
  /// @param encoding    Optional encoding string (NULL for none).
  /// @param attachment  Optional z_owned_bytes_t* (consumed if non-NULL).
  /// @return 0 on success, negative on failure.
  int zd_querier_get(
    ffi.Pointer<ffi.Uint8> querier,
    ffi.Pointer<ffi.Char> parameters,
    int parameters_len,
    int port,
    ffi.Pointer<ffi.Uint8> payload,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
    ffi.Pointer<ffi.Uint8> attachment,
    int retain_payload,
  ) {
    return _zd_querier_get(
      querier,
      parameters,
      parameters_len,
      port,
      payload,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
      attachment,
      retain_payload,
    );
  }

  late final _zd_querier_getPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Int64,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
          )
        >
      >('zd_querier_get');
  late final _zd_querier_get = _zd_querier_getPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Char>,
          int,
          int,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Uint8>,
          int,
        )
      >();

  /// Sends a query via a declared querier, with replies landing in a BOUNDED
  /// CHANNEL instead of a callback.
  ///
  /// The channel-mode sibling of zd_querier_get. Both fill canon's per-get
  /// options through one shared body, and the channel itself is built by the same
  /// shared body all three reply carriers use — so their behaviour is identical by
  /// construction rather than by three copies staying in step.
  ///
  /// @param handler_out  Caller-allocated `zd_reply_handler_sizeof(kind)` buffer.
  /// @param tee_out      Out-param for the SHIM-owned readiness-tee context,
  /// released through the shared `zd_pull_tee_drop`.
  /// @param dart_port    Dart NativePort the tee posts readiness pings to.
  /// @param parameters   Length-carried query parameters (see zd_get).
  /// @return 0 / 10 (capacity, PRE-move) / 11 (tee allocation, PRE-move) /
  /// negative from canon.
  int zd_querier_get_channel(
    ffi.Pointer<ffi.Uint8> handler_out,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> tee_out,
    int dart_port,
    ffi.Pointer<ffi.Uint8> querier,
    int kind,
    int capacity,
    ffi.Pointer<ffi.Char> parameters,
    int parameters_len,
    ffi.Pointer<ffi.Uint8> payload,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
    ffi.Pointer<ffi.Uint8> attachment,
  ) {
    return _zd_querier_get_channel(
      handler_out,
      tee_out,
      dart_port,
      querier,
      kind,
      capacity,
      parameters,
      parameters_len,
      payload,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
      attachment,
    );
  }

  late final _zd_querier_get_channelPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Int64,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int32,
            ffi.Int64,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Uint8>,
          )
        >
      >('zd_querier_get_channel');
  late final _zd_querier_get_channel = _zd_querier_get_channelPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          int,
          ffi.Pointer<ffi.Uint8>,
          int,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Uint8>,
        )
      >();

  /// Gets the current matching status of a querier.
  ///
  /// @param querier        Pointer to a z_owned_querier_t (as uint8_t*).
  /// @param matching_out   Output: 1 if matching queryables exist, 0 otherwise.
  /// @return 0 on success, negative on failure.
  int zd_querier_get_matching_status(
    ffi.Pointer<ffi.Uint8> querier,
    ffi.Pointer<ffi.Int8> matching_out,
  ) {
    return _zd_querier_get_matching_status(
      querier,
      matching_out,
    );
  }

  late final _zd_querier_get_matching_statusPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Int8>)
        >
      >('zd_querier_get_matching_status');
  late final _zd_querier_get_matching_status =
      _zd_querier_get_matching_statusPtr
          .asFunction<
            int Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Int8>)
          >();

  /// Returns the size of z_owned_querier_t in bytes.
  int zd_querier_sizeof() {
    return _zd_querier_sizeof();
  }

  late final _zd_querier_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_querier_sizeof');
  late final _zd_querier_sizeof = _zd_querier_sizeofPtr
      .asFunction<int Function()>();

  /// Takes one query out of a bounded query channel, without waiting.
  ///
  /// One shared extraction body serves both kinds; only the loan and the try_recv
  /// call are kind-dispatched.
  ///
  /// On `Z_OK` — and ONLY then — a per-query heap wrapper is claimed and its
  /// address is written to `out_query`. That wrapper is released by the shipped
  /// allocator-side `zd_query_drop`, reached through `Query.dispose()`, exactly as
  /// on the callback path. Every failure inside this function releases it again
  /// before returning, so the hand-over is a single point.
  ///
  /// Empty is not absent on the payload, attachment and encoding: canon reports an
  /// absent one as NULL, and a present-but-empty one comes back as a **non-NULL**
  /// pointer at length 0. Discriminate on the pointer. The encoding is
  /// LENGTH-CARRIED like the key expression and the parameters beside it: read
  /// exactly `*out_encoding_len` bytes, never to the first NUL.
  ///
  /// Every `out_*` pointer buffer is malloc'd HERE and freed by the CALLER.
  ///
  /// @return 0   a query was taken.
  /// 1   `Z_CHANNEL_DISCONNECTED` — the producer is gone (undeclared, or
  /// its session closed). Terminal and sticky.
  /// 2   `Z_CHANNEL_NODATA` — alive, buffer empty right now.
  /// -1  a remote-length-driven allocation could not be satisfied;
  /// everything already claimed has been released.
  int zd_query_channel_try_recv(
    ffi.Pointer<ffi.Uint8> handler,
    int kind,
    ffi.Pointer<ffi.Int64> out_query,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_keyexpr,
    ffi.Pointer<ffi.Size> out_keyexpr_len,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_parameters,
    ffi.Pointer<ffi.Size> out_parameters_len,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_payload,
    ffi.Pointer<ffi.Size> out_payload_len,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_attachment,
    ffi.Pointer<ffi.Size> out_attachment_len,
    ffi.Pointer<ffi.Pointer<ffi.Char>> out_encoding,
    ffi.Pointer<ffi.Size> out_encoding_len,
    ffi.Pointer<ffi.Int8> out_accepts_replies,
  ) {
    return _zd_query_channel_try_recv(
      handler,
      kind,
      out_query,
      out_keyexpr,
      out_keyexpr_len,
      out_parameters,
      out_parameters_len,
      out_payload,
      out_payload_len,
      out_attachment,
      out_attachment_len,
      out_encoding,
      out_encoding_len,
      out_accepts_replies,
    );
  }

  late final _zd_query_channel_try_recvPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Int32,
            ffi.Pointer<ffi.Int64>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Char>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Int8>,
          )
        >
      >('zd_query_channel_try_recv');
  late final _zd_query_channel_try_recv = _zd_query_channel_try_recvPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          int,
          ffi.Pointer<ffi.Int64>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Char>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Int8>,
        )
      >();

  /// Drops (frees) an owned query.
  ///
  /// @param query  Pointer to a z_owned_query_t to drop.
  void zd_query_drop(
    ffi.Pointer<ffi.Uint8> query,
  ) {
    return _zd_query_drop(
      query,
    );
  }

  late final _zd_query_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_query_drop',
      );
  late final _zd_query_drop = _zd_query_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>)>();

  /// Drops an owned query handler, releasing any queries still buffered in it.
  ///
  /// ⚠️ `kind` MUST match the kind the handler was constructed with.
  ///
  /// This is also the answer to "who owns a query that was never recv'd": the
  /// native channel does, and this drop releases it. There is deliberately no
  /// Dart-side undelivered-set on this path — unlike the callback path, where a
  /// query parsed off the port but never delivered would have no other owner.
  void zd_query_handler_drop(
    ffi.Pointer<ffi.Uint8> handler,
    int kind,
  ) {
    return _zd_query_handler_drop(
      handler,
      kind,
    );
  }

  late final _zd_query_handler_dropPtr =
      _lookup<
        ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>, ffi.Int32)>
      >('zd_query_handler_drop');
  late final _zd_query_handler_drop = _zd_query_handler_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>, int)>();

  /// Returns the size of the owned query handler for `kind`, in bytes.
  int zd_query_handler_sizeof(
    int kind,
  ) {
    return _zd_query_handler_sizeof(
      kind,
    );
  }

  late final _zd_query_handler_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Int32 Function(ffi.Int32)>>(
        'zd_query_handler_sizeof',
      );
  late final _zd_query_handler_sizeof = _zd_query_handler_sizeofPtr
      .asFunction<int Function(int)>();

  /// Copies the payload of a query into a caller-provided buffer.
  ///
  /// @param query        Const pointer to a loaned query (as uint8_t*).
  /// @param payload_out  Pointer to a buffer to receive the payload.
  /// @param max_len      Maximum number of bytes to copy.
  /// @return Number of bytes copied, or negative on failure.
  int zd_query_payload(
    ffi.Pointer<ffi.Uint8> query,
    ffi.Pointer<ffi.Uint8> payload_out,
    int max_len,
  ) {
    return _zd_query_payload(
      query,
      payload_out,
      max_len,
    );
  }

  late final _zd_query_payloadPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int32 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int32,
          )
        >
      >('zd_query_payload');
  late final _zd_query_payload = _zd_query_payloadPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Uint8>, int)
      >();

  /// Clones a received query's payload into a caller-supplied owned slot.
  ///
  /// @param query        Pointer to the owned query handle Dart holds.
  /// @param dst          Caller-supplied slot of `zd_bytes_sizeof()` bytes,
  /// filled with an owned `z_owned_bytes_t` when a payload
  /// is present. Untouched when absent.
  /// @param has_payload  Out-param: 1 when a payload was present and `dst` was
  /// filled, 0 when the query carried none.
  ///
  /// No return code, and that is a result rather than an omission: `z_bytes_clone`
  /// returns `void` and cannot fail, and the slot is Dart-allocated, so there is
  /// nothing to report. Presence rides the out-param instead. Codes 1 and 2 stay
  /// reserved repo-wide for canon's channel states and 10-13 stay taken.
  ///
  /// ⛔ OWNERSHIP: a present payload's clone belongs to Dart, which must release
  /// it, and it OUTLIVES the query — `zd_query_drop` does not invalidate it.
  void zd_query_payload_clone(
    ffi.Pointer<ffi.Uint8> query,
    ffi.Pointer<ffi.Uint8> dst,
    ffi.Pointer<ffi.Int32> has_payload,
  ) {
    return _zd_query_payload_clone(
      query,
      dst,
      has_payload,
    );
  }

  late final _zd_query_payload_clonePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Int32>,
          )
        >
      >('zd_query_payload_clone');
  late final _zd_query_payload_clone = _zd_query_payload_clonePtr
      .asFunction<
        void Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Int32>,
        )
      >();

  /// Sends a reply to a query.
  ///
  /// @param query        Const pointer to a loaned query (as uint8_t*).
  /// @param key_expr     Null-terminated key expression string.
  /// @param payload      Pointer to z_owned_bytes_t (consumed via z_bytes_move).
  /// @param encoding     MIME type string (NULL = default).
  /// @param attachment   Pointer to z_owned_bytes_t (NULL = no attachment).
  /// Consumed via z_bytes_move if non-NULL.
  /// @param timestamp    Pointer to the raw 24-byte z_timestamp_t image
  /// (NULL = no timestamp). BORROWED, not consumed: the
  /// bytes are memcpy'd into 8-byte-aligned stack storage
  /// and opts.timestamp points at it for the call.
  /// @param is_express   1/0 to set express mode, or -1 to leave canon's
  /// default (false). is_express is the ONLY live QoS field
  /// on the reply path: canon deprecates and ignores
  /// congestion_control and priority here, and the C++ peer
  /// does not copy them, so neither is bound.
  /// @return 0 on success, negative on failure.
  int zd_query_reply(
    ffi.Pointer<ffi.Uint8> query,
    ffi.Pointer<ffi.Opaque> key_expr,
    ffi.Pointer<ffi.Uint8> payload,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
    ffi.Pointer<ffi.Uint8> attachment,
    ffi.Pointer<ffi.Uint8> timestamp,
    int is_express,
  ) {
    return _zd_query_reply(
      query,
      key_expr,
      payload,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
      attachment,
      timestamp,
      is_express,
    );
  }

  late final _zd_query_replyPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int8,
          )
        >
      >('zd_query_reply');
  late final _zd_query_reply = _zd_query_replyPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Uint8>,
          int,
        )
      >();

  /// Sends a DELETE-kind reply to a query.
  ///
  /// Mirrors z_query_reply_del: a DELETE-kind reply carries NO payload and NO
  /// encoding, but may carry an attachment and a timestamp.
  ///
  /// @param query        Const pointer to a loaned query (as uint8_t*).
  /// @param key_expr     Null-terminated key expression string. BORROWED (a
  /// z_view_keyexpr over the string) -- not consumed.
  /// @param attachment   Pointer to z_owned_bytes_t (NULL = no attachment).
  /// Consumed via z_bytes_move if non-NULL.
  /// @param timestamp    Pointer to the raw 24-byte z_timestamp_t image
  /// (NULL = no timestamp). BORROWED, not consumed: the
  /// bytes are memcpy'd into 8-byte-aligned stack storage
  /// and opts.timestamp points at it for the call.
  /// @param is_express   1/0 to set express mode, or -1 to leave canon's
  /// default (false). Same deprecation as zd_query_reply:
  /// congestion_control and priority are not bound.
  /// @return 0 on success, negative on failure.
  int zd_query_reply_del(
    ffi.Pointer<ffi.Uint8> query,
    ffi.Pointer<ffi.Opaque> key_expr,
    ffi.Pointer<ffi.Uint8> attachment,
    ffi.Pointer<ffi.Uint8> timestamp,
    int is_express,
  ) {
    return _zd_query_reply_del(
      query,
      key_expr,
      attachment,
      timestamp,
      is_express,
    );
  }

  late final _zd_query_reply_delPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int8,
          )
        >
      >('zd_query_reply_del');
  late final _zd_query_reply_del = _zd_query_reply_delPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Uint8>,
          int,
        )
      >();

  /// Sends an error reply to a query.
  ///
  /// Mirrors z_query_reply_err: error replies carry a payload + encoding only.
  /// There is NO key expression and NO attachment (unanimous oracle carve-out:
  /// z_query_reply_err_options_t exposes only `encoding`).
  ///
  /// @param query        Const pointer to a loaned query (as uint8_t*).
  /// @param payload      Pointer to z_owned_bytes_t (consumed via z_bytes_move).
  /// @param encoding     MIME type string (NULL = default).
  /// @return 0 on success, negative on failure.
  int zd_query_reply_err(
    ffi.Pointer<ffi.Uint8> query,
    ffi.Pointer<ffi.Uint8> payload,
    ffi.Pointer<ffi.Char> encoding,
    int encoding_len,
    ffi.Pointer<ffi.Char> encoding_schema,
    int encoding_schema_len,
  ) {
    return _zd_query_reply_err(
      query,
      payload,
      encoding,
      encoding_len,
      encoding_schema,
      encoding_schema_len,
    );
  }

  late final _zd_query_reply_errPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
          )
        >
      >('zd_query_reply_err');
  late final _zd_query_reply_err = _zd_query_reply_errPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Char>,
          int,
          ffi.Pointer<ffi.Char>,
          int,
        )
      >();

  /// Returns the size of z_owned_query_t in bytes.
  int zd_query_sizeof() {
    return _zd_query_sizeof();
  }

  late final _zd_query_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Int32 Function()>>('zd_query_sizeof');
  late final _zd_query_sizeof = _zd_query_sizeofPtr
      .asFunction<int Function()>();

  /// Drops (undeclares and frees) a queryable.
  ///
  /// @param queryable  Pointer to a z_owned_queryable_t to drop.
  void zd_queryable_drop(
    ffi.Pointer<ffi.Uint8> queryable,
  ) {
    return _zd_queryable_drop(
      queryable,
    );
  }

  late final _zd_queryable_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_queryable_drop',
      );
  late final _zd_queryable_drop = _zd_queryable_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>)>();

  /// Returns the size of z_owned_queryable_t in bytes.
  int zd_queryable_sizeof() {
    return _zd_queryable_sizeof();
  }

  late final _zd_queryable_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Int32 Function()>>('zd_queryable_sizeof');
  late final _zd_queryable_sizeof = _zd_queryable_sizeofPtr
      .asFunction<int Function()>();

  /// Takes one reply out of a bounded reply channel, without waiting.
  ///
  /// One shared extraction body serves both kinds; only the loan and the
  /// try_recv call are kind-dispatched.
  ///
  /// `out_is_ok` discriminates the two readings of the shared out-params: on an
  /// ok reply they carry the sample's key expression, payload, kind, encoding,
  /// attachment, timestamp and QoS; on an error reply the payload and encoding
  /// carry the ERROR's, and the rest are absent. `out_replier_zid` /
  /// `out_replier_eid` are filled on both branches where the unstable API is
  /// compiled in, and are absent (NULL zid) otherwise — the out-param MEANING is
  /// constant across variants, so the Dart parse is platform-invariant.
  ///
  /// Empty is not absent: a present-but-empty attachment comes back as a
  /// **non-NULL** pointer at length 0, so discriminate on the pointer. The
  /// encoding is allocated unconditionally, matching the callback path, so a
  /// zero-length rendered encoding reads as `''` on both receive surfaces rather
  /// than as `''` on one and `null` on the other. It is LENGTH-CARRIED: read
  /// exactly `*out_encoding_len` bytes.
  ///
  /// Every `out_*` pointer buffer is malloc'd HERE and freed by the CALLER.
  ///
  /// @return 0   a reply was taken (see `out_is_ok`).
  /// 1   `Z_CHANNEL_DISCONNECTED` — the query completed. Terminal and
  /// sticky; canon's own code, passed through.
  /// 2   `Z_CHANNEL_NODATA` — alive, buffer empty right now. Canon's own
  /// code, passed through.
  /// -1  a remote-length-driven allocation could not be satisfied.
  /// Everything already claimed has been released.
  int zd_reply_channel_try_recv(
    ffi.Pointer<ffi.Uint8> handler,
    int kind,
    ffi.Pointer<ffi.Int8> out_is_ok,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_keyexpr,
    ffi.Pointer<ffi.Size> out_keyexpr_len,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_payload,
    ffi.Pointer<ffi.Size> out_payload_len,
    ffi.Pointer<ffi.Int8> out_kind,
    ffi.Pointer<ffi.Pointer<ffi.Char>> out_encoding,
    ffi.Pointer<ffi.Size> out_encoding_len,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_attachment,
    ffi.Pointer<ffi.Size> out_attachment_len,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_timestamp,
    ffi.Pointer<ffi.Int8> out_priority,
    ffi.Pointer<ffi.Int8> out_congestion,
    ffi.Pointer<ffi.Int8> out_express,
    ffi.Pointer<ffi.Pointer<ffi.Uint8>> out_replier_zid,
    ffi.Pointer<ffi.Int64> out_replier_eid,
    ffi.Pointer<ffi.Uint8> retain_slot,
    ffi.Pointer<ffi.Int32> out_has_retained,
  ) {
    return _zd_reply_channel_try_recv(
      handler,
      kind,
      out_is_ok,
      out_keyexpr,
      out_keyexpr_len,
      out_payload,
      out_payload_len,
      out_kind,
      out_encoding,
      out_encoding_len,
      out_attachment,
      out_attachment_len,
      out_timestamp,
      out_priority,
      out_congestion,
      out_express,
      out_replier_zid,
      out_replier_eid,
      retain_slot,
      out_has_retained,
    );
  }

  late final _zd_reply_channel_try_recvPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Uint8>,
            ffi.Int32,
            ffi.Pointer<ffi.Int8>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Int8>,
            ffi.Pointer<ffi.Pointer<ffi.Char>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Size>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Int8>,
            ffi.Pointer<ffi.Int8>,
            ffi.Pointer<ffi.Int8>,
            ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
            ffi.Pointer<ffi.Int64>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Pointer<ffi.Int32>,
          )
        >
      >('zd_reply_channel_try_recv');
  late final _zd_reply_channel_try_recv = _zd_reply_channel_try_recvPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Uint8>,
          int,
          ffi.Pointer<ffi.Int8>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Int8>,
          ffi.Pointer<ffi.Pointer<ffi.Char>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Size>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Int8>,
          ffi.Pointer<ffi.Int8>,
          ffi.Pointer<ffi.Int8>,
          ffi.Pointer<ffi.Pointer<ffi.Uint8>>,
          ffi.Pointer<ffi.Int64>,
          ffi.Pointer<ffi.Uint8>,
          ffi.Pointer<ffi.Int32>,
        )
      >();

  /// Drops an owned reply handler.
  ///
  /// ⚠️ `kind` MUST match the kind the handler was constructed with: the two
  /// owned handler types are distinct, and releasing one through the other's
  /// entry is undefined behaviour rather than a reported error.
  void zd_reply_handler_drop(
    ffi.Pointer<ffi.Uint8> handler,
    int kind,
  ) {
    return _zd_reply_handler_drop(
      handler,
      kind,
    );
  }

  late final _zd_reply_handler_dropPtr =
      _lookup<
        ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>, ffi.Int32)>
      >('zd_reply_handler_drop');
  late final _zd_reply_handler_drop = _zd_reply_handler_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>, int)>();

  /// Returns the size of the owned reply handler for `kind`, in bytes.
  ///
  /// @param kind  0 = ring, 1 = fifo (our dispatch code, not a canon value).
  int zd_reply_handler_sizeof(
    int kind,
  ) {
    return _zd_reply_handler_sizeof(
      kind,
    );
  }

  late final _zd_reply_handler_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Int32 Function(ffi.Int32)>>(
        'zd_reply_handler_sizeof',
      );
  late final _zd_reply_handler_sizeof = _zd_reply_handler_sizeofPtr
      .asFunction<int Function(int)>();

  /// Scouts for zenoh entities on the network. Returns IMMEDIATELY.
  ///
  /// The blocking z_scout runs on a detached worker thread owned by this shim,
  /// so the calling (Dart isolate) thread is never blocked.
  ///
  /// Each discovered hello is posted to the Dart native port as a
  /// Dart_CObject array of 3 elements:
  /// [0] TypedData(Uint8, 16 bytes) -- ZID
  /// [1] Int64 -- whatami value
  /// [2] Array of String -- one element per locator, each byte-exact
  ///
  /// The null completion sentinel is posted from the hello closure's DROP
  /// callback -- canon's designated release point -- and never from this
  /// function's body.
  ///
  /// @param config      Pointer to an owned config (consumed). NULL = default config.
  /// Its content is taken into worker-owned storage before
  /// this call returns, so the caller may free its block
  /// immediately afterwards.
  /// @param dart_port   The Dart native port to post Hello messages to.
  /// @param timeout_ms  Scouting timeout in milliseconds.
  /// @param what        Bitmask of entity types to scout for (e.g., 3 = router+peer).
  /// @return 0  the worker started; exactly one completion sentinel will arrive.
  /// !0 nothing started, NO sentinel will ever be posted, and everything
  /// this call took has been released. The caller must surface this as
  /// an error rather than awaiting completion, or it will wait forever.
  /// It no longer reports z_scout's own outcome, which is only reached
  /// long after this returns.
  int zd_scout(
    ffi.Pointer<ffi.Opaque> config,
    int dart_port,
    int timeout_ms,
    int what,
  ) {
    return _zd_scout(
      config,
      dart_port,
      timeout_ms,
      what,
    );
  }

  late final _zd_scoutPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Uint64,
            ffi.Int,
          )
        >
      >('zd_scout');
  late final _zd_scout = _zd_scoutPtr
      .asFunction<int Function(ffi.Pointer<ffi.Opaque>, int, int, int)>();

  /// Drops (frees) an owned serializer.
  ///
  /// After this call the owned serializer is in gravestone state.
  ///
  /// @param ser  Pointer to a ze_owned_serializer_t to drop.
  void zd_serializer_drop(
    ffi.Pointer<ze_owned_serializer_t> ser,
  ) {
    return _zd_serializer_drop(
      ser,
    );
  }

  late final _zd_serializer_dropPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(ffi.Pointer<ze_owned_serializer_t>)
        >
      >('zd_serializer_drop');
  late final _zd_serializer_drop = _zd_serializer_dropPtr
      .asFunction<void Function(ffi.Pointer<ze_owned_serializer_t>)>();

  /// Initializes an empty serializer.
  ///
  /// @param ser  Pointer to an uninitialized ze_owned_serializer_t.
  /// @return 0 on success.
  int zd_serializer_empty(
    ffi.Pointer<ze_owned_serializer_t> ser,
  ) {
    return _zd_serializer_empty(
      ser,
    );
  }

  late final _zd_serializer_emptyPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_owned_serializer_t>)
        >
      >('zd_serializer_empty');
  late final _zd_serializer_empty = _zd_serializer_emptyPtr
      .asFunction<int Function(ffi.Pointer<ze_owned_serializer_t>)>();

  /// Finishes the serializer and produces a z_owned_bytes_t.
  ///
  /// The serializer is consumed (moved) by this call.
  ///
  /// @param ser  Pointer to a valid ze_owned_serializer_t (consumed).
  /// @param out  Receives the produced z_owned_bytes_t.
  void zd_serializer_finish(
    ffi.Pointer<ze_owned_serializer_t> ser,
    ffi.Pointer<ffi.Opaque> out,
  ) {
    return _zd_serializer_finish(
      ser,
      out,
    );
  }

  late final _zd_serializer_finishPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(
            ffi.Pointer<ze_owned_serializer_t>,
            ffi.Pointer<ffi.Opaque>,
          )
        >
      >('zd_serializer_finish');
  late final _zd_serializer_finish = _zd_serializer_finishPtr
      .asFunction<
        void Function(
          ffi.Pointer<ze_owned_serializer_t>,
          ffi.Pointer<ffi.Opaque>,
        )
      >();

  /// Obtains a mutable loaned reference to the owned serializer.
  ///
  /// @param ser  Pointer to a valid ze_owned_serializer_t.
  /// @param out  Receives the mutable loaned pointer.
  void zd_serializer_loan_mut(
    ffi.Pointer<ze_owned_serializer_t> ser,
    ffi.Pointer<ffi.Pointer<ze_loaned_serializer_t>> out,
  ) {
    return _zd_serializer_loan_mut(
      ser,
      out,
    );
  }

  late final _zd_serializer_loan_mutPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(
            ffi.Pointer<ze_owned_serializer_t>,
            ffi.Pointer<ffi.Pointer<ze_loaned_serializer_t>>,
          )
        >
      >('zd_serializer_loan_mut');
  late final _zd_serializer_loan_mut = _zd_serializer_loan_mutPtr
      .asFunction<
        void Function(
          ffi.Pointer<ze_owned_serializer_t>,
          ffi.Pointer<ffi.Pointer<ze_loaned_serializer_t>>,
        )
      >();

  /// Serializes a bool value.
  int zd_serializer_serialize_bool(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    bool val,
  ) {
    return _zd_serializer_serialize_bool(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_boolPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Bool)
        >
      >('zd_serializer_serialize_bool');
  late final _zd_serializer_serialize_bool = _zd_serializer_serialize_boolPtr
      .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, bool)>();

  /// Serializes a byte buffer of the given length.
  int zd_serializer_serialize_buf(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    ffi.Pointer<ffi.Uint8> data,
    int len,
  ) {
    return _zd_serializer_serialize_buf(
      ser,
      data,
      len,
    );
  }

  late final _zd_serializer_serialize_bufPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_loaned_serializer_t>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Size,
          )
        >
      >('zd_serializer_serialize_buf');
  late final _zd_serializer_serialize_buf = _zd_serializer_serialize_bufPtr
      .asFunction<
        int Function(
          ffi.Pointer<ze_loaned_serializer_t>,
          ffi.Pointer<ffi.Uint8>,
          int,
        )
      >();

  /// Serializes a double value.
  int zd_serializer_serialize_double(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    double val,
  ) {
    return _zd_serializer_serialize_double(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_doublePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Double)
        >
      >('zd_serializer_serialize_double');
  late final _zd_serializer_serialize_double =
      _zd_serializer_serialize_doublePtr
          .asFunction<
            int Function(ffi.Pointer<ze_loaned_serializer_t>, double)
          >();

  /// Serializes a float value.
  int zd_serializer_serialize_float(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    double val,
  ) {
    return _zd_serializer_serialize_float(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_floatPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Float)
        >
      >('zd_serializer_serialize_float');
  late final _zd_serializer_serialize_float = _zd_serializer_serialize_floatPtr
      .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, double)>();

  /// Serializes an int16_t value.
  int zd_serializer_serialize_int16(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    int val,
  ) {
    return _zd_serializer_serialize_int16(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_int16Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Int16)
        >
      >('zd_serializer_serialize_int16');
  late final _zd_serializer_serialize_int16 = _zd_serializer_serialize_int16Ptr
      .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, int)>();

  /// Serializes an int32_t value.
  int zd_serializer_serialize_int32(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    int val,
  ) {
    return _zd_serializer_serialize_int32(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_int32Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Int32)
        >
      >('zd_serializer_serialize_int32');
  late final _zd_serializer_serialize_int32 = _zd_serializer_serialize_int32Ptr
      .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, int)>();

  /// Serializes an int64_t value.
  int zd_serializer_serialize_int64(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    int val,
  ) {
    return _zd_serializer_serialize_int64(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_int64Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Int64)
        >
      >('zd_serializer_serialize_int64');
  late final _zd_serializer_serialize_int64 = _zd_serializer_serialize_int64Ptr
      .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, int)>();

  /// Serializes an int8_t value.
  int zd_serializer_serialize_int8(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    int val,
  ) {
    return _zd_serializer_serialize_int8(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_int8Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Int8)
        >
      >('zd_serializer_serialize_int8');
  late final _zd_serializer_serialize_int8 = _zd_serializer_serialize_int8Ptr
      .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, int)>();

  /// Serializes a sequence length header for a subsequent sequence of elements.
  int zd_serializer_serialize_sequence_length(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    int len,
  ) {
    return _zd_serializer_serialize_sequence_length(
      ser,
      len,
    );
  }

  late final _zd_serializer_serialize_sequence_lengthPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Size)
        >
      >('zd_serializer_serialize_sequence_length');
  late final _zd_serializer_serialize_sequence_length =
      _zd_serializer_serialize_sequence_lengthPtr
          .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, int)>();

  /// Serializes a length-delimited UTF-8 string (embedded NULs preserved).
  int zd_serializer_serialize_string(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    ffi.Pointer<ffi.Uint8> val,
    int len,
  ) {
    return _zd_serializer_serialize_string(
      ser,
      val,
      len,
    );
  }

  late final _zd_serializer_serialize_stringPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ze_loaned_serializer_t>,
            ffi.Pointer<ffi.Uint8>,
            ffi.Size,
          )
        >
      >('zd_serializer_serialize_string');
  late final _zd_serializer_serialize_string =
      _zd_serializer_serialize_stringPtr
          .asFunction<
            int Function(
              ffi.Pointer<ze_loaned_serializer_t>,
              ffi.Pointer<ffi.Uint8>,
              int,
            )
          >();

  /// Serializes a uint16_t value.
  int zd_serializer_serialize_uint16(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    int val,
  ) {
    return _zd_serializer_serialize_uint16(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_uint16Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Uint16)
        >
      >('zd_serializer_serialize_uint16');
  late final _zd_serializer_serialize_uint16 =
      _zd_serializer_serialize_uint16Ptr
          .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, int)>();

  /// Serializes a uint32_t value.
  int zd_serializer_serialize_uint32(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    int val,
  ) {
    return _zd_serializer_serialize_uint32(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_uint32Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Uint32)
        >
      >('zd_serializer_serialize_uint32');
  late final _zd_serializer_serialize_uint32 =
      _zd_serializer_serialize_uint32Ptr
          .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, int)>();

  /// Serializes a uint64_t value.
  int zd_serializer_serialize_uint64(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    int val,
  ) {
    return _zd_serializer_serialize_uint64(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_uint64Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Uint64)
        >
      >('zd_serializer_serialize_uint64');
  late final _zd_serializer_serialize_uint64 =
      _zd_serializer_serialize_uint64Ptr
          .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, int)>();

  /// Serializes a uint8_t value.
  int zd_serializer_serialize_uint8(
    ffi.Pointer<ze_loaned_serializer_t> ser,
    int val,
  ) {
    return _zd_serializer_serialize_uint8(
      ser,
      val,
    );
  }

  late final _zd_serializer_serialize_uint8Ptr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Pointer<ze_loaned_serializer_t>, ffi.Uint8)
        >
      >('zd_serializer_serialize_uint8');
  late final _zd_serializer_serialize_uint8 = _zd_serializer_serialize_uint8Ptr
      .asFunction<int Function(ffi.Pointer<ze_loaned_serializer_t>, int)>();

  /// Returns the size of ze_owned_serializer_t in bytes.
  int zd_serializer_sizeof() {
    return _zd_serializer_sizeof();
  }

  late final _zd_serializer_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_serializer_sizeof');
  late final _zd_serializer_sizeof = _zd_serializer_sizeofPtr
      .asFunction<int Function()>();

  /// Closes, drops and FREES a session block that zd_open_session_async
  /// allocated.
  ///
  /// Takes uint8_t* rather than z_owned_session_t* deliberately, and the
  /// asymmetry with zd_close_session above is the point: that one takes a typed
  /// pointer because DART allocates the slot. This block is SHIM-owned -- its
  /// address crosses to Dart as a plain kInt64 and returns as an opaque block
  /// pointer, exactly the shape zd_query_drop(uint8_t* query) already uses in
  /// this header.
  ///
  /// Allocator-side frees: the shim malloc'd it, so the shim frees it.
  ///
  /// @param session  Address delivered in element [1] of a successful post.
  void zd_session_close_drop(
    ffi.Pointer<ffi.Uint8> session,
  ) {
    return _zd_session_close_drop(
      session,
    );
  }

  late final _zd_session_close_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_session_close_drop',
      );
  late final _zd_session_close_drop = _zd_session_close_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>)>();

  /// Reports whether the session has been closed.
  ///
  /// @param session  Const pointer to a loaned (live) session.
  /// @return 1 if the session is closed, 0 if open.
  int zd_session_is_closed(
    ffi.Pointer<ffi.Opaque> session,
  ) {
    return _zd_session_is_closed(
      session,
    );
  }

  late final _zd_session_is_closedPtr =
      _lookup<ffi.NativeFunction<ffi.Int8 Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_session_is_closed',
      );
  late final _zd_session_is_closed = _zd_session_is_closedPtr
      .asFunction<int Function(ffi.Pointer<ffi.Opaque>)>();

  /// Obtains a const loaned reference to the session.
  ///
  /// @param session  Pointer to a valid z_owned_session_t.
  /// @return Const pointer to the loaned session.
  ffi.Pointer<ffi.Opaque> zd_session_loan(
    ffi.Pointer<ffi.Opaque> session,
  ) {
    return _zd_session_loan(
      session,
    );
  }

  late final _zd_session_loanPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_session_loan');
  late final _zd_session_loan = _zd_session_loanPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the size of z_owned_session_t in bytes.
  ///
  /// RETAINED with no Dart caller, and this is the ground rather than an
  /// oversight. The synchronous pair below -- zd_open_session / zd_close_session
  /// -- operates on a CALLER-ALLOCATED slot, and this is the only function that
  /// can size that slot. The RETAINED note on zd_close_session names a non-Dart
  /// embedder or a direct-bindings test as its stated use; neither can call the
  /// pair without this. Removing it would orphan the KEEP it sits beside.
  ///
  /// (Corrected: this said "Used by Dart to allocate the correct amount of
  /// native memory for opaque zenoh types." Dart stopped calling it when
  /// Session.open moved to the async pair, which owns its block end to end, so
  /// the sentence was false in the present tense -- and, being a statement of
  /// purpose rather than of retention, it left the symbol reading as dead with
  /// no reason beside it.)
  int zd_session_sizeof() {
    return _zd_session_sizeof();
  }

  late final _zd_session_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_session_sizeof');
  late final _zd_session_sizeof = _zd_session_sizeofPtr
      .asFunction<int Function()>();

  /// Releases the caller's reference to an async request's box.
  ///
  /// Exactly once per handle returned by `zd_shm_provider_alloc_async`. The
  /// deferred provider drop, if one was handed over, happens when the LAST
  /// reference goes -- whichever party releases it.
  void zd_shm_async_box_release(
    int box_handle,
  ) {
    return _zd_shm_async_box_release(
      box_handle,
    );
  }

  late final _zd_shm_async_box_releasePtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Int64)>>(
        'zd_shm_async_box_release',
      );
  late final _zd_shm_async_box_release = _zd_shm_async_box_releasePtr
      .asFunction<void Function(int)>();

  /// Takes the buffer a completed async allocation posted.
  ///
  /// Moves the content of the shim-owned block named by @p buf_handle into the
  /// caller's @p out slot and releases the shim's block. Call it EXACTLY ONCE per
  /// posted non-zero handle: the handle is dead afterwards.
  ///
  /// ⚠️ The buffer is moved out of canon's result INSIDE the result callback,
  /// not here, because canon may free its context before Dart reads the post.
  /// This entry only completes the hand-off.
  void zd_shm_async_take(
    int buf_handle,
    ffi.Pointer<ffi.Opaque> out,
  ) {
    return _zd_shm_async_take(
      buf_handle,
      out,
    );
  }

  late final _zd_shm_async_takePtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(ffi.Int64, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_shm_async_take');
  late final _zd_shm_async_take = _zd_shm_async_takePtr
      .asFunction<void Function(int, ffi.Pointer<ffi.Opaque>)>();

  /// Returns a mutable pointer to the SHM buffer data.
  ffi.Pointer<ffi.Uint8> zd_shm_mut_data_mut(
    ffi.Pointer<ffi.Opaque> buf,
  ) {
    return _zd_shm_mut_data_mut(
      buf,
    );
  }

  late final _zd_shm_mut_data_mutPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Uint8> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_shm_mut_data_mut');
  late final _zd_shm_mut_data_mut = _zd_shm_mut_data_mutPtr
      .asFunction<ffi.Pointer<ffi.Uint8> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Drops (frees) a mutable SHM buffer.
  void zd_shm_mut_drop(
    ffi.Pointer<ffi.Opaque> buf,
  ) {
    return _zd_shm_mut_drop(
      buf,
    );
  }

  late final _zd_shm_mut_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_shm_mut_drop',
      );
  late final _zd_shm_mut_drop = _zd_shm_mut_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the length of the SHM buffer.
  int zd_shm_mut_len(
    ffi.Pointer<ffi.Opaque> buf,
  ) {
    return _zd_shm_mut_len(
      buf,
    );
  }

  late final _zd_shm_mut_lenPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_shm_mut_len',
      );
  late final _zd_shm_mut_len = _zd_shm_mut_lenPtr
      .asFunction<int Function(ffi.Pointer<ffi.Opaque>)>();

  /// Obtains a mutable loaned reference to the SHM buffer.
  ffi.Pointer<ffi.Opaque> zd_shm_mut_loan_mut(
    ffi.Pointer<ffi.Opaque> buf,
  ) {
    return _zd_shm_mut_loan_mut(
      buf,
    );
  }

  late final _zd_shm_mut_loan_mutPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_shm_mut_loan_mut');
  late final _zd_shm_mut_loan_mut = _zd_shm_mut_loan_mutPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the size of z_owned_shm_mut_t in bytes.
  int zd_shm_mut_sizeof() {
    return _zd_shm_mut_sizeof();
  }

  late final _zd_shm_mut_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_shm_mut_sizeof');
  late final _zd_shm_mut_sizeof = _zd_shm_mut_sizeofPtr
      .asFunction<int Function()>();

  /// Allocates a mutable SHM buffer, dispatching over canon's ten sync entries.
  ///
  /// One wrapper, not ten: canon's ten synchronous provider-allocation entries
  /// differ only in WHICH symbol is called -- the result carriage, the
  /// status branch, and the buffer move are identical in all ten. A shared body
  /// makes the discriminant-dropping defect this replaced (an invented -1,
  /// copy-pasted into two wrappers) structurally impossible to reintroduce in
  /// one place and not the other.
  ///
  /// **Canon entries reached** (`strategy` x `alignment_pow`), the G4.4 parity
  /// checklist for this entry:
  ///
  /// strategy 0 -> `z_shm_provider_alloc`                     / `..._aligned`
  /// strategy 1 -> `z_shm_provider_alloc_gc`                  / `..._aligned`
  /// strategy 2 -> `z_shm_provider_alloc_gc_defrag`           / `..._aligned`
  /// strategy 3 -> `z_shm_provider_alloc_gc_defrag_dealloc`   / `..._aligned`
  /// strategy 4 -> `z_shm_provider_alloc_gc_defrag_blocking`  / `..._aligned`
  ///
  /// **What canon exposes here that this entry does not:** the two `_async`
  /// siblings (`z_shm_provider_alloc_gc_defrag_async`, `..._aligned_async` --
  /// the only two that return a `z_result_t`, and the only two carved from this
  /// dispatch); the precomputed-layout family (`z_shm_provider_alloc_layout*`
  /// and the `z_precomputed_layout_alloc*` entries, which out-param the 2-slot
  /// `z_buf_alloc_result_t`); and the deprecated `z_alloc_layout_*` family.
  /// Canon's own alignment struct carries exactly one field (`pow`), so nothing
  /// of the alignment surface is dropped.
  ///
  /// @param provider       Const pointer to a loaned SHM provider.
  /// @param buf            Caller-allocated `zd_shm_mut_sizeof()` slot. Written
  /// with the owned buffer **iff** `out->status == 0`;
  /// left untouched on every other path, including rc 10.
  /// On a non-OK status canon's own `buf` slot is a null
  /// gravestone owning nothing, so there is nothing to
  /// drop and the caller's slot needs only to be freed.
  /// @param size           Allocation size, carried full width. Domain: >= 0.
  /// @param strategy       0 plain, 1 gc, 2 gc+defrag, 3 gc+defrag+dealloc,
  /// 4 gc+defrag+blocking. Any other value is rejected.
  /// @param alignment_pow  **-1** selects canon's unaligned entry; **0..255**
  /// selects the `_aligned` sibling with
  /// `z_alloc_alignment_t{.pow = alignment_pow}`. Canon's
  /// field is a `uint8_t`, so the domain is its full
  /// width; canon's unaligned entries implement themselves
  /// as pow 0, which is why -1 rather than 0 is the
  /// "unspecified" sentinel.
  /// @param out            Caller-allocated result struct. Written **iff** the
  /// return code is 0; untouched on rc 10.
  /// @return 0 -- a canon call ran; read `*out` for what it reported.
  /// **10** -- the shim rejected an argument. **Nothing ran**: no canon
  /// call was made, and `*out` and `*buf` are left exactly as the
  /// caller left them. Rendered Dart-side as `ArgumentError`, the same
  /// meaning code 10 carries on the declare-channel entries -- one
  /// meaning per positive code, repo-wide, so a reader never has to
  /// check the mapping per site. Unreachable from the public Dart API,
  /// whose own guards reject all three triggers (`size`,
  /// `alignment_pow`, `strategy`) before the call; it is a structural
  /// backstop driven directly at the seam by its own test cell.
  ///
  /// ⚠️ **The whole return-code space here is shim-owned, and that is safe by
  /// construction rather than by convention:** canon's ten sync entries return
  /// `void`. There is no canon code on this channel to collide with, in either
  /// sign -- so nothing here needs to be "fixed" into the negative space later.
  /// Canon's own outcomes travel in `*out`, where they keep canon's own values.
  int zd_shm_provider_alloc(
    ffi.Pointer<ffi.Opaque> provider,
    ffi.Pointer<ffi.Opaque> buf,
    int size,
    int strategy,
    int alignment_pow,
    ffi.Pointer<zd_shm_alloc_result_t> out,
  ) {
    return _zd_shm_provider_alloc(
      provider,
      buf,
      size,
      strategy,
      alignment_pow,
      out,
    );
  }

  late final _zd_shm_provider_allocPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Int32,
            ffi.Int32,
            ffi.Pointer<zd_shm_alloc_result_t>,
          )
        >
      >('zd_shm_provider_alloc');
  late final _zd_shm_provider_alloc = _zd_shm_provider_allocPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          ffi.Pointer<ffi.Opaque>,
          int,
          int,
          int,
          ffi.Pointer<zd_shm_alloc_result_t>,
        )
      >();

  /// Starts an ASYNCHRONOUS allocation and returns immediately.
  ///
  /// Canon's `z_shm_provider_alloc_gc_defrag_async`. Its synchronous sibling
  /// `..._blocking` waits inside the call, which in Dart parks the calling
  /// ISOLATE with no await point, no timeout and no cancellation -- and on a
  /// request the pool can never satisfy it never returns at all. This entry hands
  /// the same request to canon's background machinery and returns; the outcome
  /// arrives as ONE post on @p dart_port.
  ///
  /// THE RETURN MEANS "DID IT START", NOT "DID IT SUCCEED":
  ///
  /// 0                     the request started; EXACTLY ONE post will arrive,
  /// IF canon ever completes it -- see the hazard below
  /// 10  ZD_SHM_EARG       an argument was rejected; NOTHING started
  /// 12  ZD_SHM_EALLOC     a heap allocation failed; NOTHING started
  ///
  /// ⛔⛔ THE SYNC FAMILY'S JUSTIFICATION FOR OWNING THE WHOLE CODE SPACE DOES
  /// NOT CARRY OVER HERE, and this is the sharp edge of this entry.
  /// `zd_shm_provider_alloc` may own its entire return domain because canon's ten
  /// synchronous entries return `void` -- there is no canon code on that channel
  /// to collide with, in either sign. **This entry returns `z_result_t`, so canon
  /// occupies the NEGATIVE channel.** Every shim-owned code here is therefore
  /// POSITIVE, precisely so a shim code cannot masquerade as a canon one.
  ///
  /// 10 keeps the meaning it carries everywhere in this shim. 12 keeps the
  /// meaning it carries on the open channel. ⛔ **11 is DELIBERATELY NOT REUSED**:
  /// `reply_channel_alloc_harness.dart` asserts `code == 11` from
  /// `zd_get_channel` under the malloc injector, and reusing it here would let a
  /// green report come from the wrong site.
  ///
  /// ⚠️ MEASURED, and the shim is designed around it rather than hoping otherwise
  /// (`development/research/probes-ci-shm-20260902/`):
  ///
  /// * canon's own rc is **ZERO for every size in the domain, swept to
  /// SIZE_MAX**, on the provider this binding constructs. A canon rejection
  /// is unreachable here, so a non-zero return is always the shim's own --
  /// minted BEFORE the context is handed over, which is what makes the
  /// ownership question decidable.
  /// * a REFUSAL arrives through the callback with rc 0. Canon's outcomes
  /// travel in the result, exactly as they do for the sync ten; the return
  /// code is not an outcome channel.
  /// * ⛔ a request the pool cannot satisfy is ACCEPTED and then **nothing ever
  /// fires** -- no result, and no context release either. That context and
  /// its segment are leaked for the life of the process, and canon ships no
  /// cancel entry. Not a hypothetical: even the NOMINAL pool size never
  /// completes, because the pool carries its own overhead.
  /// * ⛔ dropping the provider while a request is pending SEGFAULTS, 3/3,
  /// against a clean control. Nothing in this entry protects against that;
  /// making it unreachable is the deferred-drop work.
  ///
  /// POST SHAPE -- one array of four int64:
  /// [0] canon's status verbatim: 0 OK, 1 ALLOC_ERROR, 2 LAYOUT_ERROR
  /// [1] canon's `z_alloc_error_t` iff status == 1, else -1
  /// [2] canon's `z_layout_error_t` iff status == 2, else -1
  /// [3] on status 0, a SHIM-OWNED handle to take the buffer with; else 0
  ///
  /// ⚠️ Status 0 with handle 0 means the shim could not allocate a block to carry
  /// the buffer and released the CHUNK rather than leaking it. Canon's OK arm
  /// always carries a buffer, so the pair is unambiguous and needs no code.
  ///
  /// @param provider   Const pointer to a loaned SHM provider.
  /// @param size       Allocation size, carried full width. Domain: >= 0.
  /// @param dart_port  Native port the single result is posted to.
  /// @param out_box    Written with a handle to the shim's reference-counted
  /// box on a 0 return, and with 0 otherwise. The caller owns
  /// ONE reference to it and must release it exactly once,
  /// with `zd_shm_async_box_release`. That handle is what
  /// makes a DEFERRED provider drop possible.
  int zd_shm_provider_alloc_async(
    ffi.Pointer<ffi.Opaque> provider,
    int size,
    int dart_port,
    ffi.Pointer<ffi.Int64> out_box,
  ) {
    return _zd_shm_provider_alloc_async(
      provider,
      size,
      dart_port,
      out_box,
    );
  }

  late final _zd_shm_provider_alloc_asyncPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Int64,
            ffi.Int64,
            ffi.Pointer<ffi.Int64>,
          )
        >
      >('zd_shm_provider_alloc_async');
  late final _zd_shm_provider_alloc_async = _zd_shm_provider_alloc_asyncPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Opaque>, int, int, ffi.Pointer<ffi.Int64>)
      >();

  /// Hands the provider slot to the shim, to be dropped when canon is finished.
  ///
  /// ⛔ WHY THIS EXISTS: dropping a provider while canon still holds a request
  /// against it FAULTS on a zenoh thread -- measured 3/3 against a clean control
  /// performing the identical drop on the identical state with no request
  /// started. The drop returns first and the fault lands afterwards, so it
  /// cannot be relied on to surface in testing.
  ///
  /// ⚠️ **OWNERSHIP MOVES on a 1 return.** The shim then drops the provider AND
  /// frees the slot block, because the caller cannot know when that becomes
  /// safe. On a 0 return nothing moved and the caller proceeds as usual.
  ///
  /// @return 1  taken; do NOT drop and do NOT free the slot
  /// 0  ONLY when an argument was null. Nothing was taken.
  ///
  /// ⚠️ **CORRECTED 2026-09-03 at the merge gate.** This arm was documented as
  /// *"canon already finished; drop and free as usual"*, describing a gate on
  /// `completed` that the implementation does not have. The slot is taken
  /// **unconditionally**, because the hand-over happens when a request STARTS
  /// and canon can satisfy a small request from a fresh pool INSIDE the call
  /// that starts it — a gate there refuses the slot and nothing ever drops the
  /// provider. A caller must therefore treat a 1 as the normal answer and 0 as
  /// a programming error, not as a live "canon got there first" branch.
  int zd_shm_provider_defer_drop(
    int box_handle,
    ffi.Pointer<ffi.Opaque> provider,
  ) {
    return _zd_shm_provider_defer_drop(
      box_handle,
      provider,
    );
  }

  late final _zd_shm_provider_defer_dropPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int8 Function(ffi.Int64, ffi.Pointer<ffi.Opaque>)
        >
      >('zd_shm_provider_defer_drop');
  late final _zd_shm_provider_defer_drop = _zd_shm_provider_defer_dropPtr
      .asFunction<int Function(int, ffi.Pointer<ffi.Opaque>)>();

  /// Performs manual memory defragmentation on the provider.
  ///
  /// Straight pass-through of `z_shm_provider_defragment`, whose own words are:
  /// *"Perform memory defragmentation. The real operations taken depend on the
  /// provider's backend allocator implementation."*
  ///
  /// @param provider  Const pointer to a loaned SHM provider.
  /// @return Canon's `size_t`, verbatim. **Canon documents no meaning for it**
  /// -- not bytes reclaimed, not a count -- so it is carried rather than
  /// interpreted, and no caller should assert a magnitude.
  int zd_shm_provider_defragment(
    ffi.Pointer<ffi.Opaque> provider,
  ) {
    return _zd_shm_provider_defragment(
      provider,
    );
  }

  late final _zd_shm_provider_defragmentPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_shm_provider_defragment',
      );
  late final _zd_shm_provider_defragment = _zd_shm_provider_defragmentPtr
      .asFunction<int Function(ffi.Pointer<ffi.Opaque>)>();

  /// Drops (frees) the SHM provider.
  void zd_shm_provider_drop(
    ffi.Pointer<ffi.Opaque> provider,
  ) {
    return _zd_shm_provider_drop(
      provider,
    );
  }

  late final _zd_shm_provider_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_shm_provider_drop',
      );
  late final _zd_shm_provider_drop = _zd_shm_provider_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Performs manual garbage collection on the provider.
  ///
  /// Straight pass-through of `z_shm_provider_garbage_collect`, whose own words
  /// are: *"Perform memory garbage collection and reclaim all dereferenced SHM
  /// buffers."*
  ///
  /// @param provider  Const pointer to a loaned SHM provider.
  /// @return Canon's `size_t`, verbatim, with the same no-documented-meaning
  /// caveat as `zd_shm_provider_defragment`.
  int zd_shm_provider_garbage_collect(
    ffi.Pointer<ffi.Opaque> provider,
  ) {
    return _zd_shm_provider_garbage_collect(
      provider,
    );
  }

  late final _zd_shm_provider_garbage_collectPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_shm_provider_garbage_collect',
      );
  late final _zd_shm_provider_garbage_collect =
      _zd_shm_provider_garbage_collectPtr
          .asFunction<int Function(ffi.Pointer<ffi.Opaque>)>();

  /// Obtains a const loaned reference to the SHM provider.
  ffi.Pointer<ffi.Opaque> zd_shm_provider_loan(
    ffi.Pointer<ffi.Opaque> provider,
  ) {
    return _zd_shm_provider_loan(
      provider,
    );
  }

  late final _zd_shm_provider_loanPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_shm_provider_loan');
  late final _zd_shm_provider_loan = _zd_shm_provider_loanPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Creates a default SHM provider with the given total size.
  ///
  /// Wraps z_shm_provider_default_new(). Detail-carrying: on failure canon's
  /// own explanation of THIS call is written into the caller's err_buf, so an
  /// enriched ZenohException can surface it. See the err_buf/err_cap/err_len
  /// contract above.
  ///
  /// ⭐ WHY THIS ENTRY CARRIES DETAIL. Canon returns Z_EINVAL for every
  /// rejection class it has here -- a pool too small to back the Talc
  /// allocator, a pool the host's locked-memory budget cannot satisfy, and a
  /// pool whose element count a 32-bit ElemIndex cannot address. Three rules,
  /// one code. Canon separates them in zc_get_last_error's text and nowhere
  /// else, so without this triple a caller cannot be told which rule they broke.
  ///
  /// ⚠️ NO REDACTION IS APPLIED, and the text is canon's verbatim: the
  /// over-budget message carries an absolute path into canon's own sources.
  /// The pool size is the caller's own number, so nothing of the caller's
  /// crosses back that they did not already have -- which is why this widening
  /// does not reopen the config-echo question the contract above records.
  ///
  /// @param provider    Pointer to an uninitialized z_owned_shm_provider_t.
  /// @param total_size  Total size of the SHM pool in bytes.
  /// @param err_buf  Caller storage for this call's detail; NULL to decline.
  /// @param err_cap  Capacity of err_buf in bytes.
  /// @param err_len  Out: bytes written to err_buf; always set, 0 when none.
  /// @return 0 on success, negative on failure.
  int zd_shm_provider_new(
    ffi.Pointer<ffi.Opaque> provider,
    int total_size,
    ffi.Pointer<ffi.Uint8> err_buf,
    int err_cap,
    ffi.Pointer<ffi.Int> err_len,
  ) {
    return _zd_shm_provider_new(
      provider,
      total_size,
      err_buf,
      err_cap,
      err_len,
    );
  }

  late final _zd_shm_provider_newPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Size,
            ffi.Pointer<ffi.Uint8>,
            ffi.Int,
            ffi.Pointer<ffi.Int>,
          )
        >
      >('zd_shm_provider_new');
  late final _zd_shm_provider_new = _zd_shm_provider_newPtr
      .asFunction<
        int Function(
          ffi.Pointer<ffi.Opaque>,
          int,
          ffi.Pointer<ffi.Uint8>,
          int,
          ffi.Pointer<ffi.Int>,
        )
      >();

  /// Returns the size of z_owned_shm_provider_t in bytes.
  int zd_shm_provider_sizeof() {
    return _zd_shm_provider_sizeof();
  }

  late final _zd_shm_provider_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>(
        'zd_shm_provider_sizeof',
      );
  late final _zd_shm_provider_sizeof = _zd_shm_provider_sizeofPtr
      .asFunction<int Function()>();

  /// Takes the provider slot back out of a box without dropping it.
  ///
  /// Used when a second request replaces the first: the slot must move to the
  /// new box, or releasing the old one would drop a live provider.
  ///
  /// @return 1 if the slot was taken back, 0 if the box did not hold it.
  int zd_shm_provider_undefer_drop(
    int box_handle,
  ) {
    return _zd_shm_provider_undefer_drop(
      box_handle,
    );
  }

  late final _zd_shm_provider_undefer_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Int8 Function(ffi.Int64)>>(
        'zd_shm_provider_undefer_drop',
      );
  late final _zd_shm_provider_undefer_drop = _zd_shm_provider_undefer_dropPtr
      .asFunction<int Function(int)>();

  /// Returns a pointer to the data of a loaned string.
  ///
  /// The returned pointer is NOT guaranteed to be null-terminated.
  ///
  /// @param str  Const pointer to a loaned string.
  /// @return Pointer to the string data.
  ffi.Pointer<ffi.Char> zd_string_data(
    ffi.Pointer<ffi.Opaque> str,
  ) {
    return _zd_string_data(
      str,
    );
  }

  late final _zd_string_dataPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Char> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_string_data');
  late final _zd_string_data = _zd_string_dataPtr
      .asFunction<ffi.Pointer<ffi.Char> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Drops (frees) an owned string.
  ///
  /// After this call the owned string is in gravestone state.
  /// A second drop is a safe no-op.
  ///
  /// @param str  Pointer to a z_owned_string_t to drop.
  void zd_string_drop(
    ffi.Pointer<ffi.Opaque> str,
  ) {
    return _zd_string_drop(
      str,
    );
  }

  late final _zd_string_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_string_drop',
      );
  late final _zd_string_drop = _zd_string_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the length of a loaned string (in bytes, NOT including any terminator).
  ///
  /// @param str  Const pointer to a loaned string.
  /// @return Length of the string data in bytes.
  int zd_string_len(
    ffi.Pointer<ffi.Opaque> str,
  ) {
    return _zd_string_len(
      str,
    );
  }

  late final _zd_string_lenPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_string_len',
      );
  late final _zd_string_len = _zd_string_lenPtr
      .asFunction<int Function(ffi.Pointer<ffi.Opaque>)>();

  /// Obtains a const loaned reference to the owned string.
  ///
  /// @param str  Pointer to a valid z_owned_string_t.
  /// @return Const pointer to the loaned string.
  ffi.Pointer<ffi.Opaque> zd_string_loan(
    ffi.Pointer<ffi.Opaque> str,
  ) {
    return _zd_string_loan(
      str,
    );
  }

  late final _zd_string_loanPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_string_loan');
  late final _zd_string_loan = _zd_string_loanPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the size of z_owned_string_t in bytes.
  ///
  /// Used by Dart to allocate the correct amount of native memory
  /// for opaque zenoh types.
  int zd_string_sizeof() {
    return _zd_string_sizeof();
  }

  late final _zd_string_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_string_sizeof');
  late final _zd_string_sizeof = _zd_string_sizeofPtr
      .asFunction<int Function()>();

  /// Drops (undeclares and frees) a subscriber.
  ///
  /// After this call the owned subscriber is in gravestone state.
  /// A second drop is a safe no-op.
  ///
  /// @param subscriber  Pointer to a z_owned_subscriber_t to drop.
  void zd_subscriber_drop(
    ffi.Pointer<ffi.Opaque> subscriber,
  ) {
    return _zd_subscriber_drop(
      subscriber,
    );
  }

  late final _zd_subscriber_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_subscriber_drop',
      );
  late final _zd_subscriber_drop = _zd_subscriber_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the size of z_owned_subscriber_t in bytes.
  ///
  /// Used by Dart to allocate the correct amount of native memory
  /// for opaque zenoh types.
  int zd_subscriber_sizeof() {
    return _zd_subscriber_sizeof();
  }

  late final _zd_subscriber_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_subscriber_sizeof');
  late final _zd_subscriber_sizeof = _zd_subscriber_sizeofPtr
      .asFunction<int Function()>();

  /// Reads the 16-byte ZenohId from a raw 24-byte timestamp.
  ///
  /// @param ts      Pointer to a 24-byte raw z_timestamp_t image.
  /// @param out_id  Pointer to a 16-byte buffer to receive the id.
  void zd_timestamp_id(
    ffi.Pointer<ffi.Uint8> ts,
    ffi.Pointer<ffi.Uint8> out_id,
  ) {
    return _zd_timestamp_id(
      ts,
      out_id,
    );
  }

  late final _zd_timestamp_idPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Void Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Uint8>)
        >
      >('zd_timestamp_id');
  late final _zd_timestamp_id = _zd_timestamp_idPtr
      .asFunction<
        void Function(ffi.Pointer<ffi.Uint8>, ffi.Pointer<ffi.Uint8>)
      >();

  /// Creates a uhlc timestamp from the session's HLC clock.
  ///
  /// Writes the 24 raw bytes of the resulting z_timestamp_t into out_ts. The
  /// caller MUST check the return code and never surface a garbage timestamp.
  ///
  /// @param session  Const pointer to a loaned session.
  /// @param out_ts   Pointer to a 24-byte buffer to receive the raw timestamp.
  /// @return The zenoh result code (0 = success, negative = error).
  int zd_timestamp_new(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<ffi.Uint8> out_ts,
  ) {
    return _zd_timestamp_new(
      session,
      out_ts,
    );
  }

  late final _zd_timestamp_newPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Uint8>)
        >
      >('zd_timestamp_new');
  late final _zd_timestamp_new = _zd_timestamp_newPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Uint8>)
      >();

  /// Reads the NTP64 unsigned-64 time from a raw 24-byte timestamp.
  ///
  /// @param ts  Pointer to a 24-byte raw z_timestamp_t image.
  /// @return The NTP64 time as an unsigned 64-bit value.
  int zd_timestamp_ntp64_time(
    ffi.Pointer<ffi.Uint8> ts,
  ) {
    return _zd_timestamp_ntp64_time(
      ts,
    );
  }

  late final _zd_timestamp_ntp64_timePtr =
      _lookup<ffi.NativeFunction<ffi.Uint64 Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_timestamp_ntp64_time',
      );
  late final _zd_timestamp_ntp64_time = _zd_timestamp_ntp64_timePtr
      .asFunction<int Function(ffi.Pointer<ffi.Uint8>)>();

  /// Returns the size of z_timestamp_t in bytes (the raw timestamp image,
  /// expected 24).
  ///
  /// Lets Dart size timestamp buffers from the generator-emitted layout instead
  /// of a hardcoded literal, so a future zenoh-c layout change trips the Dart
  /// assert rather than silently mis-sizing the buffer.
  int zd_timestamp_sizeof() {
    return _zd_timestamp_sizeof();
  }

  late final _zd_timestamp_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_timestamp_sizeof');
  late final _zd_timestamp_sizeof = _zd_timestamp_sizeofPtr
      .asFunction<int Function()>();

  /// Undeclares a key expression from the session.
  ///
  /// 🔴 The handle is CONSUMED on every return code, including errors: canon
  /// takes the value out before it checks the gravestone and before it calls
  /// the session. A caller must therefore mark its handle dead regardless of
  /// what this returns. Undeclaring on a session other than the declaring one
  /// fails with Z_EGENERIC and still consumes.
  ///
  /// @param session   Const pointer to a loaned (live) session.
  /// @param key_expr  Pointer to an owned z_owned_keyexpr_t. Left gravestoned;
  /// the caller still owns the storage and must free it.
  /// @return 0 on success, Z_EINVAL (-1) on a gravestone input, Z_EGENERIC
  /// (-128) for every other failure.
  int zd_undeclare_keyexpr(
    ffi.Pointer<ffi.Opaque> session,
    ffi.Pointer<z_owned_keyexpr_t> key_expr,
  ) {
    return _zd_undeclare_keyexpr(
      session,
      key_expr,
    );
  }

  late final _zd_undeclare_keyexprPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<z_owned_keyexpr_t>,
          )
        >
      >('zd_undeclare_keyexpr');
  late final _zd_undeclare_keyexpr = _zd_undeclare_keyexprPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<z_owned_keyexpr_t>)
      >();

  /// Creates a view key expression from a buffer and an explicit byte length.
  ///
  /// The buffer must remain valid for the lifetime of the view (the view
  /// BORROWS it -- it does not copy). It need not be null-terminated: `len` is
  /// authoritative. This is the length-carried entry, and it is the only one:
  /// the key expression grammar forbids `//`, a leading or trailing `/`, and the
  /// characters `?#$`, but it does NOT forbid an interior NUL, so a C-string
  /// entry measuring with strlen would silently truncate a real domain value.
  ///
  /// @param ke    Pointer to an uninitialized z_view_keyexpr_t. Written on
  /// every path, including failure (canon writes a gravestone).
  /// @param expr  Pointer to `len` bytes of UTF-8 key expression text.
  /// @param len   Length of `expr` in bytes.
  /// @return 0 on success, Z_EINVAL (-1) if the expression is invalid.
  int zd_view_keyexpr_from_substr(
    ffi.Pointer<ffi.Opaque> ke,
    ffi.Pointer<ffi.Char> expr,
    int len,
  ) {
    return _zd_view_keyexpr_from_substr(
      ke,
      expr,
      len,
    );
  }

  late final _zd_view_keyexpr_from_substrPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Int Function(
            ffi.Pointer<ffi.Opaque>,
            ffi.Pointer<ffi.Char>,
            ffi.Size,
          )
        >
      >('zd_view_keyexpr_from_substr');
  late final _zd_view_keyexpr_from_substr = _zd_view_keyexpr_from_substrPtr
      .asFunction<
        int Function(ffi.Pointer<ffi.Opaque>, ffi.Pointer<ffi.Char>, int)
      >();

  /// Obtains a const loaned reference to the key expression.
  ///
  /// @param ke  Pointer to a valid z_view_keyexpr_t.
  /// @return Const pointer to the loaned key expression.
  ffi.Pointer<ffi.Opaque> zd_view_keyexpr_loan(
    ffi.Pointer<ffi.Opaque> ke,
  ) {
    return _zd_view_keyexpr_loan(
      ke,
    );
  }

  late final _zd_view_keyexpr_loanPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_view_keyexpr_loan');
  late final _zd_view_keyexpr_loan = _zd_view_keyexpr_loanPtr
      .asFunction<ffi.Pointer<ffi.Opaque> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the size of z_view_keyexpr_t in bytes.
  ///
  /// Used by Dart to allocate the correct amount of native memory
  /// for opaque zenoh types.
  int zd_view_keyexpr_sizeof() {
    return _zd_view_keyexpr_sizeof();
  }

  late final _zd_view_keyexpr_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>(
        'zd_view_keyexpr_sizeof',
      );
  late final _zd_view_keyexpr_sizeof = _zd_view_keyexpr_sizeofPtr
      .asFunction<int Function()>();

  /// Returns a pointer to the slice data.
  ///
  /// @param slice  Const pointer to a z_view_slice_t.
  /// @return Pointer to the data bytes.
  ffi.Pointer<ffi.Uint8> zd_view_slice_data(
    ffi.Pointer<z_view_slice_t> slice,
  ) {
    return _zd_view_slice_data(
      slice,
    );
  }

  late final _zd_view_slice_dataPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Uint8> Function(ffi.Pointer<z_view_slice_t>)
        >
      >('zd_view_slice_data');
  late final _zd_view_slice_data = _zd_view_slice_dataPtr
      .asFunction<
        ffi.Pointer<ffi.Uint8> Function(ffi.Pointer<z_view_slice_t>)
      >();

  /// Returns the length of the slice data.
  ///
  /// @param slice  Const pointer to a z_view_slice_t.
  /// @return Number of bytes in the slice.
  int zd_view_slice_len(
    ffi.Pointer<z_view_slice_t> slice,
  ) {
    return _zd_view_slice_len(
      slice,
    );
  }

  late final _zd_view_slice_lenPtr =
      _lookup<
        ffi.NativeFunction<ffi.Size Function(ffi.Pointer<z_view_slice_t>)>
      >('zd_view_slice_len');
  late final _zd_view_slice_len = _zd_view_slice_lenPtr
      .asFunction<int Function(ffi.Pointer<z_view_slice_t>)>();

  /// Returns the size of z_view_slice_t in bytes.
  int zd_view_slice_sizeof() {
    return _zd_view_slice_sizeof();
  }

  late final _zd_view_slice_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_view_slice_sizeof');
  late final _zd_view_slice_sizeof = _zd_view_slice_sizeofPtr
      .asFunction<int Function()>();

  /// Returns a pointer to the data of a view string.
  ///
  /// Internally loans the view string and calls z_string_data on the loaned ref.
  /// The returned pointer is NOT guaranteed to be null-terminated.
  ///
  /// @param str  Pointer to a valid z_view_string_t.
  /// @return Pointer to the string data.
  ffi.Pointer<ffi.Char> zd_view_string_data(
    ffi.Pointer<ffi.Opaque> str,
  ) {
    return _zd_view_string_data(
      str,
    );
  }

  late final _zd_view_string_dataPtr =
      _lookup<
        ffi.NativeFunction<
          ffi.Pointer<ffi.Char> Function(ffi.Pointer<ffi.Opaque>)
        >
      >('zd_view_string_data');
  late final _zd_view_string_data = _zd_view_string_dataPtr
      .asFunction<ffi.Pointer<ffi.Char> Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the length of a view string (in bytes, NOT including any terminator).
  ///
  /// Internally loans the view string and calls z_string_len on the loaned ref.
  ///
  /// @param str  Pointer to a valid z_view_string_t.
  /// @return Length of the string data in bytes.
  int zd_view_string_len(
    ffi.Pointer<ffi.Opaque> str,
  ) {
    return _zd_view_string_len(
      str,
    );
  }

  late final _zd_view_string_lenPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function(ffi.Pointer<ffi.Opaque>)>>(
        'zd_view_string_len',
      );
  late final _zd_view_string_len = _zd_view_string_lenPtr
      .asFunction<int Function(ffi.Pointer<ffi.Opaque>)>();

  /// Returns the size of z_view_string_t in bytes.
  ///
  /// Used by Dart to allocate the correct amount of native memory
  /// for opaque zenoh types.
  int zd_view_string_sizeof() {
    return _zd_view_string_sizeof();
  }

  late final _zd_view_string_sizeofPtr =
      _lookup<ffi.NativeFunction<ffi.Size Function()>>('zd_view_string_sizeof');
  late final _zd_view_string_sizeof = _zd_view_string_sizeofPtr
      .asFunction<int Function()>();

  /// Releases a buffer handed out by the two collectors.
  ///
  /// `NULL` is a safe no-op. A second call on the same non-`NULL` pointer is a
  /// **double free**: this entry is a raw free(), it holds no owned handle and so
  /// has no gravestone to check. The house puts idempotence on the Dart side --
  /// the same shape as zd_query_drop, whose caller guards with `Query._disposed`.
  ///
  /// @param ids  A buffer received through a collector's `out_ids`, or NULL.
  void zd_zid_list_drop(
    ffi.Pointer<ffi.Uint8> ids,
  ) {
    return _zd_zid_list_drop(
      ids,
    );
  }

  late final _zd_zid_list_dropPtr =
      _lookup<ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Uint8>)>>(
        'zd_zid_list_drop',
      );
  late final _zd_zid_list_drop = _zd_zid_list_dropPtr
      .asFunction<void Function(ffi.Pointer<ffi.Uint8>)>();
}

final class UnnamedStruct extends ffi.Struct {
  @ffi.UnsignedInt()
  external int __low;

  @ffi.UnsignedInt()
  external int __high;

  static ffi.Pointer<UnnamedStruct> $allocate(
    ffi.Allocator $allocator, {
    required int $low,
    required int $high,
  }) => $allocator<UnnamedStruct>()
    ..ref.__low = $low
    ..ref.__high = $high;
}

final class __atomic_wide_counter extends ffi.Union {
  @ffi.UnsignedLongLong()
  external int __value64;

  external UnnamedStruct __value32;
}

final class __pthread_cond_s extends ffi.Struct {
  external __atomic_wide_counter __wseq;

  external __atomic_wide_counter __g1_start;

  @ffi.Array.multi([2])
  external ffi.Array<ffi.UnsignedInt> __g_size;

  @ffi.UnsignedInt()
  external int __g1_orig_size;

  @ffi.UnsignedInt()
  external int __wrefs;

  @ffi.Array.multi([2])
  external ffi.Array<ffi.UnsignedInt> __g_signals;

  @ffi.UnsignedInt()
  external int __unused_initialized_1;

  @ffi.UnsignedInt()
  external int __unused_initialized_2;
}

final class __pthread_internal_list extends ffi.Struct {
  external ffi.Pointer<__pthread_internal_list> __prev;

  external ffi.Pointer<__pthread_internal_list> __next;
}

final class __pthread_mutex_s extends ffi.Struct {
  @ffi.Int()
  external int __lock;

  @ffi.UnsignedInt()
  external int __count;

  @ffi.Int()
  external int __owner;

  @ffi.UnsignedInt()
  external int __nusers;

  @ffi.Int()
  external int __kind;

  @ffi.Short()
  external int __spins;

  @ffi.Short()
  external int __unused;

  external __pthread_internal_list __list;
}

final class __pthread_rwlock_arch_t extends ffi.Struct {
  @ffi.UnsignedInt()
  external int __readers;

  @ffi.UnsignedInt()
  external int __writers;

  @ffi.UnsignedInt()
  external int __wrphase_futex;

  @ffi.UnsignedInt()
  external int __writers_futex;

  @ffi.UnsignedInt()
  external int __pad3;

  @ffi.UnsignedInt()
  external int __pad4;

  @ffi.Int()
  external int __cur_writer;

  @ffi.Int()
  external int __shared;

  @ffi.UnsignedLong()
  external int __pad1;

  @ffi.UnsignedLong()
  external int __pad2;

  @ffi.UnsignedInt()
  external int __flags;
}

final class pthread_attr_t extends ffi.Union {
  @ffi.Array.multi([56])
  external ffi.Array<ffi.Char> __size;

  @ffi.Long()
  external int __align;
}

final class pthread_barrier_t extends ffi.Union {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Char> __size;

  @ffi.Long()
  external int __align;
}

final class pthread_barrierattr_t extends ffi.Union {
  @ffi.Array.multi([4])
  external ffi.Array<ffi.Char> __size;

  @ffi.Int()
  external int __align;
}

final class pthread_cond_t extends ffi.Union {
  external __pthread_cond_s __data;

  @ffi.Array.multi([48])
  external ffi.Array<ffi.Char> __size;

  @ffi.LongLong()
  external int __align;
}

final class pthread_condattr_t extends ffi.Union {
  @ffi.Array.multi([4])
  external ffi.Array<ffi.Char> __size;

  @ffi.Int()
  external int __align;
}

final class pthread_mutex_t extends ffi.Union {
  external __pthread_mutex_s __data;

  @ffi.Array.multi([40])
  external ffi.Array<ffi.Char> __size;

  @ffi.Long()
  external int __align;
}

final class pthread_mutexattr_t extends ffi.Union {
  @ffi.Array.multi([4])
  external ffi.Array<ffi.Char> __size;

  @ffi.Int()
  external int __align;
}

final class pthread_rwlock_t extends ffi.Union {
  external __pthread_rwlock_arch_t __data;

  @ffi.Array.multi([56])
  external ffi.Array<ffi.Char> __size;

  @ffi.Long()
  external int __align;
}

final class pthread_rwlockattr_t extends ffi.Union {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Char> __size;

  @ffi.Long()
  external int __align;
}

/// An iterator over slices of serialized data.
final class z_bytes_slice_iterator_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A Zenoh ID.
///
/// In general, valid Zenoh IDs are LSB-first 128bit unsigned and non-zero integers.
final class z_id_t extends ffi.Struct {
  @ffi.Array.multi([16])
  external ffi.Array<ffi.Uint8> id;
}

/// A loaned Zenoh data.
final class z_loaned_bytes_t extends ffi.Struct {
  @ffi.Array.multi([40])
  external ffi.Array<ffi.Uint8> _0;
}

/// An loaned writer for payload.
final class z_loaned_bytes_writer_t extends ffi.Struct {
  @ffi.Array.multi([64])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A loaned cancellation token, which can be used to interrupt GET queries.
final class z_loaned_cancellation_token_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// Loaned closure.
final class z_loaned_closure_hello_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_hello_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_hello_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

final class z_loaned_closure_link_event_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_link_event_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_link_event_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

final class z_loaned_closure_link_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_link_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_link_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief Loaned closure.
final class z_loaned_closure_matching_status_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_matching_status_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_matching_status_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

/// Loaned closure.
final class z_loaned_closure_query_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_query_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_query_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

/// Loaned closure.
final class z_loaned_closure_reply_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_reply_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_reply_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

/// Loaned closure.
final class z_loaned_closure_sample_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_sample_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_sample_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

final class z_loaned_closure_transport_event_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_transport_event_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_transport_event_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

final class z_loaned_closure_transport_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_transport_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_transport_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

/// @brief Loaned closure.
final class z_loaned_closure_zid_t extends ffi.Struct {
  @ffi.Size()
  external int _0;

  @ffi.Size()
  external int _1;

  @ffi.Size()
  external int _2;

  static ffi.Pointer<z_loaned_closure_zid_t> $allocate(
    ffi.Allocator $allocator, {
    required int $0,
    required int $1,
    required int $2,
  }) => $allocator<z_loaned_closure_zid_t>()
    ..ref._0 = $0
    ..ref._1 = $1
    ..ref._2 = $2;
}

/// A loaned conditional variable.
final class z_loaned_condvar_t extends ffi.Struct {
  @ffi.Array.multi([4])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh configuration.
final class z_loaned_config_t extends ffi.Struct {
  @ffi.Array.multi([2008])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh encoding.
final class z_loaned_encoding_t extends ffi.Struct {
  @ffi.Array.multi([48])
  external ffi.Array<ffi.Uint8> _0;
}

/// An loaned Zenoh fifo query handler.
final class z_loaned_fifo_handler_query_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An loaned Zenoh fifo reply handler.
final class z_loaned_fifo_handler_reply_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An loaned Zenoh fifo sample handler.
final class z_loaned_fifo_handler_sample_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned hello message.
final class z_loaned_hello_t extends ffi.Struct {
  @ffi.Array.multi([48])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned key expression.
///
/// Key expressions can identify a single key or a set of keys.
///
/// Examples :
/// - ``"key/expression"``.
/// - ``"key/ex*"``.
///
/// Using `z_declare_keyexpr` allows Zenoh to optimize a key expression,
/// both for local processing and network-wise.
final class z_loaned_keyexpr_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A loaned LinkEvent structure.
final class z_loaned_link_event_t extends ffi.Struct {
  @ffi.Array.multi([152])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A loaned LinkEventsListener structure.
final class z_loaned_link_events_listener_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A loaned Link structure.
final class z_loaned_link_t extends ffi.Struct {
  @ffi.Array.multi([144])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A loaned liveliness token.
final class z_loaned_liveliness_token_t extends ffi.Struct {
  @ffi.Array.multi([16])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A loaned MemoryLayout.
final class z_loaned_memory_layout_t extends ffi.Struct {
  @ffi.Array.multi([16])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned mutex.
final class z_loaned_mutex_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A loaned ShmProvider's PrecomputedLayout.
final class z_loaned_precomputed_layout_t extends ffi.Struct {
  @ffi.Array.multi([40])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A loaned pointer in SHM Segment.
final class z_loaned_ptr_in_segment_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh publisher.
final class z_loaned_publisher_t extends ffi.Struct {
  @ffi.Array.multi([120])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh queryable.
final class z_loaned_querier_t extends ffi.Struct {
  @ffi.Array.multi([88])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh query.
final class z_loaned_query_t extends ffi.Struct {
  @ffi.Array.multi([144])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh queryable.
final class z_loaned_queryable_t extends ffi.Struct {
  @ffi.Array.multi([56])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh reply error.
final class z_loaned_reply_err_t extends ffi.Struct {
  @ffi.Array.multi([88])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned reply.
final class z_loaned_reply_t extends ffi.Struct {
  @ffi.Array.multi([248])
  external ffi.Array<ffi.Uint8> _0;
}

/// An loaned Zenoh ring query handler.
final class z_loaned_ring_handler_query_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An loaned Zenoh ring reply handler.
final class z_loaned_ring_handler_reply_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An loaned Zenoh ring sample handler.
final class z_loaned_ring_handler_sample_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh sample.
final class z_loaned_sample_t extends ffi.Struct {
  @ffi.Array.multi([224])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh session.
final class z_loaned_session_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A loaned shared ShmProvider.
final class z_loaned_shared_shm_provider_t extends ffi.Struct {
  @ffi.Array.multi([104])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned SHM Client Storage.
final class z_loaned_shm_client_storage_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A loaned ZShmMut slice.
final class z_loaned_shm_mut_t extends ffi.Struct {
  @ffi.Array.multi([80])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A loaned ShmProvider.
final class z_loaned_shm_provider_t extends ffi.Struct {
  @ffi.Array.multi([104])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A loaned ZShm slice.
final class z_loaned_shm_t extends ffi.Struct {
  @ffi.Array.multi([80])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned sequence of bytes.
final class z_loaned_slice_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned string array.
final class z_loaned_string_array_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned string.
final class z_loaned_string_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// A loaned Zenoh subscriber.
final class z_loaned_subscriber_t extends ffi.Struct {
  @ffi.Array.multi([56])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A loaned TransportEvent structure.
final class z_loaned_transport_event_t extends ffi.Struct {
  @ffi.Array.multi([21])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A loaned TransportEventsListener structure.
final class z_loaned_transport_events_listener_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A loaned Transport structure.
final class z_loaned_transport_t extends ffi.Struct {
  @ffi.Array.multi([20])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A struct that indicates if there exist Subscribers matching the Publisher's key expression or Queryables matching Querier's key expression and target.
final class z_matching_status_t extends ffi.Struct {
  /// True if there exist matching Zenoh entities, false otherwise.
  @ffi.Bool()
  external bool matching;

  static ffi.Pointer<z_matching_status_t> $allocate(
    ffi.Allocator $allocator, {
    required bool matching,
  }) => $allocator<z_matching_status_t>()..ref.matching = matching;
}

final class z_moved_bytes_t extends ffi.Struct {
  external z_owned_bytes_t _this;
}

final class z_moved_bytes_writer_t extends ffi.Struct {
  external z_owned_bytes_writer_t _this;
}

final class z_moved_cancellation_token_t extends ffi.Struct {
  external z_owned_cancellation_token_t _this;
}

final class z_moved_chunk_alloc_result_t extends ffi.Struct {
  external z_owned_chunk_alloc_result_t _this;
}

/// Moved closure.
final class z_moved_closure_hello_t extends ffi.Struct {
  external z_owned_closure_hello_t _this;
}

final class z_moved_closure_link_event_t extends ffi.Struct {
  external z_owned_closure_link_event_t _this;
}

final class z_moved_closure_link_t extends ffi.Struct {
  external z_owned_closure_link_t _this;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief Moved closure.
final class z_moved_closure_matching_status_t extends ffi.Struct {
  external z_owned_closure_matching_status_t _this;
}

/// Moved closure.
final class z_moved_closure_query_t extends ffi.Struct {
  external z_owned_closure_query_t _this;
}

/// Moved closure.
final class z_moved_closure_reply_t extends ffi.Struct {
  external z_owned_closure_reply_t _this;
}

/// Moved closure.
final class z_moved_closure_sample_t extends ffi.Struct {
  external z_owned_closure_sample_t _this;
}

final class z_moved_closure_transport_event_t extends ffi.Struct {
  external z_owned_closure_transport_event_t _this;
}

final class z_moved_closure_transport_t extends ffi.Struct {
  external z_owned_closure_transport_t _this;
}

/// @brief Moved closure.
final class z_moved_closure_zid_t extends ffi.Struct {
  external z_owned_closure_zid_t _this;
}

final class z_moved_condvar_t extends ffi.Struct {
  external z_owned_condvar_t _this;
}

final class z_moved_config_t extends ffi.Struct {
  external z_owned_config_t _this;
}

final class z_moved_encoding_t extends ffi.Struct {
  external z_owned_encoding_t _this;
}

final class z_moved_fifo_handler_query_t extends ffi.Struct {
  external z_owned_fifo_handler_query_t _this;
}

final class z_moved_fifo_handler_reply_t extends ffi.Struct {
  external z_owned_fifo_handler_reply_t _this;
}

final class z_moved_fifo_handler_sample_t extends ffi.Struct {
  external z_owned_fifo_handler_sample_t _this;
}

final class z_moved_hello_t extends ffi.Struct {
  external z_owned_hello_t _this;
}

final class z_moved_keyexpr_t extends ffi.Struct {
  external z_owned_keyexpr_t _this;
}

final class z_moved_link_event_t extends ffi.Struct {
  external z_owned_link_event_t _this;
}

final class z_moved_link_events_listener_t extends ffi.Struct {
  external z_owned_link_events_listener_t _this;
}

final class z_moved_link_t extends ffi.Struct {
  external z_owned_link_t _this;
}

final class z_moved_liveliness_token_t extends ffi.Struct {
  external z_owned_liveliness_token_t _this;
}

final class z_moved_matching_listener_t extends ffi.Struct {
  external z_owned_matching_listener_t _this;
}

final class z_moved_memory_layout_t extends ffi.Struct {
  external z_owned_memory_layout_t _this;
}

final class z_moved_mutex_t extends ffi.Struct {
  external z_owned_mutex_t _this;
}

final class z_moved_precomputed_layout_t extends ffi.Struct {
  external z_owned_precomputed_layout_t _this;
}

final class z_moved_ptr_in_segment_t extends ffi.Struct {
  external z_owned_ptr_in_segment_t _this;
}

final class z_moved_publisher_t extends ffi.Struct {
  external z_owned_publisher_t _this;
}

final class z_moved_querier_t extends ffi.Struct {
  external z_owned_querier_t _this;
}

final class z_moved_query_t extends ffi.Struct {
  external z_owned_query_t _this;
}

final class z_moved_queryable_t extends ffi.Struct {
  external z_owned_queryable_t _this;
}

final class z_moved_reply_err_t extends ffi.Struct {
  external z_owned_reply_err_t _this;
}

final class z_moved_reply_t extends ffi.Struct {
  external z_owned_reply_t _this;
}

final class z_moved_ring_handler_query_t extends ffi.Struct {
  external z_owned_ring_handler_query_t _this;
}

final class z_moved_ring_handler_reply_t extends ffi.Struct {
  external z_owned_ring_handler_reply_t _this;
}

final class z_moved_ring_handler_sample_t extends ffi.Struct {
  external z_owned_ring_handler_sample_t _this;
}

final class z_moved_sample_t extends ffi.Struct {
  external z_owned_sample_t _this;
}

final class z_moved_session_t extends ffi.Struct {
  external z_owned_session_t _this;
}

final class z_moved_shared_shm_provider_t extends ffi.Struct {
  external z_owned_shared_shm_provider_t _this;
}

final class z_moved_shm_client_storage_t extends ffi.Struct {
  external z_owned_shm_client_storage_t _this;
}

final class z_moved_shm_client_t extends ffi.Struct {
  external z_owned_shm_client_t _this;
}

final class z_moved_shm_mut_t extends ffi.Struct {
  external z_owned_shm_mut_t _this;
}

final class z_moved_shm_provider_t extends ffi.Struct {
  external z_owned_shm_provider_t _this;
}

final class z_moved_shm_t extends ffi.Struct {
  external z_owned_shm_t _this;
}

final class z_moved_slice_t extends ffi.Struct {
  external z_owned_slice_t _this;
}

final class z_moved_string_array_t extends ffi.Struct {
  external z_owned_string_array_t _this;
}

final class z_moved_string_t extends ffi.Struct {
  external z_owned_string_t _this;
}

final class z_moved_subscriber_t extends ffi.Struct {
  external z_owned_subscriber_t _this;
}

final class z_moved_task_t extends ffi.Struct {
  external z_owned_task_t _this;
}

final class z_moved_transport_event_t extends ffi.Struct {
  external z_owned_transport_event_t _this;
}

final class z_moved_transport_events_listener_t extends ffi.Struct {
  external z_owned_transport_events_listener_t _this;
}

final class z_moved_transport_t extends ffi.Struct {
  external z_owned_transport_t _this;
}

/// A Zenoh data.
///
/// To minimize copies and reallocations, Zenoh may provide data in several separate buffers.
final class z_owned_bytes_t extends ffi.Struct {
  @ffi.Array.multi([40])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned writer for payload.
final class z_owned_bytes_writer_t extends ffi.Struct {
  @ffi.Array.multi([64])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned cancellation token, which can be used to interrupt GET queries.
final class z_owned_cancellation_token_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned ChunkAllocResult.
final class z_owned_chunk_alloc_result_t extends ffi.Struct {
  @ffi.Array.multi([48])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A hello message-processing closure.
///
/// A closure is a structure that contains all the elements for stateful, memory-leak-free callbacks.
final class z_owned_closure_hello_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(
        ffi.Pointer<z_loaned_hello_t> hello,
        ffi.Pointer<ffi.Void> context,
      )
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_hello_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_loaned_hello_t> hello,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_hello_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

final class z_owned_closure_link_event_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(
        ffi.Pointer<z_loaned_link_event_t> event,
        ffi.Pointer<ffi.Void> context,
      )
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_link_event_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_loaned_link_event_t> event,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_link_event_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

final class z_owned_closure_link_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(
        ffi.Pointer<z_loaned_link_t> link,
        ffi.Pointer<ffi.Void> context,
      )
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_link_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_loaned_link_t> link,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_link_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A matching status-processing closure.
///
/// A closure is a structure that contains all the elements for stateful, memory-leak-free callbacks.
final class z_owned_closure_matching_status_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(
        ffi.Pointer<z_matching_status_t> matching_status,
        ffi.Pointer<ffi.Void> context,
      )
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_matching_status_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_matching_status_t> matching_status,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_matching_status_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

/// @brief A query-processing closure.
///
/// A closure is a structure that contains all the elements for stateful, memory-leak-free callbacks.
final class z_owned_closure_query_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(
        ffi.Pointer<z_loaned_query_t> query,
        ffi.Pointer<ffi.Void> context,
      )
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_query_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_loaned_query_t> query,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_query_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

/// @brief A reply-processing closure.
///
/// A closure is a structure that contains all the elements for stateful, memory-leak-free callbacks.
final class z_owned_closure_reply_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(
        ffi.Pointer<z_loaned_reply_t> reply,
        ffi.Pointer<ffi.Void> context,
      )
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_reply_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_loaned_reply_t> reply,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_reply_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

/// @brief A sample-processing closure.
///
/// A closure is a structure that contains all the elements for stateful, memory-leak-free callbacks.
final class z_owned_closure_sample_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(
        ffi.Pointer<z_loaned_sample_t> sample,
        ffi.Pointer<ffi.Void> context,
      )
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_sample_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_loaned_sample_t> sample,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_sample_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

final class z_owned_closure_transport_event_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(
        ffi.Pointer<z_loaned_transport_event_t> event,
        ffi.Pointer<ffi.Void> context,
      )
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_transport_event_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_loaned_transport_event_t> event,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_transport_event_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

final class z_owned_closure_transport_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(
        ffi.Pointer<z_loaned_transport_t> transport,
        ffi.Pointer<ffi.Void> context,
      )
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_transport_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_loaned_transport_t> transport,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_transport_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

/// @brief A zenoh id-processing closure.
///
/// A closure is a structure that contains all the elements for stateful, memory-leak-free callbacks:
final class z_owned_closure_zid_t extends ffi.Struct {
  external ffi.Pointer<ffi.Void> _context;

  external ffi.Pointer<
    ffi.NativeFunction<
      ffi.Void Function(ffi.Pointer<z_id_t> z_id, ffi.Pointer<ffi.Void> context)
    >
  >
  _call;

  external ffi.Pointer<
    ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
  >
  _drop;

  static ffi.Pointer<z_owned_closure_zid_t> $allocate(
    ffi.Allocator $allocator, {
    required ffi.Pointer<ffi.Void> $context,
    required ffi.Pointer<
      ffi.NativeFunction<
        ffi.Void Function(
          ffi.Pointer<z_id_t> z_id,
          ffi.Pointer<ffi.Void> context,
        )
      >
    >
    $call,
    required ffi.Pointer<
      ffi.NativeFunction<ffi.Void Function(ffi.Pointer<ffi.Void> context)>
    >
    $drop,
  }) => $allocator<z_owned_closure_zid_t>()
    ..ref._context = $context
    ..ref._call = $call
    ..ref._drop = $drop;
}

/// An owned conditional variable.
///
/// Used in combination with `z_owned_mutex_t` to wake up thread when certain conditions are met.
final class z_owned_condvar_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh configuration.
final class z_owned_config_t extends ffi.Struct {
  @ffi.Array.multi([2008])
  external ffi.Array<ffi.Uint8> _0;
}

/// The <a href="https://zenoh.io/docs/manual/abstractions/#encoding"> encoding </a> of Zenoh data.
final class z_owned_encoding_t extends ffi.Struct {
  @ffi.Array.multi([48])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh fifo query handler.
final class z_owned_fifo_handler_query_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh fifo reply handler.
final class z_owned_fifo_handler_reply_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh fifo sample handler.
final class z_owned_fifo_handler_sample_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh-allocated hello message returned by a Zenoh entity to a scout message sent with `z_scout()`.
final class z_owned_hello_t extends ffi.Struct {
  @ffi.Array.multi([48])
  external ffi.Array<ffi.Uint8> _0;
}

/// A Zenoh-allocated <a href="https://zenoh.io/docs/manual/abstractions/#key-expression"> key expression </a>.
///
/// Key expressions can identify a single key or a set of keys.
///
/// Examples :
/// - ``"key/expression"``.
/// - ``"key/ex*"``.
///
/// Key expressions can be mapped to numerical ids through `z_declare_keyexpr`
/// for wire and computation efficiency.
///
/// Internally key expressiobn can be either:
/// - A plain string expression.
/// - A pure numerical id.
/// - The combination of a numerical prefix and a string suffix.
final class z_owned_keyexpr_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief The event notifyting about addition or removal of a link `z_owned_link_t`
///
/// Used in Zenoh connectivity API to notify about establishment or break of data links with remote zenoh nodes.
final class z_owned_link_event_t extends ffi.Struct {
  @ffi.Array.multi([152])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A listener for link events.
///
/// Used in Zenoh connectivity API to get notified about establishment or break of data links with remote zenoh nodes.
final class z_owned_link_events_listener_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A Zenoh link structure returned by Zenoh connectivity API.
///
/// Represents an actual data link with a remote zenoh node over a specific protocol.
final class z_owned_link_t extends ffi.Struct {
  @ffi.Array.multi([144])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A liveliness token that can be used to provide the network with information about connectivity to its
/// declarer: when constructed, a PUT sample will be received by liveliness subscribers on intersecting key
/// expressions.
///
/// A DELETE on the token's key expression will be received by subscribers if the token is destroyed, or if connectivity between the subscriber and the token's creator is lost.
final class z_owned_liveliness_token_t extends ffi.Struct {
  @ffi.Array.multi([16])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief An owned Zenoh matching listener.
///
/// A listener that sends notifications when the [`MatchingStatus`] of a publisher or querier changes.
/// Dropping the corresponding publisher, also drops matching listener.
final class z_owned_matching_listener_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned MemoryLayout.
final class z_owned_memory_layout_t extends ffi.Struct {
  @ffi.Array.multi([16])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned mutex.
final class z_owned_mutex_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned ShmProvider's PrecomputedLayout.
final class z_owned_precomputed_layout_t extends ffi.Struct {
  @ffi.Array.multi([40])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief A pointer in SHM Segment.
final class z_owned_ptr_in_segment_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh <a href="https://zenoh.io/docs/manual/abstractions/#publisher"> publisher </a>.
final class z_owned_publisher_t extends ffi.Struct {
  @ffi.Array.multi([120])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh querier.
///
/// Sends queries to matching queryables.
final class z_owned_querier_t extends ffi.Struct {
  @ffi.Array.multi([88])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh query received by a queryable.
///
/// Queries are atomically reference-counted, letting you extract them from the callback that handed them to you by cloning.
final class z_owned_query_t extends ffi.Struct {
  @ffi.Array.multi([144])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh <a href="https://zenoh.io/docs/manual/abstractions/#queryable"> queryable </a>.
///
/// Responds to queries sent via `z_get()` with intersecting key expression.
final class z_owned_queryable_t extends ffi.Struct {
  @ffi.Array.multi([56])
  external ffi.Array<ffi.Uint8> _0;
}

/// A Zenoh reply error - a combination of reply error payload and its encoding.
final class z_owned_reply_err_t extends ffi.Struct {
  @ffi.Array.multi([88])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned reply from a Queryable to a `z_get()`.
final class z_owned_reply_t extends ffi.Struct {
  @ffi.Array.multi([248])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh ring query handler.
final class z_owned_ring_handler_query_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh ring reply handler.
final class z_owned_ring_handler_reply_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh ring sample handler.
final class z_owned_ring_handler_sample_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh sample.
///
/// This is a read only type that can only be constructed by cloning a `z_loaned_sample_t`.
/// Like all owned types, it should be freed using z_drop or z_sample_drop.
final class z_owned_sample_t extends ffi.Struct {
  @ffi.Array.multi([224])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh session.
final class z_owned_session_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned shared ShmProvider.
final class z_owned_shared_shm_provider_t extends ffi.Struct {
  @ffi.Array.multi([104])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned SHM Client Storage
final class z_owned_shm_client_storage_t extends ffi.Struct {
  @ffi.Array.multi([8])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned SHM Client.
final class z_owned_shm_client_t extends ffi.Struct {
  @ffi.Array.multi([16])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned ZShmMut slice.
final class z_owned_shm_mut_t extends ffi.Struct {
  @ffi.Array.multi([80])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned ShmProvider.
final class z_owned_shm_provider_t extends ffi.Struct {
  @ffi.Array.multi([104])
  external ffi.Array<ffi.Uint8> _0;
}

/// @warning This API has been marked as unstable: it works as advertised, but it may be changed in a future release.
/// @brief An owned ZShm slice.
final class z_owned_shm_t extends ffi.Struct {
  @ffi.Array.multi([80])
  external ffi.Array<ffi.Uint8> _0;
}

final class z_owned_slice_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// An array of maybe-owned non-null terminated strings.
final class z_owned_string_array_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// The wrapper type for strings allocated by Zenoh.
final class z_owned_string_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh <a href="https://zenoh.io/docs/manual/abstractions/#subscriber"> subscriber </a>.
///
/// Receives data from publication on intersecting key expressions.
/// Destroying the subscriber cancels the subscription.
final class z_owned_subscriber_t extends ffi.Struct {
  @ffi.Array.multi([56])
  external ffi.Array<ffi.Uint8> _0;
}

/// An owned Zenoh task.
final class z_owned_task_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief The event notifyting about addition or removal of a transport `z_owned_transport_t`
///
/// Used in Zenoh connectivity API to notify about connecting or disconnecting to remote zenoh nodes.
final class z_owned_transport_event_t extends ffi.Struct {
  @ffi.Array.multi([21])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A listener for transport events.
///
/// Used in Zenoh connectivity API to get notified about connecting or disconnecting to remote zenoh nodes.
final class z_owned_transport_events_listener_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A Transport structure returned by Zenoh connectivity API.
///
/// Represents a remote zenoh node connected to this node. Only one transport per remote node exists.
/// Each transport can have multiple corresponding `z_owned_link_t` which represent
/// actual established data links with various protocols.
final class z_owned_transport_t extends ffi.Struct {
  @ffi.Array.multi([20])
  external ffi.Array<ffi.Uint8> _0;
}

/// A user allocated string, viewed as a key expression.
final class z_view_keyexpr_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// A contiguous sequence of bytes owned by some other entity.
final class z_view_slice_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// The view over a string.
final class z_view_string_t extends ffi.Struct {
  @ffi.Array.multi([32])
  external ffi.Array<ffi.Uint8> _0;
}

/// Canon's tagged allocation result, flattened for the FFI seam.
///
/// Canon reports every allocation through `z_buf_layout_alloc_result_t`, a
/// four-slot tagged union: a status, the buffer, and one error enum per
/// failure class. The buffer travels out through its own `buf` out-param (Dart
/// already owns a `z_owned_shm_mut_t` slot for it), so this struct carries the
/// three discriminating scalars.
///
/// ⚠️ **FIELD VALIDITY IS STATUS-GATED, and the gating is done HERE, not by
/// the caller.** Canon backfills the error field its status did NOT select
/// with an arbitrary member -- on an OK result it writes `alloc_error = OTHER`
/// and `layout_error = PROVIDER_INCOMPATIBLE_LAYOUT`, which are real enum
/// members meaning nothing. That placeholder garbage never crosses this seam:
/// the shim writes canon's raw value into the one field `status` selects and
/// writes **-1** into the other. -1 is outside both canon domains, so a caller
/// that reads the wrong field gets an obviously-invalid value rather than a
/// plausible one.
final class zd_shm_alloc_result_t extends ffi.Struct {
  /// Canon's `zc_buf_layout_alloc_status_t`, verbatim: 0 OK, 1 ALLOC_ERROR,
  /// 2 LAYOUT_ERROR. **Only canon's own values ever appear here** -- the
  /// shim's own signal lives in the return code, never in this field -- so a
  /// value outside {0,1,2} means canon's contract changed, and the Dart decode
  /// seam treats it as a contract violation.
  @ffi.Int8()
  external int status;

  /// Canon's `z_alloc_error_t` (0 NEED_DEFRAGMENT, 1 OUT_OF_MEMORY, 2 OTHER)
  /// iff `status == 1`; otherwise **-1**.
  @ffi.Int8()
  external int alloc_error;

  /// Canon's `z_layout_error_t` (0 INCORRECT_LAYOUT_ARGS,
  /// 1 PROVIDER_INCOMPATIBLE_LAYOUT) iff `status == 2`; otherwise **-1**.
  @ffi.Int8()
  external int layout_error;

  static ffi.Pointer<zd_shm_alloc_result_t> $allocate(
    ffi.Allocator $allocator, {
    required int status,
    required int alloc_error,
    required int layout_error,
  }) => $allocator<zd_shm_alloc_result_t>()
    ..ref.status = status
    ..ref.alloc_error = alloc_error
    ..ref.layout_error = layout_error;
}

/// @brief A Zenoh serializer.
final class ze_deserializer_t extends ffi.Struct {
  @ffi.Array.multi([24])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief A loaned Zenoh serializer.
final class ze_loaned_serializer_t extends ffi.Struct {
  @ffi.Array.multi([64])
  external ffi.Array<ffi.Uint8> _0;
}

/// @brief An owned Zenoh serializer.
final class ze_owned_serializer_t extends ffi.Struct {
  @ffi.Array.multi([64])
  external ffi.Array<ffi.Uint8> _0;
}
